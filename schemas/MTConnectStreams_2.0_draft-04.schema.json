{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "https://schemas.mtconnect.org/schemas/MTConnectStreams_2.0_draft-04.schema.json",
  "title": "MTConnectStreams",
  "description": "MTConnect MTConnectStreams response, v2 JSON, from SysML model v2.0.",
  "type": "object",
  "required": [
    "MTConnectStreams"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "MTConnectStreams": {
      "$ref": "#/definitions/MTConnectStreams"
    }
  },
  "additionalProperties": false,
  "definitions": {
    "DeviceStream": {
      "type": "object",
      "title": "DeviceStream",
      "description": "{{termplural(organize)}} data reported from a {{block(Device)}}.",
      "properties": {
        "name": {
          "type": "string",
          "description": "name of the {{block(Device)}}. The value reported for {{property(name,DeviceStream)}} **MUST** be the same as the value defined for the {{property(name,Device)}} attribute of the same {{block(Device)}} in the {{term(MTConnectDevices Response Document)}}."
        },
        "uuid": {
          "type": "string",
          "description": "uuid of the {{block(Device)}}. The value reported for {{property(uuid,DeviceStream)}} **MUST** be the same as the value defined for the {{property(uuid,Device)}} attribute of the same {{block(Device)}} in the {{term(MTConnectDevices Response Document)}}."
        },
        "ComponentStream": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ComponentStream"
          }
        }
      },
      "required": [
        "name",
        "uuid"
      ],
      "additionalProperties": false
    },
    "ComponentStream": {
      "type": "object",
      "title": "ComponentStream",
      "description": "organizes the data associated with each {{block(Component)}} entity defined for a {{block(Device)}} in the associated {{term(MTConnectDevices Response Document)}}.",
      "properties": {
        "component": {
          "type": "string",
          "description": "identifies the {{block(Component)}} type associated with the {{block(ComponentStream)}}. Examples of {{property(component)}} are {{block(Device)}}, {{block(Controller)}}, {{block(Linear)}} and {{block(Loader)}}."
        },
        "componentId": {
          "type": "string",
          "description": "identifier of the {{block(Component)}} as defined by the {{property(id)}} attribute in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(Component)}} associated with the {{block(ComponentStream)}}."
        },
        "nativeName": {
          "type": "string",
          "description": "common name of the {{block(Component)}} associated with the {{block(ComponentStream)}}."
        },
        "uuid": {
          "type": "string",
          "description": "uuid of the {{block(Component)}} associated with the {{block(ComponentStream)}}."
        },
        "Events": {
          "type": "object",
          "properties": {
            "Event": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Event"
              }
            },
            "ActiveAxes": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ActiveAxes"
              }
            },
            "ActuatorState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ActuatorState"
              }
            },
            "Alarm": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Alarm"
              }
            },
            "AssetChanged": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AssetChanged"
              }
            },
            "AssetRemoved": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AssetRemoved"
              }
            },
            "Availability": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Availability"
              }
            },
            "AxisCoupling": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AxisCoupling"
              }
            },
            "AxisFeedrateOverride": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AxisFeedrateOverride"
              }
            },
            "AxisInterlock": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AxisInterlock"
              }
            },
            "AxisState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AxisState"
              }
            },
            "Block": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Block"
              }
            },
            "BlockCount": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/BlockCount"
              }
            },
            "ChuckInterlock": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ChuckInterlock"
              }
            },
            "ChuckState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ChuckState"
              }
            },
            "Code": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Code"
              }
            },
            "CompositionState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CompositionState"
              }
            },
            "ControllerMode": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ControllerMode"
              }
            },
            "ControllerModeOverride": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ControllerModeOverride"
              }
            },
            "CoupledAxes": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CoupledAxes"
              }
            },
            "DateCode": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DateCode"
              }
            },
            "DeviceUuid": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DeviceUuid"
              }
            },
            "Direction": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Direction"
              }
            },
            "DoorState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DoorState"
              }
            },
            "EmergencyStop": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/EmergencyStop"
              }
            },
            "EndOfBar": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/EndOfBar"
              }
            },
            "EquipmentMode": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/EquipmentMode"
              }
            },
            "Execution": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Execution"
              }
            },
            "FunctionalMode": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/FunctionalMode"
              }
            },
            "Hardness": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Hardness"
              }
            },
            "Line": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Line"
              }
            },
            "LineLabel": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/LineLabel"
              }
            },
            "LineNumber": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/LineNumber"
              }
            },
            "Material": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Material"
              }
            },
            "MaterialLayer": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/MaterialLayer"
              }
            },
            "Message": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Message"
              }
            },
            "OperatorId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/OperatorId"
              }
            },
            "PalletId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PalletId"
              }
            },
            "PartCount": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartCount"
              }
            },
            "PartDetect": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartDetect"
              }
            },
            "PartId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartId"
              }
            },
            "PartNumber": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartNumber"
              }
            },
            "PathFeedrateOverride": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PathFeedrateOverride"
              }
            },
            "PathMode": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PathMode"
              }
            },
            "PowerState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PowerState"
              }
            },
            "PowerStatus": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PowerStatus"
              }
            },
            "ProcessTime": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProcessTime"
              }
            },
            "Program": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Program"
              }
            },
            "ProgramComment": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProgramComment"
              }
            },
            "ProgramEdit": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProgramEdit"
              }
            },
            "ProgramEditName": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProgramEditName"
              }
            },
            "ProgramHeader": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProgramHeader"
              }
            },
            "ProgramLocation": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProgramLocation"
              }
            },
            "ProgramLocationType": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProgramLocationType"
              }
            },
            "ProgramNestLevel": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProgramNestLevel"
              }
            },
            "RotaryMode": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/RotaryMode"
              }
            },
            "RotaryVelocityOverride": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/RotaryVelocityOverride"
              }
            },
            "SerialNumber": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SerialNumber"
              }
            },
            "SpindleInterlock": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SpindleInterlock"
              }
            },
            "ToolAssetId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ToolAssetId"
              }
            },
            "ToolGroup": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ToolGroup"
              }
            },
            "ToolId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ToolId"
              }
            },
            "ToolNumber": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ToolNumber"
              }
            },
            "ToolOffset": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ToolOffset"
              }
            },
            "User": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/User"
              }
            },
            "Variable": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Variable"
              }
            },
            "WaitState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/WaitState"
              }
            },
            "Wire": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Wire"
              }
            },
            "WorkholdingId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/WorkholdingId"
              }
            },
            "WorkOffset": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/WorkOffset"
              }
            },
            "OperatingSystem": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/OperatingSystem"
              }
            },
            "Firmware": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Firmware"
              }
            },
            "Application": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Application"
              }
            },
            "Library": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Library"
              }
            },
            "Hardware": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Hardware"
              }
            },
            "Network": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Network"
              }
            },
            "Rotation": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Rotation"
              }
            },
            "Translation": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Translation"
              }
            },
            "ProcessKindId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProcessKindId"
              }
            },
            "PartStatus": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartStatus"
              }
            },
            "AlarmLimit": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AlarmLimit"
              }
            },
            "ProcessAggregateId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProcessAggregateId"
              }
            },
            "PartKindId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartKindId"
              }
            },
            "AdapterURI": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AdapterURI"
              }
            },
            "DeviceRemoved": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DeviceRemoved"
              }
            },
            "DeviceChanged": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DeviceChanged"
              }
            },
            "SpecificationLimit": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SpecificationLimit"
              }
            },
            "ConnectionStatus": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ConnectionStatus"
              }
            },
            "AdapterSoftwareVersion": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AdapterSoftwareVersion"
              }
            },
            "SensorAttachment": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SensorAttachment"
              }
            },
            "ControlLimit": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ControlLimit"
              }
            },
            "DeviceAdded": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DeviceAdded"
              }
            },
            "MTConnectVersion": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/MTConnectVersion"
              }
            },
            "ProcessOccurrenceId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProcessOccurrenceId"
              }
            },
            "PartGroupId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartGroupId"
              }
            },
            "PartUniqueId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartUniqueId"
              }
            },
            "ActivationCount": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ActivationCount"
              }
            },
            "DeactivationCount": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DeactivationCount"
              }
            },
            "TransferCount": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/TransferCount"
              }
            },
            "LoadCount": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/LoadCount"
              }
            },
            "PartProcessingState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartProcessingState"
              }
            },
            "ProcessState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProcessState"
              }
            },
            "ValveState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ValveState"
              }
            },
            "LockState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/LockState"
              }
            },
            "UnloadCount": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/UnloadCount"
              }
            },
            "CycleCount": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CycleCount"
              }
            },
            "OperatingMode": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/OperatingMode"
              }
            },
            "AssetCount": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AssetCount"
              }
            },
            "MaintenanceList": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/MaintenanceList"
              }
            },
            "FixtureId": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/FixtureId"
              }
            },
            "PartCountType": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartCountType"
              }
            },
            "VariableDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VariableDataSet"
              }
            },
            "WorkOffsetTable": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/WorkOffsetTable"
              }
            },
            "MaterialFeed": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/MaterialFeed"
              }
            },
            "MaterialChange": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/MaterialChange"
              }
            },
            "MaterialRetract": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/MaterialRetract"
              }
            },
            "MaterialLoad": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/MaterialLoad"
              }
            },
            "MaterialUnload": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/MaterialUnload"
              }
            },
            "OpenChuck": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/OpenChuck"
              }
            },
            "OpenDoor": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/OpenDoor"
              }
            },
            "PartChange": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PartChange"
              }
            },
            "CloseDoor": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CloseDoor"
              }
            },
            "CloseChuck": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CloseChuck"
              }
            },
            "InterfaceState": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/InterfaceState"
              }
            },
            "AssetCountDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AssetCountDataSet"
              }
            }
          },
          "patternProperties": {
            "^[a-ln-zA-LN-Z]:[A-Za-z_][A-Za-z0-9_]*$": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Event"
              }
            }
          },
          "additionalProperties": false
        },
        "Samples": {
          "type": "object",
          "properties": {
            "Sample": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Sample"
              }
            },
            "SampleTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SampleTimeSeries"
              }
            },
            "Acceleration": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Acceleration"
              }
            },
            "AccelerationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AccelerationTimeSeries"
              }
            },
            "AccumulatedTime": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AccumulatedTime"
              }
            },
            "AccumulatedTimeTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AccumulatedTimeTimeSeries"
              }
            },
            "Amperage": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Amperage"
              }
            },
            "AmperageTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AmperageTimeSeries"
              }
            },
            "Angle": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Angle"
              }
            },
            "AngleTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AngleTimeSeries"
              }
            },
            "AngularAcceleration": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AngularAcceleration"
              }
            },
            "AngularAccelerationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AngularAccelerationTimeSeries"
              }
            },
            "AngularVelocity": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AngularVelocity"
              }
            },
            "AngularVelocityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AngularVelocityTimeSeries"
              }
            },
            "AxisFeedrate": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AxisFeedrate"
              }
            },
            "AxisFeedrateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AxisFeedrateTimeSeries"
              }
            },
            "CapacityFluid": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CapacityFluid"
              }
            },
            "CapacityFluidTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CapacityFluidTimeSeries"
              }
            },
            "CapacitySpatial": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CapacitySpatial"
              }
            },
            "CapacitySpatialTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CapacitySpatialTimeSeries"
              }
            },
            "Concentration": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Concentration"
              }
            },
            "ConcentrationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ConcentrationTimeSeries"
              }
            },
            "Conductivity": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Conductivity"
              }
            },
            "ConductivityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ConductivityTimeSeries"
              }
            },
            "CuttingSpeed": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CuttingSpeed"
              }
            },
            "CuttingSpeedTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/CuttingSpeedTimeSeries"
              }
            },
            "Density": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Density"
              }
            },
            "DensityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DensityTimeSeries"
              }
            },
            "DepositionAccelerationVolumetric": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionAccelerationVolumetric"
              }
            },
            "DepositionAccelerationVolumetricTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionAccelerationVolumetricTimeSeries"
              }
            },
            "DepositionDensity": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionDensity"
              }
            },
            "DepositionDensityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionDensityTimeSeries"
              }
            },
            "DepositionMass": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionMass"
              }
            },
            "DepositionMassTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionMassTimeSeries"
              }
            },
            "DepositionRateVolumetric": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionRateVolumetric"
              }
            },
            "DepositionRateVolumetricTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionRateVolumetricTimeSeries"
              }
            },
            "DepositionVolume": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionVolume"
              }
            },
            "DepositionVolumeTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DepositionVolumeTimeSeries"
              }
            },
            "Displacement": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Displacement"
              }
            },
            "DisplacementTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DisplacementTimeSeries"
              }
            },
            "ElectricalEnergy": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ElectricalEnergy"
              }
            },
            "ElectricalEnergyTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ElectricalEnergyTimeSeries"
              }
            },
            "EquipmentTimer": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/EquipmentTimer"
              }
            },
            "EquipmentTimerTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/EquipmentTimerTimeSeries"
              }
            },
            "FillLevel": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/FillLevel"
              }
            },
            "FillLevelTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/FillLevelTimeSeries"
              }
            },
            "Flow": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Flow"
              }
            },
            "FlowTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/FlowTimeSeries"
              }
            },
            "Frequency": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Frequency"
              }
            },
            "FrequencyTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/FrequencyTimeSeries"
              }
            },
            "GlobalPosition": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/GlobalPosition"
              }
            },
            "GlobalPositionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/GlobalPositionTimeSeries"
              }
            },
            "Length": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Length"
              }
            },
            "LengthTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/LengthTimeSeries"
              }
            },
            "Level": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Level"
              }
            },
            "LevelTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/LevelTimeSeries"
              }
            },
            "LinearForce": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/LinearForce"
              }
            },
            "LinearForceTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/LinearForceTimeSeries"
              }
            },
            "Load": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Load"
              }
            },
            "LoadTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/LoadTimeSeries"
              }
            },
            "Mass": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Mass"
              }
            },
            "MassTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/MassTimeSeries"
              }
            },
            "PathFeedrate": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PathFeedrate"
              }
            },
            "PathFeedrateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PathFeedrateTimeSeries"
              }
            },
            "PathFeedratePerRevolution": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PathFeedratePerRevolution"
              }
            },
            "PathFeedratePerRevolutionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PathFeedratePerRevolutionTimeSeries"
              }
            },
            "PathPosition": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PathPosition"
              }
            },
            "PH": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PH"
              }
            },
            "PHTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PHTimeSeries"
              }
            },
            "Position": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Position"
              }
            },
            "PositionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PositionTimeSeries"
              }
            },
            "PowerFactor": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PowerFactor"
              }
            },
            "PowerFactorTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PowerFactorTimeSeries"
              }
            },
            "Pressure": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Pressure"
              }
            },
            "ProcessTimer": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProcessTimer"
              }
            },
            "ProcessTimerTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ProcessTimerTimeSeries"
              }
            },
            "Resistance": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Resistance"
              }
            },
            "ResistanceTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ResistanceTimeSeries"
              }
            },
            "RotaryVelocity": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/RotaryVelocity"
              }
            },
            "RotaryVelocityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/RotaryVelocityTimeSeries"
              }
            },
            "SoundLevel": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SoundLevel"
              }
            },
            "SoundLevelTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SoundLevelTimeSeries"
              }
            },
            "SpindleSpeed": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SpindleSpeed"
              }
            },
            "SpindleSpeedTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SpindleSpeedTimeSeries"
              }
            },
            "Strain": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Strain"
              }
            },
            "StrainTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/StrainTimeSeries"
              }
            },
            "Temperature": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Temperature"
              }
            },
            "TemperatureTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/TemperatureTimeSeries"
              }
            },
            "Tension": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Tension"
              }
            },
            "TensionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/TensionTimeSeries"
              }
            },
            "Tilt": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Tilt"
              }
            },
            "TiltTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/TiltTimeSeries"
              }
            },
            "Torque": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Torque"
              }
            },
            "TorqueTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/TorqueTimeSeries"
              }
            },
            "Velocity": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Velocity"
              }
            },
            "VelocityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VelocityTimeSeries"
              }
            },
            "Viscosity": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Viscosity"
              }
            },
            "ViscosityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ViscosityTimeSeries"
              }
            },
            "Voltage": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Voltage"
              }
            },
            "VoltageTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VoltageTimeSeries"
              }
            },
            "VoltAmpere": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VoltAmpere"
              }
            },
            "VoltAmpereTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VoltAmpereTimeSeries"
              }
            },
            "VoltAmpereReactive": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VoltAmpereReactive"
              }
            },
            "VoltAmpereReactiveTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VoltAmpereReactiveTimeSeries"
              }
            },
            "VolumeFluid": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VolumeFluid"
              }
            },
            "VolumeFluidTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VolumeFluidTimeSeries"
              }
            },
            "VolumeSpatial": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VolumeSpatial"
              }
            },
            "VolumeSpatialTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VolumeSpatialTimeSeries"
              }
            },
            "Wattage": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Wattage"
              }
            },
            "WattageTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/WattageTimeSeries"
              }
            },
            "AmperageDC": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AmperageDC"
              }
            },
            "AmperageDCTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AmperageDCTimeSeries"
              }
            },
            "AmperageAC": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AmperageAC"
              }
            },
            "AmperageACTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AmperageACTimeSeries"
              }
            },
            "VoltageAC": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VoltageAC"
              }
            },
            "VoltageACTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VoltageACTimeSeries"
              }
            },
            "VoltageDC": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VoltageDC"
              }
            },
            "VoltageDCTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/VoltageDCTimeSeries"
              }
            },
            "XDimension": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/XDimension"
              }
            },
            "XDimensionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/XDimensionTimeSeries"
              }
            },
            "YDimension": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/YDimension"
              }
            },
            "YDimensionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/YDimensionTimeSeries"
              }
            },
            "ZDimension": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ZDimension"
              }
            },
            "ZDimensionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ZDimensionTimeSeries"
              }
            },
            "Diameter": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Diameter"
              }
            },
            "DiameterTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DiameterTimeSeries"
              }
            },
            "Orientation": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Orientation"
              }
            },
            "HumidityRelative": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HumidityRelative"
              }
            },
            "HumidityRelativeTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HumidityRelativeTimeSeries"
              }
            },
            "HumidityAbsolute": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HumidityAbsolute"
              }
            },
            "HumidityAbsoluteTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HumidityAbsoluteTimeSeries"
              }
            },
            "HumiditySpecific": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HumiditySpecific"
              }
            },
            "HumiditySpecificTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/HumiditySpecificTimeSeries"
              }
            },
            "PressurizationRate": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PressurizationRate"
              }
            },
            "PressurizationRateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PressurizationRateTimeSeries"
              }
            },
            "Deceleration": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Deceleration"
              }
            },
            "DecelerationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DecelerationTimeSeries"
              }
            },
            "AssetUpdateRate": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AssetUpdateRate"
              }
            },
            "AssetUpdateRateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AssetUpdateRateTimeSeries"
              }
            },
            "AngularDeceleration": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AngularDeceleration"
              }
            },
            "AngularDecelerationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/AngularDecelerationTimeSeries"
              }
            },
            "ObservationUpdateRate": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ObservationUpdateRate"
              }
            },
            "ObservationUpdateRateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ObservationUpdateRateTimeSeries"
              }
            },
            "PressureAbsolute": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PressureAbsolute"
              }
            },
            "PressureAbsoluteTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PressureAbsoluteTimeSeries"
              }
            },
            "Openness": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Openness"
              }
            },
            "OpennessTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/OpennessTimeSeries"
              }
            },
            "ClockTime": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ClockTime"
              }
            },
            "ClockTimeTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/ClockTimeTimeSeries"
              }
            }
          },
          "patternProperties": {
            "^[a-ln-zA-LN-Z]:[A-Za-z_][A-Za-z0-9_]*$": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Sample"
              }
            }
          },
          "additionalProperties": false
        },
        "Condition": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/definitions/Condition"
            }
          }
        }
      },
      "required": [
        "component",
        "componentId"
      ],
      "additionalProperties": false
    },
    "Observation": {
      "type": "object",
      "title": "Observation",
      "description": "abstract entity that provides telemetry data for a {{block(DataItem)}} at a point in time.",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/DataItemTypeEnum",
          "description": "type of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(type)}} **MUST** match the {{property(type)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "Sample": {
      "type": "object",
      "title": "Sample",
      "description": "{{block(Observation)}} that is continuously changing or analog data value.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "Event": {
      "type": "object",
      "title": "Event",
      "description": "{{block(Observation)}} that is a discrete piece of information from a piece of equipment.",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "Condition": {
      "type": "object",
      "title": "Condition",
      "description": "{{block(Observation)}} that provides the {{term(condition)}} of a piece of equipment or a {{term(Component)}}.",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "type": "string"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code (usually an alpha-numeric value) generated by the controller of a piece of equipment providing a reference identifier for a {{block(Condition)}}. This is the same information an operator or maintenance personnel may see as a reference code designating a specific fault code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/definitions/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "{{property(statistic)}} provides additional information describing the meaning of the {{block(Condition)}} element. {{property(statistic)}} **MUST** match the {{property(statistic)}} attribute of the {{block(DataItem)}} entity defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/definitions/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ]
    },
    "Representation": {
      "type": "object",
      "title": "Representation",
      "description": "specifies the format and structure of the {{property(result)}} for an {{block(Observation)}}."
    },
    "Value": {
      "type": "object",
      "title": "Value",
      "description": "default {{block(Representation)}} type for all {{block(Observation)}} types where {{property(result)}} of the {{block(Observation)}} types is an MTConnect data type. See {{package(DataTypes)}}.",
      "additionalProperties": false
    },
    "TimeSeries": {
      "type": "object",
      "title": "TimeSeries",
      "description": "{{block(Representation)}} for an {{block(Observation)}} composed of a series of sampled data.",
      "properties": {
        "sampleCount": {
          "type": "integer",
          "description": "number of values given for the {{block(Observation)}}."
        },
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        }
      },
      "required": [
        "sampleCount",
        "value"
      ],
      "additionalProperties": false
    },
    "Discrete": {
      "type": "object",
      "title": "Discrete",
      "description": "**DEPRECATED** {{block(Representation)}} for an {{block(Observation)}} where each discrete occurrence of the data may have the same value as the previous occurrence of the data.",
      "properties": {
        "value": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "DataSet": {
      "type": "object",
      "title": "DataSet",
      "description": "{{block(Representation)}} for an {{block(Observation)}} composed of value(s) represented as a set of {{termplural(key-value pair)}}.",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "Entry": {
      "type": "object",
      "title": "Entry",
      "description": "{{term(key-value pair)}} published as part of a {{block(DataSet)}}.",
      "properties": {
        "key": {
          "type": "string",
          "description": "unique identifier for each {{term(key-value pair)}}."
        },
        "removed": {
          "type": "boolean",
          "description": "removal indicator of a {{term(key-value pair)}}."
        },
        "value": {
          "type": "string",
          "description": "value of the {{block(Entry)}}."
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "Table": {
      "type": "object",
      "title": "Table",
      "description": "{{block(Representation)}} for an {{block(Observation)}} composed of two-dimensional sets of {{termplural(key-value pair)}} where the {{block(Entry)}} represents rows containing sets of {{termplural(key-value pair)}} given by {{block(Cell)}} entities.",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{termplural(key-value pair)}} represented as {{block(Entry)}} entities."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "TableEntry": {
      "type": "object",
      "title": "TableEntry",
      "description": "{{term(key-value pair)}} published as part of a {{block(Table)}}. > Note: In the {{term(XML)}} representation, {{block(TableEntry)}} **MUST** appear as {{block(Entry)}}.",
      "properties": {
        "key": {
          "type": "string",
          "description": "unique identifier for each {{term(key-value pair)}}."
        },
        "removed": {
          "type": "boolean",
          "description": "removal indicator of a {{term(key-value pair)}}."
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "Cell": {
      "type": "object",
      "title": "Cell",
      "description": "{{term(key-value pair)}} published as part of a {{block(TableEntry)}}.",
      "properties": {
        "key": {
          "type": "string",
          "description": "unique identifier for each {{term(key-value pair)}}."
        },
        "value": {
          "type": "string",
          "description": "value of the {{block(Cell)}}."
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "Acceleration": {
      "type": "object",
      "title": "Acceleration",
      "description": "{{def(SampleEnum:ACCELERATION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ACTUAL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ACCELERATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AccumulatedTime": {
      "type": "object",
      "title": "AccumulatedTime",
      "description": "{{def(SampleEnum:ACCUMULATED_TIME)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ACCUMULATED_TIME"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Amperage": {
      "type": "object",
      "title": "Amperage",
      "description": "{{def(SampleEnum:AMPERAGE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "AMPERAGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "Angle": {
      "type": "object",
      "title": "Angle",
      "description": "{{def(SampleEnum:ANGLE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ANGLE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AngularAcceleration": {
      "type": "object",
      "title": "AngularAcceleration",
      "description": "{{def(SampleEnum:ANGULAR_ACCELERATION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ACTUAL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ANGULAR_ACCELERATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AngularVelocity": {
      "type": "object",
      "title": "AngularVelocity",
      "description": "{{def(SampleEnum:ANGULAR_VELOCITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ANGULAR_VELOCITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AxisFeedrate": {
      "type": "object",
      "title": "AxisFeedrate",
      "description": "{{def(SampleEnum:AXIS_FEEDRATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "AXIS_FEEDRATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "CapacityFluid": {
      "type": "object",
      "title": "CapacityFluid",
      "description": "{{def(SampleEnum:CAPACITY_FLUID)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CAPACITY_FLUID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLILITER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "CapacitySpatial": {
      "type": "object",
      "title": "CapacitySpatial",
      "description": "{{def(SampleEnum:CAPACITY_SPATIAL)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CAPACITY_SPATIAL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Concentration": {
      "type": "object",
      "title": "Concentration",
      "description": "{{def(SampleEnum:CONCENTRATION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CONCENTRATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Conductivity": {
      "type": "object",
      "title": "Conductivity",
      "description": "{{def(SampleEnum:CONDUCTIVITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CONDUCTIVITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "SIEMENS/METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "CuttingSpeed": {
      "type": "object",
      "title": "CuttingSpeed",
      "description": "{{def(SampleEnum:CUTTING_SPEED)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CUTTING_SPEED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "Density": {
      "type": "object",
      "title": "Density",
      "description": "{{def(SampleEnum:DENSITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DENSITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIGRAM/CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DepositionAccelerationVolumetric": {
      "type": "object",
      "title": "DepositionAccelerationVolumetric",
      "description": "{{def(SampleEnum:DEPOSITION_ACCELERATION_VOLUMETRIC)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_ACCELERATION_VOLUMETRIC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DepositionDensity": {
      "type": "object",
      "title": "DepositionDensity",
      "description": "{{def(SampleEnum:DEPOSITION_DENSITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_DENSITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIGRAM/CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DepositionMass": {
      "type": "object",
      "title": "DepositionMass",
      "description": "{{def(SampleEnum:DEPOSITION_MASS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_MASS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIGRAM"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DepositionRateVolumetric": {
      "type": "object",
      "title": "DepositionRateVolumetric",
      "description": "{{def(SampleEnum:DEPOSITION_RATE_VOLUMETRIC)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_RATE_VOLUMETRIC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DepositionVolume": {
      "type": "object",
      "title": "DepositionVolume",
      "description": "{{def(SampleEnum:DEPOSITION_VOLUME)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_VOLUME"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "Displacement": {
      "type": "object",
      "title": "Displacement",
      "description": "{{def(SampleEnum:DISPLACEMENT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DISPLACEMENT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ElectricalEnergy": {
      "type": "object",
      "title": "ElectricalEnergy",
      "description": "{{def(SampleEnum:ELECTRICAL_ENERGY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ELECTRICAL_ENERGY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "WATT_SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "EquipmentTimer": {
      "type": "object",
      "title": "EquipmentTimer",
      "description": "{{def(SampleEnum:EQUIPMENT_TIMER)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "EQUIPMENT_TIMER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "FillLevel": {
      "type": "object",
      "title": "FillLevel",
      "description": "{{def(SampleEnum:FILL_LEVEL)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "FILL_LEVEL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Flow": {
      "type": "object",
      "title": "Flow",
      "description": "{{def(SampleEnum:FLOW)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "FLOW"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "LITER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Frequency": {
      "type": "object",
      "title": "Frequency",
      "description": "{{def(SampleEnum:FREQUENCY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "FREQUENCY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "HERTZ"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "GlobalPosition": {
      "type": "object",
      "title": "GlobalPosition",
      "description": "{{def(SampleEnum:GLOBAL_POSITION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "GLOBAL_POSITION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "Length": {
      "type": "object",
      "title": "Length",
      "description": "{{def(SampleEnum:LENGTH)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "LENGTH"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "Level": {
      "type": "object",
      "title": "Level",
      "description": "{{def(SampleEnum:LEVEL)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "LEVEL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "LinearForce": {
      "type": "object",
      "title": "LinearForce",
      "description": "{{def(SampleEnum:LINEAR_FORCE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "LINEAR_FORCE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "NEWTON"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Load": {
      "type": "object",
      "title": "Load",
      "description": "{{def(SampleEnum:LOAD)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "LOAD"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Mass": {
      "type": "object",
      "title": "Mass",
      "description": "{{def(SampleEnum:MASS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "MASS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "KILOGRAM"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PathFeedrate": {
      "type": "object",
      "title": "PathFeedrate",
      "description": "{{def(SampleEnum:PATH_FEEDRATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PATH_FEEDRATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "PathFeedratePerRevolution": {
      "type": "object",
      "title": "PathFeedratePerRevolution",
      "description": "{{def(SampleEnum:PATH_FEEDRATE_PER_REVOLUTION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PATH_FEEDRATE_PER_REVOLUTION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/REVOLUTION"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "PathPosition": {
      "type": "object",
      "title": "PathPosition",
      "description": "{{def(SampleEnum:PATH_POSITION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PATH_POSITION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER_3D"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "PH": {
      "type": "object",
      "title": "PH",
      "description": "{{def(SampleEnum:PH)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PH"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PH"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Position": {
      "type": "object",
      "title": "Position",
      "description": "{{def(SampleEnum:POSITION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "POSITION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "PowerFactor": {
      "type": "object",
      "title": "PowerFactor",
      "description": "{{def(SampleEnum:POWER_FACTOR)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "POWER_FACTOR"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Pressure": {
      "type": "object",
      "title": "Pressure",
      "description": "{{def(SampleEnum:PRESSURE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PRESSURE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PASCAL"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ProcessTimer": {
      "type": "object",
      "title": "ProcessTimer",
      "description": "{{def(SampleEnum:PROCESS_TIMER)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PROCESS_TIMER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "Resistance": {
      "type": "object",
      "title": "Resistance",
      "description": "{{def(SampleEnum:RESISTANCE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "RESISTANCE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "OHM"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "RotaryVelocity": {
      "type": "object",
      "title": "RotaryVelocity",
      "description": "{{def(SampleEnum:ROTARY_VELOCITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ROTARY_VELOCITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "REVOLUTION/MINUTE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "SoundLevel": {
      "type": "object",
      "title": "SoundLevel",
      "description": "{{def(SampleEnum:SOUND_LEVEL)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "A_SCALE"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "SOUND_LEVEL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DECIBEL"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "SpindleSpeed": {
      "type": "object",
      "title": "SpindleSpeed",
      "description": "{{def(SampleEnum:SPINDLE_SPEED)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "SPINDLE_SPEED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "REVOLUTION/MINUTE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "Strain": {
      "type": "object",
      "title": "Strain",
      "description": "{{def(SampleEnum:STRAIN)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "STRAIN"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Temperature": {
      "type": "object",
      "title": "Temperature",
      "description": "{{def(SampleEnum:TEMPERATURE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "TEMPERATURE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CELSIUS"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Tension": {
      "type": "object",
      "title": "Tension",
      "description": "{{def(SampleEnum:TENSION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "TENSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "NEWTON"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Tilt": {
      "type": "object",
      "title": "Tilt",
      "description": "{{def(SampleEnum:TILT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "TILT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MICRO_RADIAN"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Torque": {
      "type": "object",
      "title": "Torque",
      "description": "{{def(SampleEnum:TORQUE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "TORQUE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "NEWTON_METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Velocity": {
      "type": "object",
      "title": "Velocity",
      "description": "{{def(SampleEnum:VELOCITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VELOCITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Viscosity": {
      "type": "object",
      "title": "Viscosity",
      "description": "{{def(SampleEnum:VISCOSITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VISCOSITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PASCAL_SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Voltage": {
      "type": "object",
      "title": "Voltage",
      "description": "{{def(SampleEnum:VOLTAGE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLTAGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "VoltAmpere": {
      "type": "object",
      "title": "VoltAmpere",
      "description": "{{def(SampleEnum:VOLT_AMPERE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLT_AMPERE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT_AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VoltAmpereReactive": {
      "type": "object",
      "title": "VoltAmpereReactive",
      "description": "{{def(SampleEnum:VOLT_AMPERE_REACTIVE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLT_AMPERE_REACTIVE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT_AMPERE_REACTIVE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VolumeFluid": {
      "type": "object",
      "title": "VolumeFluid",
      "description": "{{def(SampleEnum:VOLUME_FLUID)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLUME_FLUID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLILITER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "VolumeSpatial": {
      "type": "object",
      "title": "VolumeSpatial",
      "description": "{{def(SampleEnum:VOLUME_SPATIAL)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLUME_SPATIAL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "Wattage": {
      "type": "object",
      "title": "Wattage",
      "description": "{{def(SampleEnum:WATTAGE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "WATTAGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "WATT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AmperageDC": {
      "type": "object",
      "title": "AmperageDC",
      "description": "{{def(SampleEnum:AMPERAGE_DC)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "AMPERAGE_DC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "AmperageAC": {
      "type": "object",
      "title": "AmperageAC",
      "description": "{{def(SampleEnum:AMPERAGE_AC)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "AMPERAGE_AC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "VoltageAC": {
      "type": "object",
      "title": "VoltageAC",
      "description": "{{def(SampleEnum:VOLTAGE_AC)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLTAGE_AC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "VoltageDC": {
      "type": "object",
      "title": "VoltageDC",
      "description": "{{def(SampleEnum:VOLTAGE_DC)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLTAGE_DC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "XDimension": {
      "type": "object",
      "title": "XDimension",
      "description": "{{def(SampleEnum:X_DIMENSION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "X_DIMENSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "YDimension": {
      "type": "object",
      "title": "YDimension",
      "description": "{{def(SampleEnum:Y_DIMENSION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "Y_DIMENSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ZDimension": {
      "type": "object",
      "title": "ZDimension",
      "description": "{{def(SampleEnum:Z_DIMENSION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "Z_DIMENSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Diameter": {
      "type": "object",
      "title": "Diameter",
      "description": "{{def(SampleEnum:DIAMETER)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DIAMETER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Orientation": {
      "type": "object",
      "title": "Orientation",
      "description": "{{def(SampleEnum:ORIENTATION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ORIENTATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE_3D"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "HumidityRelative": {
      "type": "object",
      "title": "HumidityRelative",
      "description": "{{def(SampleEnum:HUMIDITY_RELATIVE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "HUMIDITY_RELATIVE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "HumidityAbsolute": {
      "type": "object",
      "title": "HumidityAbsolute",
      "description": "{{def(SampleEnum:HUMIDITY_ABSOLUTE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "HUMIDITY_ABSOLUTE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "GRAM/CUBIC_METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "HumiditySpecific": {
      "type": "object",
      "title": "HumiditySpecific",
      "description": "{{def(SampleEnum:HUMIDITY_SPECIFIC)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "HUMIDITY_SPECIFIC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "PressurizationRate": {
      "type": "object",
      "title": "PressurizationRate",
      "description": "{{def(SampleEnum:PRESSURIZATION_RATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PRESSURIZATION_RATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PASCAL/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "Deceleration": {
      "type": "object",
      "title": "Deceleration",
      "description": "{{def(SampleEnum:DECELERATION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ACTUAL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DECELERATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AssetUpdateRate": {
      "type": "object",
      "title": "AssetUpdateRate",
      "description": "{{def(SampleEnum:ASSET_UPDATE_RATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ASSET_UPDATE_RATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "COUNT/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AngularDeceleration": {
      "type": "object",
      "title": "AngularDeceleration",
      "description": "{{def(SampleEnum:ANGULAR_DECELERATION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ACTUAL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ANGULAR_DECELERATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "ObservationUpdateRate": {
      "type": "object",
      "title": "ObservationUpdateRate",
      "description": "{{def(SampleEnum:OBSERVATION_UPDATE_RATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "OBSERVATION_UPDATE_RATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "COUNT/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PressureAbsolute": {
      "type": "object",
      "title": "PressureAbsolute",
      "description": "The force per unit area measured relative to a vacuum.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PRESSURE_ABSOLUTE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PASCAL"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Openness": {
      "type": "object",
      "title": "Openness",
      "description": "{{def(SampleEnum:OPENNESS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "OPENNESS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ActiveAxes": {
      "type": "object",
      "title": "ActiveAxes",
      "description": "{{def(EventEnum:ACTIVE_AXES)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ACTIVE_AXES"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ActuatorState": {
      "type": "object",
      "title": "ActuatorState",
      "description": "{{def(EventEnum:ACTUATOR_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ActuatorStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ACTUATOR_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Alarm": {
      "type": "object",
      "title": "Alarm",
      "description": "{{def(EventEnum:ALARM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ALARM"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "code": {
          "$ref": "#/definitions/AlarmCodeEnum",
          "description": "type of alarm."
        },
        "severity": {
          "$ref": "#/definitions/AlarmSeverityEnum",
          "description": "severity of the alarm."
        },
        "nativeCode": {
          "type": "string",
          "description": "native code for the piece of equipment."
        },
        "state": {
          "$ref": "#/definitions/AlarmStateEnum",
          "description": "state of the alarm."
        },
        "lang": {
          "type": "string",
          "description": "specifies the language of the alarm text. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        }
      },
      "required": [
        "code",
        "dataItemId",
        "nativeCode",
        "sequence",
        "severity",
        "state",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AssetChanged": {
      "type": "object",
      "title": "AssetChanged",
      "description": "{{def(EventEnum:ASSET_CHANGED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string",
          "description": "type of {{block(Asset)}} changed. See {{package(Asset Information Model)}} for details on the {{block(Asset)}} model."
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ASSET_CHANGED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "assetType",
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AssetRemoved": {
      "type": "object",
      "title": "AssetRemoved",
      "description": "{{def(EventEnum:ASSET_REMOVED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string",
          "description": "type of {{block(Asset)}} removed. See {{package(Asset Information Model)}} for details on the {{block(Asset)}} model."
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ASSET_REMOVED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "assetType",
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Availability": {
      "type": "object",
      "title": "Availability",
      "description": "{{def(EventEnum:AVAILABILITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/AvailabilityEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "AVAILABILITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AxisCoupling": {
      "type": "object",
      "title": "AxisCoupling",
      "description": "{{def(EventEnum:AXIS_COUPLING)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/AxisCouplingEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "AXIS_COUPLING"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AxisFeedrateOverride": {
      "type": "object",
      "title": "AxisFeedrateOverride",
      "description": "{{def(EventEnum:AXIS_FEEDRATE_OVERRIDE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "AXIS_FEEDRATE_OVERRIDE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "AxisInterlock": {
      "type": "object",
      "title": "AxisInterlock",
      "description": "{{def(EventEnum:AXIS_INTERLOCK)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/AxisInterlockEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "AXIS_INTERLOCK"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AxisState": {
      "type": "object",
      "title": "AxisState",
      "description": "{{def(EventEnum:AXIS_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/AxisStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "AXIS_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Block": {
      "type": "object",
      "title": "Block",
      "description": "{{def(EventEnum:BLOCK)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "BLOCK"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "BlockCount": {
      "type": "object",
      "title": "BlockCount",
      "description": "{{def(EventEnum:BLOCK_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "BLOCK_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ChuckInterlock": {
      "type": "object",
      "title": "ChuckInterlock",
      "description": "{{def(EventEnum:CHUCK_INTERLOCK)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ChuckInterlockEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CHUCK_INTERLOCK"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "ChuckState": {
      "type": "object",
      "title": "ChuckState",
      "description": "{{def(EventEnum:CHUCK_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ChuckStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CHUCK_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Code": {
      "type": "object",
      "title": "Code",
      "description": "{{def(EventEnum:CODE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CODE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "CompositionState": {
      "type": "object",
      "title": "CompositionState",
      "description": "{{def(EventEnum:COMPOSITION_STATE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "COMPOSITION_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "ControllerMode": {
      "type": "object",
      "title": "ControllerMode",
      "description": "{{def(EventEnum:CONTROLLER_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ControllerModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CONTROLLER_MODE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ControllerModeOverride": {
      "type": "object",
      "title": "ControllerModeOverride",
      "description": "{{def(EventEnum:CONTROLLER_MODE_OVERRIDE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ControllerModeOverrideEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CONTROLLER_MODE_OVERRIDE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "CoupledAxes": {
      "type": "object",
      "title": "CoupledAxes",
      "description": "{{def(EventEnum:COUPLED_AXES)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "COUPLED_AXES"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "DateCode": {
      "type": "object",
      "title": "DateCode",
      "description": "{{def(EventEnum:DATE_CODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "string",
              "format": "date-time"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "DATE_CODE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "DeviceUuid": {
      "type": "object",
      "title": "DeviceUuid",
      "description": "{{def(EventEnum:DEVICE_UUID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "DEVICE_UUID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Direction": {
      "type": "object",
      "title": "Direction",
      "description": "{{def(EventEnum:DIRECTION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/DirectionEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "DIRECTION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "DoorState": {
      "type": "object",
      "title": "DoorState",
      "description": "{{def(EventEnum:DOOR_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/DoorStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "DOOR_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "EmergencyStop": {
      "type": "object",
      "title": "EmergencyStop",
      "description": "{{def(EventEnum:EMERGENCY_STOP)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/EmergencyStopEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "EMERGENCY_STOP"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "EndOfBar": {
      "type": "object",
      "title": "EndOfBar",
      "description": "{{def(EventEnum:END_OF_BAR)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/EndOfBarEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "PRIMARY"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "END_OF_BAR"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "EquipmentMode": {
      "type": "object",
      "title": "EquipmentMode",
      "description": "{{def(EventEnum:EQUIPMENT_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/EquipmentModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "EQUIPMENT_MODE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "Execution": {
      "type": "object",
      "title": "Execution",
      "description": "{{def(EventEnum:EXECUTION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ExecutionEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "EXECUTION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "FunctionalMode": {
      "type": "object",
      "title": "FunctionalMode",
      "description": "{{def(EventEnum:FUNCTIONAL_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/FunctionalModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "FUNCTIONAL_MODE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Hardness": {
      "type": "object",
      "title": "Hardness",
      "description": "{{def(EventEnum:HARDNESS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "HARDNESS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "Line": {
      "type": "object",
      "title": "Line",
      "description": "{{def(EventEnum:LINE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "LINE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "LineLabel": {
      "type": "object",
      "title": "LineLabel",
      "description": "{{def(EventEnum:LINE_LABEL)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "LINE_LABEL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "LineNumber": {
      "type": "object",
      "title": "LineNumber",
      "description": "{{def(EventEnum:LINE_NUMBER)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "LINE_NUMBER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "Material": {
      "type": "object",
      "title": "Material",
      "description": "{{def(EventEnum:MATERIAL)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "MATERIAL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "MaterialLayer": {
      "type": "object",
      "title": "MaterialLayer",
      "description": "{{def(EventEnum:MATERIAL_LAYER)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "MATERIAL_LAYER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "Message": {
      "type": "object",
      "title": "Message",
      "description": "{{def(EventEnum:MESSAGE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "MESSAGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "OperatorId": {
      "type": "object",
      "title": "OperatorId",
      "description": "{{def(EventEnum:OPERATOR_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "OPERATOR_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PalletId": {
      "type": "object",
      "title": "PalletId",
      "description": "{{def(EventEnum:PALLET_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PALLET_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PartCount": {
      "type": "object",
      "title": "PartCount",
      "description": "{{def(EventEnum:PART_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ALL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "PartDetect": {
      "type": "object",
      "title": "PartDetect",
      "description": "{{def(EventEnum:PART_DETECT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/PartDetectEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_DETECT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PartId": {
      "type": "object",
      "title": "PartId",
      "description": "{{def(EventEnum:PART_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PartNumber": {
      "type": "object",
      "title": "PartNumber",
      "description": "{{def(EventEnum:PART_NUMBER)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_NUMBER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PathFeedrateOverride": {
      "type": "object",
      "title": "PathFeedrateOverride",
      "description": "{{def(EventEnum:PATH_FEEDRATE_OVERRIDE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PATH_FEEDRATE_OVERRIDE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "PathMode": {
      "type": "object",
      "title": "PathMode",
      "description": "{{def(EventEnum:PATH_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/PathModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ],
          "default": "INDEPENDENT"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PATH_MODE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PowerState": {
      "type": "object",
      "title": "PowerState",
      "description": "{{def(EventEnum:POWER_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/PowerStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "POWER_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "PowerStatus": {
      "type": "object",
      "title": "PowerStatus",
      "description": "{{def(EventEnum:POWER_STATUS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/PowerStatusEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "POWER_STATUS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessTime": {
      "type": "object",
      "title": "ProcessTime",
      "description": "{{def(EventEnum:PROCESS_TIME)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROCESS_TIME"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "Program": {
      "type": "object",
      "title": "Program",
      "description": "{{def(EventEnum:PROGRAM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROGRAM"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "ProgramComment": {
      "type": "object",
      "title": "ProgramComment",
      "description": "{{def(EventEnum:PROGRAM_COMMENT)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROGRAM_COMMENT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProgramEdit": {
      "type": "object",
      "title": "ProgramEdit",
      "description": "{{def(EventEnum:PROGRAM_EDIT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ProgramEditEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROGRAM_EDIT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProgramEditName": {
      "type": "object",
      "title": "ProgramEditName",
      "description": "{{def(EventEnum:PROGRAM_EDIT_NAME)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROGRAM_EDIT_NAME"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProgramHeader": {
      "type": "object",
      "title": "ProgramHeader",
      "description": "{{def(EventEnum:PROGRAM_HEADER)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "MAIN"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROGRAM_HEADER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "ProgramLocation": {
      "type": "object",
      "title": "ProgramLocation",
      "description": "{{def(EventEnum:PROGRAM_LOCATION)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROGRAM_LOCATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "ProgramLocationType": {
      "type": "object",
      "title": "ProgramLocationType",
      "description": "{{def(EventEnum:PROGRAM_LOCATION_TYPE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ProgramLocationTypeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROGRAM_LOCATION_TYPE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "ProgramNestLevel": {
      "type": "object",
      "title": "ProgramNestLevel",
      "description": "{{def(EventEnum:PROGRAM_NEST_LEVEL)}} If an initial value is not defined, the nesting level associated with the highest or initial nesting level of the program **MUST** default to zero (0).",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROGRAM_NEST_LEVEL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "RotaryMode": {
      "type": "object",
      "title": "RotaryMode",
      "description": "{{def(EventEnum:ROTARY_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/RotaryModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ROTARY_MODE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "RotaryVelocityOverride": {
      "type": "object",
      "title": "RotaryVelocityOverride",
      "description": "{{def(EventEnum:ROTARY_VELOCITY_OVERRIDE)}} This command represents a percentage change to the velocity calculated by a logic or motion program or set by a switch for a {{block(Rotary)}} type axis.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ROTARY_VELOCITY_OVERRIDE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SerialNumber": {
      "type": "object",
      "title": "SerialNumber",
      "description": "{{def(EventEnum:SERIAL_NUMBER)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "SERIAL_NUMBER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SpindleInterlock": {
      "type": "object",
      "title": "SpindleInterlock",
      "description": "{{def(EventEnum:SPINDLE_INTERLOCK)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/SpindleInterlockEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "SPINDLE_INTERLOCK"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolAssetId": {
      "type": "object",
      "title": "ToolAssetId",
      "description": "{{def(EventEnum:TOOL_ASSET_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "TOOL_ASSET_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolGroup": {
      "type": "object",
      "title": "ToolGroup",
      "description": "{{def(EventEnum:TOOL_GROUP)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "TOOL_GROUP"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolId": {
      "type": "object",
      "title": "ToolId",
      "description": "{{def(EventEnum:TOOL_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "TOOL_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolNumber": {
      "type": "object",
      "title": "ToolNumber",
      "description": "{{def(EventEnum:TOOL_NUMBER)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "TOOL_NUMBER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolOffset": {
      "type": "object",
      "title": "ToolOffset",
      "description": "{{def(EventEnum:TOOL_OFFSET)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "TOOL_OFFSET"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "User": {
      "type": "object",
      "title": "User",
      "description": "{{def(EventEnum:USER)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "USER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "Variable": {
      "type": "object",
      "title": "Variable",
      "description": "{{def(EventEnum:VARIABLE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "VARIABLE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WaitState": {
      "type": "object",
      "title": "WaitState",
      "description": "{{def(EventEnum:WAIT_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/WaitStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "WAIT_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Wire": {
      "type": "object",
      "title": "Wire",
      "description": "{{def(EventEnum:WIRE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "WIRE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WorkholdingId": {
      "type": "object",
      "title": "WorkholdingId",
      "description": "{{def(EventEnum:WORKHOLDING_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "WORKHOLDING_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WorkOffset": {
      "type": "object",
      "title": "WorkOffset",
      "description": "{{def(EventEnum:WORK_OFFSET)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "WORK_OFFSET"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "OperatingSystem": {
      "type": "object",
      "title": "OperatingSystem",
      "description": "{{def(EventEnum:OPERATING_SYSTEM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "OPERATING_SYSTEM"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "Firmware": {
      "type": "object",
      "title": "Firmware",
      "description": "{{def(EventEnum:FIRMWARE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "FIRMWARE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "Application": {
      "type": "object",
      "title": "Application",
      "description": "{{def(EventEnum:APPLICATION)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "APPLICATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "Library": {
      "type": "object",
      "title": "Library",
      "description": "{{def(EventEnum:LIBRARY)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "LIBRARY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "Hardware": {
      "type": "object",
      "title": "Hardware",
      "description": "{{def(EventEnum:HARDWARE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "HARDWARE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "Network": {
      "type": "object",
      "title": "Network",
      "description": "{{def(EventEnum:NETWORK)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "NETWORK"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "Rotation": {
      "type": "object",
      "title": "Rotation",
      "description": "{{def(EventEnum:ROTATION)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ROTATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE_3D"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Translation": {
      "type": "object",
      "title": "Translation",
      "description": "{{def(EventEnum:TRANSLATION)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "TRANSLATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER_3D"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessKindId": {
      "type": "object",
      "title": "ProcessKindId",
      "description": "{{def(EventEnum:PROCESS_KIND_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROCESS_KIND_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "PartStatus": {
      "type": "object",
      "title": "PartStatus",
      "description": "{{def(EventEnum:PART_STATUS)}} If unique identifier is given, part status is for that individual. If group identifier is given without a unique identifier, then the status is assumed to be for the whole group.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/PartStatusEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_STATUS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AlarmLimit": {
      "type": "object",
      "title": "AlarmLimit",
      "description": "{{def(EventEnum:ALARM_LIMIT)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ALARM_LIMIT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessAggregateId": {
      "type": "object",
      "title": "ProcessAggregateId",
      "description": "{{def(EventEnum:PROCESS_AGGREGATE_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROCESS_AGGREGATE_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "PartKindId": {
      "type": "object",
      "title": "PartKindId",
      "description": "{{def(EventEnum:PART_KIND_ID)}} If no {{property(subType)}} is specified, `UUID` is default.",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "UUID"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_KIND_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "AdapterURI": {
      "type": "object",
      "title": "AdapterURI",
      "description": "{{def(EventEnum:ADAPTER_URI)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ADAPTER_URI"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "DeviceRemoved": {
      "type": "object",
      "title": "DeviceRemoved",
      "description": "{{def(EventEnum:DEVICE_REMOVED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "DEVICE_REMOVED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "DeviceChanged": {
      "type": "object",
      "title": "DeviceChanged",
      "description": "{{def(EventEnum:DEVICE_CHANGED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "DEVICE_CHANGED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SpecificationLimit": {
      "type": "object",
      "title": "SpecificationLimit",
      "description": "{{def(EventEnum:SPECIFICATION_LIMIT)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "SPECIFICATION_LIMIT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ConnectionStatus": {
      "type": "object",
      "title": "ConnectionStatus",
      "description": "{{def(EventEnum:CONNECTION_STATUS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ConnectionStatusEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CONNECTION_STATUS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AdapterSoftwareVersion": {
      "type": "object",
      "title": "AdapterSoftwareVersion",
      "description": "{{def(EventEnum:ADAPTER_SOFTWARE_VERSION)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ADAPTER_SOFTWARE_VERSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SensorAttachment": {
      "type": "object",
      "title": "SensorAttachment",
      "description": "{{def(EventEnum:SENSOR_ATTACHMENT)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "SENSOR_ATTACHMENT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ControlLimit": {
      "type": "object",
      "title": "ControlLimit",
      "description": "{{def(EventEnum:CONTROL_LIMIT)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CONTROL_LIMIT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "DeviceAdded": {
      "type": "object",
      "title": "DeviceAdded",
      "description": "{{def(EventEnum:DEVICE_ADDED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "DEVICE_ADDED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "MTConnectVersion": {
      "type": "object",
      "title": "MTConnectVersion",
      "description": "{{def(EventEnum:MTCONNECT_VERSION)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "MTCONNECT_VERSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessOccurrenceId": {
      "type": "object",
      "title": "ProcessOccurrenceId",
      "description": "{{def(EventEnum:PROCESS_OCCURRENCE_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROCESS_OCCURRENCE_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "PartGroupId": {
      "type": "object",
      "title": "PartGroupId",
      "description": "{{def(EventEnum:PART_GROUP_ID)}} If no {{property(subType)}} is specified, `UUID` is default.",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "UUID"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_GROUP_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "PartUniqueId": {
      "type": "object",
      "title": "PartUniqueId",
      "description": "{{def(EventEnum:PART_UNIQUE_ID)}} If no {{property(subType)}} is specified, `UUID` is default.",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "UUID"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_UNIQUE_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "ActivationCount": {
      "type": "object",
      "title": "ActivationCount",
      "description": "{{def(EventEnum:ACTIVATION_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ALL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ACTIVATION_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "DeactivationCount": {
      "type": "object",
      "title": "DeactivationCount",
      "description": "{{def(EventEnum:DEACTIVATION_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ALL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "DEACTIVATION_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "TransferCount": {
      "type": "object",
      "title": "TransferCount",
      "description": "{{def(EventEnum:TRANSFER_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ALL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "TRANSFER_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "LoadCount": {
      "type": "object",
      "title": "LoadCount",
      "description": "{{def(EventEnum:LOAD_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ALL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "LOAD_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "PartProcessingState": {
      "type": "object",
      "title": "PartProcessingState",
      "description": "{{def(EventEnum:PART_PROCESSING_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/PartProcessingStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_PROCESSING_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessState": {
      "type": "object",
      "title": "ProcessState",
      "description": "{{def(EventEnum:PROCESS_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ProcessStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PROCESS_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ValveState": {
      "type": "object",
      "title": "ValveState",
      "description": "{{def(EventEnum:VALVE_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/ValveStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "VALVE_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "LockState": {
      "type": "object",
      "title": "LockState",
      "description": "{{def(EventEnum:LOCK_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/LockStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "LOCK_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "UnloadCount": {
      "type": "object",
      "title": "UnloadCount",
      "description": "{{def(EventEnum:UNLOAD_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ALL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "UNLOAD_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "CycleCount": {
      "type": "object",
      "title": "CycleCount",
      "description": "{{def(EventEnum:CYCLE_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ALL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CYCLE_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "OperatingMode": {
      "type": "object",
      "title": "OperatingMode",
      "description": "{{def(EventEnum:OPERATING_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/OperatingModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "OPERATING_MODE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AssetCount": {
      "type": "object",
      "title": "AssetCount",
      "description": "{{def(EventEnum:ASSET_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ASSET_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "MaintenanceList": {
      "type": "object",
      "title": "MaintenanceList",
      "description": "{{def(EventEnum:MAINTENANCE_LIST)}} If the {{property(INTERVAL)}} {{property(key)}} is not provided, it is assumed `ABSOLUTE`. If the {{property(DIRECTION)}} {{property(key)}} is not provided, it is assumed `UP`. If the {{property(UNITS)}} {{property(key)}} is not provided, it is assumed to be `COUNT`.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/MaintenanceListResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "MAINTENANCE_LIST"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "FixtureId": {
      "type": "object",
      "title": "FixtureId",
      "description": "{{def(EventEnum:FIXTURE_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "FIXTURE_ID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PartCountType": {
      "type": "object",
      "title": "PartCountType",
      "description": "{{def(EventEnum:PART_COUNT_TYPE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/PartCountTypeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "PART_COUNT_TYPE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ClockTime": {
      "type": "object",
      "title": "ClockTime",
      "description": "{{def(EventEnum:CLOCK_TIME)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CLOCK_TIME"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Actuator": {
      "type": "object",
      "title": "Actuator",
      "description": "{{def(ConditionEnum:ACTUATOR)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/ConditionEnum",
          "default": "ACTUATOR"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code (usually an alpha-numeric value) generated by the controller of a piece of equipment providing a reference identifier for a {{block(Condition)}}. This is the same information an operator or maintenance personnel may see as a reference code designating a specific fault code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/definitions/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "{{property(statistic)}} provides additional information describing the meaning of the {{block(Condition)}} element. {{property(statistic)}} **MUST** match the {{property(statistic)}} attribute of the {{block(DataItem)}} entity defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/definitions/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "Communications": {
      "type": "object",
      "title": "Communications",
      "description": "{{def(ConditionEnum:COMMUNICATIONS)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/ConditionEnum",
          "default": "COMMUNICATIONS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code (usually an alpha-numeric value) generated by the controller of a piece of equipment providing a reference identifier for a {{block(Condition)}}. This is the same information an operator or maintenance personnel may see as a reference code designating a specific fault code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/definitions/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "{{property(statistic)}} provides additional information describing the meaning of the {{block(Condition)}} element. {{property(statistic)}} **MUST** match the {{property(statistic)}} attribute of the {{block(DataItem)}} entity defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/definitions/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "DataRange": {
      "type": "object",
      "title": "DataRange",
      "description": "{{def(ConditionEnum:DATA_RANGE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/ConditionEnum",
          "default": "DATA_RANGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code (usually an alpha-numeric value) generated by the controller of a piece of equipment providing a reference identifier for a {{block(Condition)}}. This is the same information an operator or maintenance personnel may see as a reference code designating a specific fault code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/definitions/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "{{property(statistic)}} provides additional information describing the meaning of the {{block(Condition)}} element. {{property(statistic)}} **MUST** match the {{property(statistic)}} attribute of the {{block(DataItem)}} entity defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/definitions/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "LogicProgram": {
      "type": "object",
      "title": "LogicProgram",
      "description": "{{def(ConditionEnum:LOGIC_PROGRAM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/ConditionEnum",
          "default": "LOGIC_PROGRAM"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code (usually an alpha-numeric value) generated by the controller of a piece of equipment providing a reference identifier for a {{block(Condition)}}. This is the same information an operator or maintenance personnel may see as a reference code designating a specific fault code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/definitions/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "{{property(statistic)}} provides additional information describing the meaning of the {{block(Condition)}} element. {{property(statistic)}} **MUST** match the {{property(statistic)}} attribute of the {{block(DataItem)}} entity defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/definitions/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "MotionProgram": {
      "type": "object",
      "title": "MotionProgram",
      "description": "{{def(ConditionEnum:MOTION_PROGRAM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/ConditionEnum",
          "default": "MOTION_PROGRAM"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code (usually an alpha-numeric value) generated by the controller of a piece of equipment providing a reference identifier for a {{block(Condition)}}. This is the same information an operator or maintenance personnel may see as a reference code designating a specific fault code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/definitions/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "{{property(statistic)}} provides additional information describing the meaning of the {{block(Condition)}} element. {{property(statistic)}} **MUST** match the {{property(statistic)}} attribute of the {{block(DataItem)}} entity defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/definitions/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "System": {
      "type": "object",
      "title": "System",
      "description": "{{def(ConditionEnum:SYSTEM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/ConditionEnum",
          "default": "SYSTEM"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code (usually an alpha-numeric value) generated by the controller of a piece of equipment providing a reference identifier for a {{block(Condition)}}. This is the same information an operator or maintenance personnel may see as a reference code designating a specific fault code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/definitions/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "{{property(statistic)}} provides additional information describing the meaning of the {{block(Condition)}} element. {{property(statistic)}} **MUST** match the {{property(statistic)}} attribute of the {{block(DataItem)}} entity defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/definitions/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "MaterialFeed": {
      "type": "object",
      "title": "MaterialFeed",
      "description": "{{def(InterfaceEventEnum:MATERIAL_FEED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "MATERIAL_FEED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "MaterialChange": {
      "type": "object",
      "title": "MaterialChange",
      "description": "{{def(InterfaceEventEnum:MATERIAL_CHANGE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "MATERIAL_CHANGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "MaterialRetract": {
      "type": "object",
      "title": "MaterialRetract",
      "description": "{{def(InterfaceEventEnum:MATERIAL_RETRACT)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "MATERIAL_RETRACT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "MaterialLoad": {
      "type": "object",
      "title": "MaterialLoad",
      "description": "{{def(InterfaceEventEnum:MATERIAL_LOAD)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "MATERIAL_LOAD"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "MaterialUnload": {
      "type": "object",
      "title": "MaterialUnload",
      "description": "{{def(InterfaceEventEnum:MATERIAL_UNLOAD)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "MATERIAL_UNLOAD"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "OpenChuck": {
      "type": "object",
      "title": "OpenChuck",
      "description": "{{def(InterfaceEventEnum:OPEN_CHUCK)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "OPEN_CHUCK"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "OpenDoor": {
      "type": "object",
      "title": "OpenDoor",
      "description": "{{def(InterfaceEventEnum:OPEN_DOOR)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "OPEN_DOOR"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "PartChange": {
      "type": "object",
      "title": "PartChange",
      "description": "{{def(InterfaceEventEnum:PART_CHANGE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "PART_CHANGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "CloseDoor": {
      "type": "object",
      "title": "CloseDoor",
      "description": "{{def(InterfaceEventEnum:CLOSE_DOOR)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "CLOSE_DOOR"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "CloseChuck": {
      "type": "object",
      "title": "CloseChuck",
      "description": "{{def(InterfaceEventEnum:CLOSE_CHUCK)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(result)}} is `string`."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "CLOSE_CHUCK"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp"
      ]
    },
    "InterfaceState": {
      "type": "object",
      "title": "InterfaceState",
      "description": "{{def(InterfaceEventEnum:INTERFACE_STATE)}} When the {{block(InterfaceState)}} is `DISABLED`, the state of all data items that are specific for the {{term(interaction model)}} associated with that {{block(Interface)}} **MUST** be set to `NOT_READY`.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/definitions/InterfaceStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/InterfaceEventEnum",
          "default": "INTERFACE_STATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "CoordinateSystemEnum": {
      "title": "CoordinateSystemEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MACHINE",
            "WORK"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ResetTriggerEnum": {
      "title": "ResetTriggerEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTION_COMPLETE",
            "ANNUAL",
            "DAY",
            "LIFE",
            "MAINTENANCE",
            "MONTH",
            "POWER_ON",
            "SHIFT",
            "WEEK"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "StatisticEnum": {
      "title": "StatisticEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AVERAGE",
            "KURTOSIS",
            "MAXIMUM",
            "MEDIAN",
            "MINIMUM",
            "MODE",
            "RANGE",
            "ROOT_MEAN_SQUARE",
            "STANDARD_DEVIATION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "UnitEnum": {
      "title": "UnitEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AMPERE",
            "CELSIUS",
            "COUNT",
            "DECIBEL",
            "DEGREE",
            "DEGREE_3D",
            "DEGREE/SECOND",
            "DEGREE/SECOND\\^2",
            "HERTZ",
            "JOULE",
            "KILOGRAM",
            "LITER",
            "LITER/SECOND",
            "MICRO_RADIAN",
            "MILLIMETER",
            "MILLIMETER_3D",
            "MILLIMETER/REVOLUTION",
            "MILLIMETER/SECOND",
            "MILLIMETER/SECOND\\^2",
            "NEWTON",
            "NEWTON_METER",
            "OHM",
            "PASCAL",
            "PASCAL_SECOND",
            "PERCENT",
            "PH",
            "REVOLUTION/MINUTE",
            "SECOND",
            "SIEMENS/METER",
            "VOLT",
            "VOLT_AMPERE",
            "VOLT_AMPERE_REACTIVE",
            "WATT",
            "WATT_SECOND",
            "GRAM/CUBIC_METER",
            "CUBIC_MILLIMETER",
            "CUBIC_MILLIMETER/SECOND",
            "CUBIC_MILLIMETER/SECOND\\^2",
            "MILLIGRAM",
            "MILLIGRAM/CUBIC_MILLIMETER",
            "MILLILITER",
            "COUNT/SECOND",
            "PASCAL/SECOND",
            "UNIT_VECTOR_3D",
            "REVOLUTION/SECOND\\^2",
            "REVOLUTION/SECOND",
            "GRAM"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DataItemTypeEnum": {
      "type": "string",
      "title": "DataItemTypeEnum"
    },
    "CategoryEnum": {
      "title": "CategoryEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "SAMPLE",
            "EVENT",
            "CONDITION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DataItemSubTypeEnum": {
      "title": "DataItemSubTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ABSOLUTE",
            "ACTION",
            "ACTUAL",
            "ALL",
            "ALTERNATING",
            "A_SCALE",
            "AUXILIARY",
            "BAD",
            "BRINELL",
            "B_SCALE",
            "COMMANDED",
            "CONSUMED",
            "CONTROL",
            "C_SCALE",
            "DELAY",
            "DIRECT",
            "DRY_RUN",
            "D_SCALE",
            "EXPIRATION",
            "FIRST_USE",
            "GOOD",
            "INCREMENTAL",
            "JOG",
            "LATERAL",
            "LEEB",
            "LENGTH",
            "LINE",
            "LINEAR",
            "LOADED",
            "MACHINE_AXIS_LOCK",
            "MAIN",
            "MAINTENANCE",
            "MANUAL_UNCLAMP",
            "MANUFACTURE",
            "MAXIMUM",
            "MINIMUM",
            "MOHS",
            "MOTION",
            "NO_SCALE",
            "OPERATING",
            "OPERATOR",
            "OPTIONAL_STOP",
            "OVERRIDE",
            "POWERED",
            "PRIMARY",
            "PROBE",
            "PROCESS",
            "PROGRAMMED",
            "RADIAL",
            "RAPID",
            "REMAINING",
            "ROCKWELL",
            "ROTARY",
            "SCHEDULE",
            "SET_UP",
            "SHORE",
            "SINGLE_BLOCK",
            "STANDARD",
            "START",
            "SWITCHED",
            "TARGET",
            "TARGET_COMPLETION",
            "TOOL_CHANGE_STOP",
            "USEABLE",
            "VERTICAL",
            "VICKERS",
            "WORKING",
            "IPV4_ADDRESS",
            "IPV6_ADDRESS",
            "GATEWAY",
            "SUBNET_MASK",
            "VLAN_ID",
            "MAC_ADDRESS",
            "WIRELESS",
            "LICENSE",
            "VERSION",
            "RELEASE_DATE",
            "INSTALL_DATE",
            "MANUFACTURER",
            "UUID",
            "SERIAL_NUMBER",
            "RAW_MATERIAL",
            "LOT",
            "BATCH",
            "HEAT_TREAT",
            "PART_NUMBER",
            "PART_FAMILY",
            "PART_NAME",
            "PROCESS_STEP",
            "PROCESS_PLAN",
            "ORDER_NUMBER",
            "PROCESS_NAME",
            "ISO_STEP_EXECUTABLE",
            "COMPLETE",
            "ACTIVE",
            "FAILED",
            "ABORTED",
            "ENDED",
            "WASTE",
            "PART",
            "REQUEST",
            "RESPONSE",
            "ACTIVITY",
            "SEGMENT",
            "RECIPE",
            "OPERATION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "NativeUnitEnum": {
      "title": "NativeUnitEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CENTIPOISE",
            "DEGREE/MINUTE",
            "FAHRENHEIT",
            "FOOT",
            "FOOT/MINUTE",
            "FOOT/SECOND",
            "FOOT/SECOND\\^2",
            "FOOT_3D",
            "GALLON/MINUTE",
            "HOUR",
            "INCH",
            "INCH/MINUTE",
            "INCH/SECOND",
            "INCH/SECOND\\^2",
            "INCH_POUND",
            "INCH_3D",
            "KELVIN",
            "KILOWATT",
            "KILOWATT_HOUR",
            "LITER/MINUTE",
            "MILLIMETER/MINUTE",
            "MINUTE",
            "OTHER",
            "POUND",
            "POUND/INCH\\^2",
            "RADIAN",
            "RADIAN/MINUTE",
            "RADIAN/SECOND",
            "RADIAN/SECOND\\^2",
            "BAR",
            "TORR",
            "MILLIMETER_MERCURY",
            "PASCAL/MINUTE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FilterEnum": {
      "title": "FilterEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MINIMUM_DELTA",
            "PERIOD"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RepresentationEnum": {
      "title": "RepresentationEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "TIME_SERIES",
            "VALUE",
            "DATA_SET",
            "DISCRETE",
            "TABLE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "boolean": {
      "type": "boolean",
      "title": "boolean",
      "description": "primitive type."
    },
    "ID": {
      "type": "string",
      "title": "ID",
      "description": "string that represents an {{term(ID)}}."
    },
    "string": {
      "type": "string",
      "title": "string",
      "description": "primitive type."
    },
    "float": {
      "type": "number",
      "title": "float",
      "description": "primitive type."
    },
    "dateTime": {
      "type": "string",
      "format": "date-time",
      "title": "dateTime",
      "description": "string that represents timestamp in ISO 8601 format."
    },
    "integer": {
      "type": "integer",
      "title": "integer",
      "description": "primitive type."
    },
    "xlinktype": {
      "type": "string",
      "title": "xlinktype",
      "description": "string that represents the type of an XLink element. See {{url(https://www.w3.org/TR/xlink11/)}}."
    },
    "xslang": {
      "type": "string",
      "title": "xslang",
      "description": "string that represents a language tag. See {{url(http://www.ietf.org/rfc/rfc4646.txt)}}."
    },
    "SECOND": {
      "type": "string",
      "title": "SECOND",
      "description": "float that represents time in seconds."
    },
    "IDREF": {
      "type": "string",
      "title": "IDREF",
      "description": "string that represents a reference to an `ID`."
    },
    "xlinkhref": {
      "type": "string",
      "title": "xlinkhref",
      "description": "string that represents the locator attribute of an XLink element. See {{url(https://www.w3.org/TR/xlink11/)}}."
    },
    "ResetTriggeredEnum": {
      "title": "ResetTriggeredEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTION_COMPLETE",
            "ANNUAL",
            "DAY",
            "MAINTENANCE",
            "MANUAL",
            "MONTH",
            "POWER_ON",
            "SHIFT",
            "WEEK"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "QualifierEnum": {
      "title": "QualifierEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "HIGH",
            "LOW"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "SampleEnum": {
      "title": "SampleEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACCELERATION",
            "ACCUMULATED_TIME",
            "AMPERAGE",
            "ANGLE",
            "ANGULAR_ACCELERATION",
            "ANGULAR_VELOCITY",
            "AXIS_FEEDRATE",
            "CAPACITY_FLUID",
            "CAPACITY_SPATIAL",
            "CONCENTRATION",
            "CONDUCTIVITY",
            "CUTTING_SPEED",
            "DENSITY",
            "DEPOSITION_ACCELERATION_VOLUMETRIC",
            "DEPOSITION_DENSITY",
            "DEPOSITION_MASS",
            "DEPOSITION_RATE_VOLUMETRIC",
            "DEPOSITION_VOLUME",
            "DISPLACEMENT",
            "ELECTRICAL_ENERGY",
            "EQUIPMENT_TIMER",
            "FILL_LEVEL",
            "FLOW",
            "FREQUENCY",
            "GLOBAL_POSITION",
            "LENGTH",
            "LEVEL",
            "LINEAR_FORCE",
            "LOAD",
            "MASS",
            "PATH_FEEDRATE",
            "PATH_FEEDRATE_PER_REVOLUTION",
            "PATH_POSITION",
            "PH",
            "POSITION",
            "POWER_FACTOR",
            "PRESSURE",
            "PROCESS_TIMER",
            "RESISTANCE",
            "ROTARY_VELOCITY",
            "SOUND_LEVEL",
            "SPINDLE_SPEED",
            "STRAIN",
            "TEMPERATURE",
            "TENSION",
            "TILT",
            "TORQUE",
            "VELOCITY",
            "VISCOSITY",
            "VOLTAGE",
            "VOLT_AMPERE",
            "VOLT_AMPERE_REACTIVE",
            "VOLUME_FLUID",
            "VOLUME_SPATIAL",
            "WATTAGE",
            "AMPERAGE_AC",
            "AMPERAGE_DC",
            "VOLTAGE_AC",
            "VOLTAGE_DC",
            "X_DIMENSION",
            "Y_DIMENSION",
            "Z_DIMENSION",
            "DIAMETER",
            "ORIENTATION",
            "HUMIDITY_RELATIVE",
            "HUMIDITY_ABSOLUTE",
            "HUMIDITY_SPECIFIC",
            "OBSERVATION_UPDATE_RATE",
            "ASSET_UPDATE_RATE",
            "PRESSURIZATION_RATE",
            "DECELERATION",
            "ANGULAR_DECELERATION",
            "PRESSURE_ABSOLUTE",
            "OPENNESS"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "EventEnum": {
      "title": "EventEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE_AXES",
            "ACTUATOR_STATE",
            "ALARM",
            "ASSET_CHANGED",
            "ASSET_REMOVED",
            "AVAILABILITY",
            "AXIS_COUPLING",
            "AXIS_FEEDRATE_OVERRIDE",
            "AXIS_INTERLOCK",
            "AXIS_STATE",
            "BLOCK",
            "BLOCK_COUNT",
            "CHUCK_INTERLOCK",
            "CHUCK_STATE",
            "CODE",
            "COMPOSITION_STATE",
            "CONTROLLER_MODE",
            "CONTROLLER_MODE_OVERRIDE",
            "COUPLED_AXES",
            "DATE_CODE",
            "DEVICE_UUID",
            "DIRECTION",
            "DOOR_STATE",
            "EMERGENCY_STOP",
            "END_OF_BAR",
            "EQUIPMENT_MODE",
            "EXECUTION",
            "FUNCTIONAL_MODE",
            "HARDNESS",
            "LINE",
            "LINE_LABEL",
            "LINE_NUMBER",
            "MATERIAL",
            "MATERIAL_LAYER",
            "MESSAGE",
            "OPERATOR_ID",
            "PALLET_ID",
            "PART_COUNT",
            "PART_DETECT",
            "PART_ID",
            "PART_NUMBER",
            "PATH_FEEDRATE_OVERRIDE",
            "PATH_MODE",
            "POWER_STATE",
            "POWER_STATUS",
            "PROCESS_TIME",
            "PROGRAM",
            "PROGRAM_COMMENT",
            "PROGRAM_EDIT",
            "PROGRAM_EDIT_NAME",
            "PROGRAM_HEADER",
            "PROGRAM_LOCATION",
            "PROGRAM_LOCATION_TYPE",
            "PROGRAM_NEST_LEVEL",
            "ROTARY_MODE",
            "ROTARY_VELOCITY_OVERRIDE",
            "SERIAL_NUMBER",
            "SPINDLE_INTERLOCK",
            "TOOL_ASSET_ID",
            "TOOL_GROUP",
            "TOOL_ID",
            "TOOL_NUMBER",
            "TOOL_OFFSET",
            "USER",
            "VARIABLE",
            "WAIT_STATE",
            "WIRE",
            "WORKHOLDING_ID",
            "WORK_OFFSET",
            "OPERATING_SYSTEM",
            "FIRMWARE",
            "APPLICATION",
            "LIBRARY",
            "HARDWARE",
            "NETWORK",
            "ROTATION",
            "TRANSLATION",
            "DEVICE_ADDED",
            "DEVICE_REMOVED",
            "DEVICE_CHANGED",
            "CONNECTION_STATUS",
            "ADAPTER_SOFTWARE_VERSION",
            "ADAPTER_URI",
            "MTCONNECT_VERSION",
            "SENSOR_ATTACHMENT",
            "PART_STATUS",
            "PROCESS_OCCURRENCE_ID",
            "PROCESS_AGGREGATE_ID",
            "PROCESS_KIND_ID",
            "PART_GROUP_ID",
            "PART_KIND_ID",
            "PART_UNIQUE_ID",
            "CONTROL_LIMIT",
            "SPECIFICATION_LIMIT",
            "ALARM_LIMIT",
            "LOAD_COUNT",
            "UNLOAD_COUNT",
            "TRANSFER_COUNT",
            "ACTIVATION_COUNT",
            "DEACTIVATION_COUNT",
            "CYCLE_COUNT",
            "VALVE_STATE",
            "LOCK_STATE",
            "PROCESS_STATE",
            "PART_PROCESSING_STATE",
            "OPERATING_MODE",
            "ASSET_COUNT",
            "MAINTENANCE_LIST",
            "FIXTURE_ID",
            "PART_COUNT_TYPE",
            "CLOCK_TIME"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ConditionEnum": {
      "title": "ConditionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "COMMUNICATIONS",
            "DATA_RANGE",
            "LOGIC_PROGRAM",
            "MOTION_PROGRAM",
            "SYSTEM",
            "ACTUATOR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MotionActuationTypeEnum": {
      "title": "MotionActuationTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "DIRECT",
            "VIRTUAL",
            "NONE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MotionTypeEnum": {
      "title": "MotionTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PRISMATIC",
            "CONTINUOUS",
            "REVOLUTE",
            "FIXED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CoordinateSystemTypeEnum": {
      "title": "CoordinateSystemTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "WORLD",
            "BASE",
            "OBJECT",
            "TASK",
            "MECHANICAL_INTERFACE",
            "TOOL",
            "MOBILE_PLATFORM",
            "MACHINE",
            "CAMERA"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DataItemRelationshipTypeEnum": {
      "title": "DataItemRelationshipTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ATTACHMENT",
            "COORDINATE_SYSTEM",
            "LIMIT",
            "OBSERVATION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "SpecificationRelationshipTypeEnum": {
      "title": "SpecificationRelationshipTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "LIMIT"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MediaTypeEnum": {
      "title": "MediaTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "STEP",
            "STL",
            "GDML",
            "OBJ",
            "COLLADA",
            "IGES",
            "3DS",
            "ACIS",
            "X_T"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "OriginatorEnum": {
      "title": "OriginatorEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MANUFACTURER",
            "USER"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "QIFDocumentTypeEnum": {
      "title": "QIFDocumentTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MEASUREMENT_RESOURCE",
            "PLAN",
            "PRODUCT",
            "RESULTS",
            "RULES",
            "STATISTICS"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ProcessStateEnum": {
      "title": "ProcessStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "INITIALIZING",
            "READY",
            "ACTIVE",
            "COMPLETE",
            "INTERRUPTED",
            "ABORTED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AssetTypeEnum": {
      "title": "AssetTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CuttingTool",
            "File",
            "QIFDocumentWrapper",
            "RawMaterial"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ChuckStateEnum": {
      "title": "ChuckStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "OPEN",
            "CLOSED",
            "UNLATCHED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AxisCouplingEnum": {
      "title": "AxisCouplingEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "TANDEM",
            "SYNCHRONOUS",
            "MASTER",
            "SLAVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PartStatusEnum": {
      "title": "PartStatusEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PASS",
            "FAIL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DirectionRotaryEnum": {
      "title": "DirectionRotaryEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CLOCKWISE",
            "COUNTER_CLOCKWISE",
            "NONE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FileStateEnum": {
      "title": "FileStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "EXPERIMENTAL",
            "PRODUCTION",
            "REVISION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CriticalityTypeEnum": {
      "title": "CriticalityTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CRITICAL",
            "NONCRITICAL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateMotionEnum": {
      "title": "CompositionStateMotionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "OPEN",
            "UNLATCHED",
            "CLOSED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ActuatorStateEnum": {
      "title": "ActuatorStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ConnectionStatusEnum": {
      "title": "ConnectionStatusEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CLOSED",
            "LISTEN",
            "ESTABLISHED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RoleTypeEnum": {
      "title": "RoleTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "SYSTEM",
            "AUXILIARY"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PathModeEnum": {
      "title": "PathModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "INDEPENDENT",
            "MASTER",
            "SYNCHRONOUS",
            "MIRROR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateActionEnum": {
      "title": "CompositionStateActionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MILLIMETER": {
      "type": "string",
      "title": "MILLIMETER",
      "description": "float that represents measurement in millimeter."
    },
    "PartProcessingStateEnum": {
      "title": "PartProcessingStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "NEEDS_PROCESSING",
            "IN_PROCESS",
            "PROCESSING_ENDED",
            "PROCESSING_ENDED_COMPLETE",
            "PROCESSING_ENDED_STOPPED",
            "PROCESSING_ENDED_ABORTED",
            "PROCESSING_ENDED_LOST",
            "PROCESSING_ENDED_SKIPPED",
            "PROCESSING_ENDED_REJECTED",
            "WAITING_FOR_TRANSIT",
            "IN_TRANSIT",
            "TRANSIT_COMPLETE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ControllerModeEnum": {
      "title": "ControllerModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AUTOMATIC",
            "MANUAL",
            "MANUAL_DATA_INPUT",
            "SEMI_AUTOMATIC",
            "EDIT",
            "FEED_HOLD"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DirectionLinearEnum": {
      "title": "DirectionLinearEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "POSITIVE",
            "NEGATIVE",
            "NONE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "EndOfBarEnum": {
      "title": "EndOfBarEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "YES",
            "NO"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "LocationTypeEnum": {
      "title": "LocationTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "description": "Enumeration for Location types",
          "enum": [
            "POT",
            "STATION",
            "CRIB",
            "SPINDLE",
            "TRANSFER_POT",
            "RETURN_POT",
            "STAGING_POT",
            "REMOVAL_POT",
            "EXPIRED_POT",
            "END_EFFECTOR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ValveStateEnum": {
      "title": "ValveStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "OPEN",
            "OPENING",
            "CLOSED",
            "CLOSING"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CodeEnum": {
      "title": "CodeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "BDX",
            "LBX",
            "APMX",
            "DC",
            "DF",
            "OAL",
            "DMM",
            "H",
            "LS",
            "LUX",
            "LPR",
            "WT",
            "LF",
            "CRP",
            "L",
            "DRVA",
            "WF",
            "IC",
            "SIG",
            "KAPR",
            "PSIR",
            "N/A",
            "BS",
            "SDLx",
            "STAx",
            "DCx",
            "HF",
            "RE",
            "LFx",
            "BCH",
            "CHW",
            "W1"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ApplicationCategoryEnum": {
      "title": "ApplicationCategoryEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ASSEMBLY",
            "DEVICE",
            "HANDLING",
            "MAINTENANCE",
            "PART",
            "PROCESS",
            "INSPECTION",
            "SETUP"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CutterStatusTypeEnum": {
      "title": "CutterStatusTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "description": "Enumeration for CutterStatus values.",
          "enum": [
            "NEW",
            "AVAILABLE",
            "UNAVAILABLE",
            "ALLOCATED",
            "UNALLOCATED",
            "MEASURED",
            "RECONDITIONED",
            "USED",
            "EXPIRED",
            "BROKEN",
            "NOT_REGISTERED",
            "UNKNOWN"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateLateralEnum": {
      "title": "CompositionStateLateralEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "RIGHT",
            "LEFT",
            "TRANSITIONING"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PartDetectEnum": {
      "title": "PartDetectEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PRESENT",
            "NOT_PRESENT"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ExecutionEnum": {
      "title": "ExecutionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "READY",
            "ACTIVE",
            "INTERRUPTED",
            "FEED_HOLD",
            "STOPPED",
            "OPTIONAL_STOP",
            "PROGRAM_STOPPED",
            "PROGRAM_COMPLETED",
            "WAIT"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DEGREE": {
      "type": "string",
      "title": "DEGREE",
      "description": "float that represents angle in degrees."
    },
    "AvailabilityEnum": {
      "title": "AvailabilityEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AVAILABLE",
            "UNAVAILABLE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ProgramEditEnum": {
      "title": "ProgramEditEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "READY",
            "NOT_READY"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ControllerModeOverrideEnum": {
      "title": "ControllerModeOverrideEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FunctionalModeEnum": {
      "title": "FunctionalModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PRODUCTION",
            "SETUP",
            "TEARDOWN",
            "MAINTENANCE",
            "PROCESS_DEVELOPMENT"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "WaitStateEnum": {
      "title": "WaitStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "POWERING_UP",
            "POWERING_DOWN",
            "PART_LOAD",
            "PART_UNLOAD",
            "TOOL_LOAD",
            "TOOL_UNLOAD",
            "MATERIAL_LOAD",
            "MATERIAL_UNLOAD",
            "SECONDARY_PROCESS",
            "PAUSING",
            "RESUMING"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RotaryModeEnum": {
      "title": "RotaryModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "SPINDLE",
            "INDEX",
            "CONTOUR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateSwitchedEnum": {
      "title": "CompositionStateSwitchedEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "EmergencyStopEnum": {
      "title": "EmergencyStopEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ARMED",
            "TRIGGERED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "LockStateEnum": {
      "title": "LockStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "LOCKED",
            "UNLOCKED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AxisStateEnum": {
      "title": "AxisStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "HOME",
            "TRAVEL",
            "PARKED",
            "STOPPED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "InterfaceStateEnum": {
      "title": "InterfaceStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ENABLED",
            "DISABLED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RelationshipTypeEnum": {
      "title": "RelationshipTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PARENT",
            "CHILD",
            "PEER"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FormatTypeEnum": {
      "title": "FormatTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "description": "Enumeration for CuttingToolDefinition format values.",
          "enum": [
            "EXPRESS",
            "TEXT",
            "UNDEFINED",
            "XML"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionTypeEnum": {
      "title": "CompositionTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTUATOR",
            "AMPLIFIER",
            "BALLSCREW",
            "BELT",
            "BRAKE",
            "CHAIN",
            "CHOPPER",
            "CHUCK",
            "CHUTE",
            "CIRCUIT_BREAKER",
            "CLAMP",
            "COMPRESSOR",
            "DOOR",
            "DRAIN",
            "ENCODER",
            "EXPOSURE_UNIT",
            "EXTRUSION_UNIT",
            "FAN",
            "FILTER",
            "GALVANOMOTOR",
            "GRIPPER",
            "HOPPER",
            "LINEAR_POSITION_FEEDBACK",
            "MOTOR",
            "OIL",
            "POWER_SUPPLY",
            "PULLEY",
            "PUMP",
            "REEL",
            "SENSING_ELEMENT",
            "SPREADER",
            "STORAGE_BATTERY",
            "SWITCH",
            "TABLE",
            "TANK",
            "TENSIONER",
            "TRANSFORMER",
            "VALVE",
            "VAT",
            "WATER",
            "WIRE",
            "WORKPIECE",
            "COOLING_TOWER",
            "POT",
            "STATION",
            "TRANSFER_ARM",
            "TRANSFER_POT",
            "RETURN_POT",
            "STAGING_POT",
            "REMOVAL_POT",
            "EXPIRED_POT"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CountDirectionTypeEnum": {
      "title": "CountDirectionTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "description": "Enumeration for countDirection types.",
          "enum": [
            "UP",
            "DOWN"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ApplicationTypeEnum": {
      "title": "ApplicationTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "DESIGN",
            "DATA",
            "DOCUMENTATION",
            "INSTRUCTIONS",
            "LOG",
            "PRODUCTION_PROGRAM"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FormEnum": {
      "title": "FormEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "BAR",
            "SHEET",
            "BLOCK",
            "CASTING",
            "POWDER",
            "LIQUID",
            "GEL",
            "FILAMENT",
            "GAS"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateVerticalEnum": {
      "title": "CompositionStateVerticalEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "UP",
            "DOWN",
            "TRANSITIONING"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RequestStateEnum": {
      "title": "RequestStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "NOT_READY",
            "READY",
            "ACTIVE",
            "FAIL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ResponseStateEnum": {
      "title": "ResponseStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "NOT_READY",
            "READY",
            "ACTIVE",
            "COMPLETE",
            "FAIL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "SpindleInterlockEnum": {
      "title": "SpindleInterlockEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AxisInterlockEnum": {
      "title": "AxisInterlockEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ChuckInterlockEnum": {
      "title": "ChuckInterlockEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DoorStateEnum": {
      "title": "DoorStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "OPEN",
            "CLOSED",
            "UNLATCHED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "EquipmentModeEnum": {
      "title": "EquipmentModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PowerStateEnum": {
      "title": "PowerStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ConditionStateEnum": {
      "title": "ConditionStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "Normal",
            "Warning",
            "Fault"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ToolLifeEnum": {
      "title": "ToolLifeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MINUTES",
            "PART_COUNT",
            "WEAR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "x509": {
      "type": "string",
      "title": "x509",
      "description": "string that represents an `x509` data block. {{cite(ISO/IEC 9594-8:2020)}}."
    },
    "unit": {
      "type": "string",
      "title": "unit"
    },
    "CUBIC_MILLIMETER": {
      "type": "string",
      "title": "CUBIC_MILLIMETER",
      "description": "float that represents volume in cubic millimeter."
    },
    "OperatingModeEnum": {
      "title": "OperatingModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AUTOMATIC",
            "MANUAL",
            "SEMI_AUTOMATIC"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "int32": {
      "type": "string",
      "title": "int32",
      "description": "32-bit integer."
    },
    "int64": {
      "type": "string",
      "title": "int64",
      "description": "64-bit integer."
    },
    "version": {
      "type": "string",
      "title": "version",
      "description": "series of four numeric values, separated by a decimal point, representing a {{term(major)}}, {{term(minor)}}, and {{term(revision)}} number of the MTConnect Standard and the revision number of a specific {{term(schema)}}."
    },
    "uInt32": {
      "type": "string",
      "title": "uInt32",
      "description": "32-bit unsigned integer."
    },
    "uInt64": {
      "type": "string",
      "title": "uInt64",
      "description": "64-bit unsigned integer."
    },
    "ErrorCodeEnum": {
      "title": "ErrorCodeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ASSET_NOT_FOUND",
            "INTERNAL_ERROR",
            "INVALID_REQUEST",
            "INVALID_URI",
            "INVALID_XPATH",
            "NO_DEVICE",
            "OUT_OF_RANGE",
            "QUERY_ERROR",
            "TOO_MANY",
            "UNAUTHORIZED",
            "UNSUPPORTED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MaintenanceListDirectionEnum": {
      "title": "MaintenanceListDirectionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "UP",
            "DOWN"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MaintenanceListIntervalEnum": {
      "title": "MaintenanceListIntervalEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ABSOLUTE",
            "INCREMENTAL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PartCountTypeEnum": {
      "title": "PartCountTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "EACH",
            "BATCH"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AlarmSeverityEnum": {
      "title": "AlarmSeverityEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CRITICAL",
            "ERROR",
            "WARNING",
            "INFORMATION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AlarmStateEnum": {
      "title": "AlarmStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "INSTANT",
            "ACTIVE",
            "CLEARED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AlarmCodeEnum": {
      "title": "AlarmCodeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CRASH",
            "JAM",
            "FAILURE",
            "FAULT",
            "STALLED",
            "OVERLOAD",
            "ESTOP",
            "MATERIAL",
            "MESSAGE",
            "OTHER"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DirectionEnum": {
      "title": "DirectionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CLOCKWISE",
            "COUNTER_CLOCKWISE",
            "POSITIVE",
            "NEGATIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PowerStatusEnum": {
      "title": "PowerStatusEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "NetworkWirelessEnum": {
      "title": "NetworkWirelessEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "YES",
            "NO"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ProgramLocationTypeEnum": {
      "title": "ProgramLocationTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "LOCAL",
            "EXTERNAL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "InterfaceEventEnum": {
      "title": "InterfaceEventEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "INTERFACE_STATE",
            "MATERIAL_FEED",
            "MATERIAL_CHANGE",
            "MATERIAL_RETRACT",
            "PART_CHANGE",
            "MATERIAL_LOAD",
            "MATERIAL_UNLOAD",
            "OPEN_CHUCK",
            "OPEN_DOOR",
            "CLOSE_CHUCK",
            "CLOSE_DOOR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ControlLimitResult": {
      "type": "object",
      "title": "ControlLimitResult",
      "properties": {
        "UPPER_LIMIT": {
          "type": "number",
          "description": "upper conformance boundary for a variable. > Note: immediate concern or action may be required."
        },
        "UPPER_WARNING": {
          "type": "number",
          "description": "upper boundary indicating increased concern and supervision may be required."
        },
        "NOMINAL": {
          "type": "number",
          "description": "ideal or desired value for a variable."
        },
        "LOWER_WARNING": {
          "type": "number",
          "description": "lower boundary indicating increased concern and supervision may be required."
        },
        "LOWER_LIMIT": {
          "type": "number",
          "description": "lower conformance boundary for a variable. > Note: immediate concern or action may be required."
        }
      },
      "additionalProperties": false
    },
    "SpecificationLimitResult": {
      "type": "object",
      "title": "SpecificationLimitResult",
      "properties": {
        "UPPER_LIMIT": {
          "type": "number",
          "description": "upper conformance boundary for a variable. > Note: immediate concern or action may be required."
        },
        "NOMINAL": {
          "type": "number",
          "description": "ideal or desired value for a variable."
        },
        "LOWER_LIMIT": {
          "type": "number",
          "description": "lower conformance boundary for a variable. > Note: immediate concern or action may be required."
        }
      },
      "additionalProperties": false
    },
    "AlarmLimitResult": {
      "type": "object",
      "title": "AlarmLimitResult",
      "properties": {
        "UPPER_LIMIT": {
          "type": "number",
          "description": "upper conformance boundary for a variable. > Note: immediate concern or action may be required."
        },
        "UPPER_WARNING": {
          "type": "number",
          "description": "upper boundary indicating increased concern and supervision may be required."
        },
        "LOWER_WARNING": {
          "type": "number",
          "description": "lower boundary indicating increased concern and supervision may be required."
        },
        "LOWER_LIMIT": {
          "type": "number",
          "description": "lower conformance boundary for a variable. > Note: immediate concern or action may be required."
        }
      },
      "additionalProperties": false
    },
    "SensorAttachmentResult": {
      "type": "object",
      "title": "SensorAttachmentResult",
      "properties": {
        "SENSOR_ID": {
          "type": "string",
          "description": "The identity of a sensor used to observe some measurement of an item."
        }
      },
      "additionalProperties": false
    },
    "MaintenanceListResult": {
      "type": "object",
      "title": "MaintenanceListResult",
      "properties": {
        "VALUE": {
          "type": "number",
          "description": "current interval value of the activity."
        },
        "INTERVAL": {
          "$ref": "#/definitions/MaintenanceListIntervalEnum",
          "description": "interval of the value observed.",
          "default": "ABSOLUTE"
        },
        "NEXT_SERVICE_DATE": {
          "type": "string",
          "format": "date-time",
          "description": "next date/time stamp that maintenance should be performed."
        },
        "RESET": {
          "type": "string",
          "format": "date-time",
          "description": "last date/time stamp of the {{term(observation)}} was reset."
        },
        "SEVERITY": {
          "type": "integer",
          "description": "level of severity on a scale of 1-10."
        },
        "DIRECTION": {
          "$ref": "#/definitions/MaintenanceListDirectionEnum",
          "description": "direction of the value observed.",
          "default": "UP"
        },
        "NAME": {
          "type": "string",
          "description": "identifier of the maintenance activity."
        },
        "LAST_SERVICE_DATE": {
          "type": "string",
          "format": "date-time",
          "description": "last date/time stamp that maintenance was performed."
        },
        "UNITS": {
          "$ref": "#/definitions/UnitEnum",
          "description": "same as {{block(DataItem)}} {{property(units)}}. See {{package(Device Information Model)}}.",
          "default": "COUNT"
        },
        "TARGET": {
          "type": "number",
          "description": "target value of the next maintenance."
        }
      },
      "required": [
        "NAME",
        "VALUE"
      ],
      "additionalProperties": false
    },
    "organizer": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "deprecated": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "extensible": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "informative": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "valueType": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "normative": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "observes": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "Header": {
      "type": "object",
      "title": "Header",
      "description": "provides information from an {{term(agent)}} defining version information, storage capacity, and parameters associated with the data management within the {{term(agent)}}.",
      "properties": {
        "schemaVersion": {
          "type": "string"
        },
        "bufferSize": {
          "type": "integer",
          "minimum": 0
        },
        "nextSequence": {
          "type": "integer"
        },
        "lastSequence": {
          "type": "integer"
        },
        "firstSequence": {
          "type": "integer"
        },
        "deviceModelChangeTime": {
          "type": "string",
          "format": "date-time",
          "description": "timestamp of the last update of the {{block(Device)}} information for any device."
        },
        "assetBufferSize": {
          "type": "integer",
          "minimum": 0,
          "description": "maximum number of {{block(Asset)}} types that can be stored in the {{term(agent)}} that published the {{term(response document)}}. > Note: The implementer is responsible for allocating the appropriate amount of storage capacity required to accommodate the {{property(assetBufferSize)}}."
        },
        "assetCount": {
          "type": "integer",
          "minimum": 0,
          "description": "current number of {{block(Asset)}} that are currently stored in the {{term(agent)}} as of the {{property(creationTime)}} that the {{term(agent)}} published the {{term(response document)}}. {{property(assetCount)}} **MUST NOT** be larger than the value reported for {{property(assetBufferSize)}}."
        },
        "creationTime": {
          "type": "string",
          "format": "date-time",
          "description": "timestamp that an {{term(agent)}} published the {{term(response document)}}."
        },
        "instanceId": {
          "type": "integer",
          "description": "identifier for a specific instantiation of the {{term(buffer)}} associated with the {{term(agent)}} that published the {{term(response document)}}. {{property(instanceId)}} **MUST** be changed to a different unique number each time the {{term(buffer)}} is cleared and a new set of data begins to be collected."
        },
        "sender": {
          "type": "string",
          "description": "identification defining where the {{term(agent)}} that published the {{term(response document)}} is installed or hosted. {{property(sender)}} **MUST** be either an IP Address or Hostname describing where the {{term(agent)}} is installed or the URL of the {{term(agent)}}; e.g., `http://<address>[:port]/`. > Note: The port number need not be specified if it is the default HTTP port 80."
        },
        "testIndicator": {
          "type": "boolean",
          "description": "indicates whether the {{term(agent)}} that published the {{term(response document)}} is operating in a test mode. If {{property(testIndicator)}} is not specified, the value for {{property(testIndicator)}} **MUST** be interpreted to be `false`.",
          "default": "false"
        },
        "version": {
          "type": "string",
          "description": "{{term(major)}}, {{term(minor)}}, and {{term(revision)}} number of the MTConnect Standard that defines the {{term(semantic data model)}} that represents the content of the {{term(response document)}}. It also includes the revision number of the {{term(schema)}} associated with that specific {{term(semantic data model)}}. As an example, the value reported for {{property(version)}} for a {{term(response document)}} that was structured based on {{term(schema)}} revision 10 associated with Version 1.4.0 of the MTConnect Standard would be: 1.4.0.10"
        }
      },
      "required": [
        "creationTime",
        "deviceModelChangeTime",
        "instanceId",
        "sender",
        "version"
      ],
      "additionalProperties": false
    },
    "MTConnectStreams": {
      "type": "object",
      "title": "MTConnectStreams",
      "description": "root entity of an {{term(MTConnectStreams Response Document)}} that contains the {{term(Observation Information Model)}} of one or more {{block(Device)}} entities. ![MTConnectStreams](figures/MTConnectStreams.png \"MTConnectStreams\"){: width=\"0.8\"} > Note: Additional properties of {{block(MTConnectStreams)}} **MAY** be defined for schema and namespace declaration. See {{sect(Schema and Namespace Declaration Information)}} for an {{term(XML)}} example.",
      "properties": {
        "jsonVersion": {
          "type": "integer"
        },
        "schemaVersion": {
          "type": "string"
        },
        "Header": {
          "$ref": "#/definitions/Header"
        },
        "Streams": {
          "type": "object",
          "properties": {
            "DeviceStream": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/DeviceStream"
              }
            }
          },
          "additionalProperties": false
        }
      },
      "required": [
        "Header"
      ],
      "additionalProperties": false
    },
    "Error": {
      "type": "object",
      "title": "Error",
      "description": "error encountered by an {{term(agent)}} when responding to a {{term(request)}}.",
      "properties": {
        "errorCode": {
          "$ref": "#/definitions/ErrorCodeEnum",
          "description": "descriptive code that indicates the type of error that was encountered by an {{term(agent)}}."
        },
        "value": {
          "type": "string",
          "description": "textual description of the error and any additional information an {{term(agent)}} is capable of providing regarding a specific error."
        }
      },
      "required": [
        "errorCode"
      ],
      "additionalProperties": false
    },
    "Agent": {
      "type": "object",
      "title": "Agent",
      "description": "{{term(agent)}}. An {{term(agent)}} **MUST** perform the following tasks: * Collect data from manufacturing equipment. * Generate {{termplural(response document)}}. * Provide a REST interface using {{term(HTTP)}}. In addition to {{term(XML)}} and {{term(HTTP)}}, An {{term(agent)}} **MAY** provide additional protocols and representations. Some representations **MAY** have companion specifications.",
      "properties": {
        "mtconnectVersion": {
          "type": "string"
        },
        "instanceId": {
          "type": "string",
          "description": "identifier for an {{term(instance)}} of the {{term(agent)}}. {{property(instanceId)}} **MUST** be changed to a different unique number each time the {{term(buffer)}} is cleared and a new set of data begins to be collected."
        },
        "sequenceNumber": {
          "type": "string",
          "description": "{{term(sequence number)}}."
        },
        "bufferSize": {
          "type": "string",
          "description": "maximum number of {{termplural(Observation)}} that **MAY** be retained in the {{term(agent)}} that published the {{term(response document)}} at any point in time."
        },
        "maxAssets": {
          "type": "string",
          "description": "maximum number of {{termplural(Asset)}} that **MAY** be retained in the {{term(agent)}} that published the {{term(response document)}} at any point in time."
        },
        "assetCount": {
          "type": "string",
          "description": "current number of {{termplural(Asset)}} that are currently stored in the {{term(agent)}} as of the {{property(creationTime)}} that the {{term(agent)}} published the {{term(response document)}}."
        }
      },
      "required": [
        "assetCount",
        "bufferSize",
        "instanceId",
        "maxAssets",
        "sequenceNumber"
      ],
      "additionalProperties": false
    },
    "SampleTimeSeries": {
      "type": "object",
      "title": "SampleTimeSeries",
      "description": "{{block(Observation)}} that is continuously changing or analog data value.",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AccelerationTimeSeries": {
      "type": "object",
      "title": "AccelerationTimeSeries",
      "description": "{{def(SampleEnum:ACCELERATION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ACTUAL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ACCELERATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AccumulatedTimeTimeSeries": {
      "type": "object",
      "title": "AccumulatedTimeTimeSeries",
      "description": "{{def(SampleEnum:ACCUMULATED_TIME)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ACCUMULATED_TIME"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AmperageTimeSeries": {
      "type": "object",
      "title": "AmperageTimeSeries",
      "description": "{{def(SampleEnum:AMPERAGE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "AMPERAGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AngleTimeSeries": {
      "type": "object",
      "title": "AngleTimeSeries",
      "description": "{{def(SampleEnum:ANGLE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ANGLE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AngularAccelerationTimeSeries": {
      "type": "object",
      "title": "AngularAccelerationTimeSeries",
      "description": "{{def(SampleEnum:ANGULAR_ACCELERATION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ACTUAL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ANGULAR_ACCELERATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AngularVelocityTimeSeries": {
      "type": "object",
      "title": "AngularVelocityTimeSeries",
      "description": "{{def(SampleEnum:ANGULAR_VELOCITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ANGULAR_VELOCITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AxisFeedrateTimeSeries": {
      "type": "object",
      "title": "AxisFeedrateTimeSeries",
      "description": "{{def(SampleEnum:AXIS_FEEDRATE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "AXIS_FEEDRATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "CapacityFluidTimeSeries": {
      "type": "object",
      "title": "CapacityFluidTimeSeries",
      "description": "{{def(SampleEnum:CAPACITY_FLUID)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CAPACITY_FLUID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLILITER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "CapacitySpatialTimeSeries": {
      "type": "object",
      "title": "CapacitySpatialTimeSeries",
      "description": "{{def(SampleEnum:CAPACITY_SPATIAL)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CAPACITY_SPATIAL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ConcentrationTimeSeries": {
      "type": "object",
      "title": "ConcentrationTimeSeries",
      "description": "{{def(SampleEnum:CONCENTRATION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CONCENTRATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ConductivityTimeSeries": {
      "type": "object",
      "title": "ConductivityTimeSeries",
      "description": "{{def(SampleEnum:CONDUCTIVITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CONDUCTIVITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "SIEMENS/METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "CuttingSpeedTimeSeries": {
      "type": "object",
      "title": "CuttingSpeedTimeSeries",
      "description": "{{def(SampleEnum:CUTTING_SPEED)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "CUTTING_SPEED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DensityTimeSeries": {
      "type": "object",
      "title": "DensityTimeSeries",
      "description": "{{def(SampleEnum:DENSITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DENSITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIGRAM/CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DepositionAccelerationVolumetricTimeSeries": {
      "type": "object",
      "title": "DepositionAccelerationVolumetricTimeSeries",
      "description": "{{def(SampleEnum:DEPOSITION_ACCELERATION_VOLUMETRIC)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_ACCELERATION_VOLUMETRIC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DepositionDensityTimeSeries": {
      "type": "object",
      "title": "DepositionDensityTimeSeries",
      "description": "{{def(SampleEnum:DEPOSITION_DENSITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_DENSITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIGRAM/CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DepositionMassTimeSeries": {
      "type": "object",
      "title": "DepositionMassTimeSeries",
      "description": "{{def(SampleEnum:DEPOSITION_MASS)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_MASS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIGRAM"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DepositionRateVolumetricTimeSeries": {
      "type": "object",
      "title": "DepositionRateVolumetricTimeSeries",
      "description": "{{def(SampleEnum:DEPOSITION_RATE_VOLUMETRIC)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_RATE_VOLUMETRIC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DepositionVolumeTimeSeries": {
      "type": "object",
      "title": "DepositionVolumeTimeSeries",
      "description": "{{def(SampleEnum:DEPOSITION_VOLUME)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DEPOSITION_VOLUME"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DisplacementTimeSeries": {
      "type": "object",
      "title": "DisplacementTimeSeries",
      "description": "{{def(SampleEnum:DISPLACEMENT)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DISPLACEMENT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ElectricalEnergyTimeSeries": {
      "type": "object",
      "title": "ElectricalEnergyTimeSeries",
      "description": "{{def(SampleEnum:ELECTRICAL_ENERGY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ELECTRICAL_ENERGY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "WATT_SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "EquipmentTimerTimeSeries": {
      "type": "object",
      "title": "EquipmentTimerTimeSeries",
      "description": "{{def(SampleEnum:EQUIPMENT_TIMER)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "EQUIPMENT_TIMER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "FillLevelTimeSeries": {
      "type": "object",
      "title": "FillLevelTimeSeries",
      "description": "{{def(SampleEnum:FILL_LEVEL)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "FILL_LEVEL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "FlowTimeSeries": {
      "type": "object",
      "title": "FlowTimeSeries",
      "description": "{{def(SampleEnum:FLOW)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "FLOW"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "LITER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "FrequencyTimeSeries": {
      "type": "object",
      "title": "FrequencyTimeSeries",
      "description": "{{def(SampleEnum:FREQUENCY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "FREQUENCY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "HERTZ"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "GlobalPositionTimeSeries": {
      "type": "object",
      "title": "GlobalPositionTimeSeries",
      "description": "{{def(SampleEnum:GLOBAL_POSITION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "GLOBAL_POSITION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "LengthTimeSeries": {
      "type": "object",
      "title": "LengthTimeSeries",
      "description": "{{def(SampleEnum:LENGTH)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "LENGTH"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "LevelTimeSeries": {
      "type": "object",
      "title": "LevelTimeSeries",
      "description": "{{def(SampleEnum:LEVEL)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "LEVEL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "LinearForceTimeSeries": {
      "type": "object",
      "title": "LinearForceTimeSeries",
      "description": "{{def(SampleEnum:LINEAR_FORCE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "LINEAR_FORCE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "NEWTON"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "LoadTimeSeries": {
      "type": "object",
      "title": "LoadTimeSeries",
      "description": "{{def(SampleEnum:LOAD)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "LOAD"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "MassTimeSeries": {
      "type": "object",
      "title": "MassTimeSeries",
      "description": "{{def(SampleEnum:MASS)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "MASS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "KILOGRAM"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PathFeedrateTimeSeries": {
      "type": "object",
      "title": "PathFeedrateTimeSeries",
      "description": "{{def(SampleEnum:PATH_FEEDRATE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PATH_FEEDRATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "PathFeedratePerRevolutionTimeSeries": {
      "type": "object",
      "title": "PathFeedratePerRevolutionTimeSeries",
      "description": "{{def(SampleEnum:PATH_FEEDRATE_PER_REVOLUTION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PATH_FEEDRATE_PER_REVOLUTION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/REVOLUTION"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "PHTimeSeries": {
      "type": "object",
      "title": "PHTimeSeries",
      "description": "{{def(SampleEnum:PH)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PH"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PH"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PositionTimeSeries": {
      "type": "object",
      "title": "PositionTimeSeries",
      "description": "{{def(SampleEnum:POSITION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "POSITION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "PowerFactorTimeSeries": {
      "type": "object",
      "title": "PowerFactorTimeSeries",
      "description": "{{def(SampleEnum:POWER_FACTOR)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "POWER_FACTOR"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ProcessTimerTimeSeries": {
      "type": "object",
      "title": "ProcessTimerTimeSeries",
      "description": "{{def(SampleEnum:PROCESS_TIMER)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PROCESS_TIMER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "ResistanceTimeSeries": {
      "type": "object",
      "title": "ResistanceTimeSeries",
      "description": "{{def(SampleEnum:RESISTANCE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "RESISTANCE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "OHM"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "RotaryVelocityTimeSeries": {
      "type": "object",
      "title": "RotaryVelocityTimeSeries",
      "description": "{{def(SampleEnum:ROTARY_VELOCITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ROTARY_VELOCITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "REVOLUTION/MINUTE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "SoundLevelTimeSeries": {
      "type": "object",
      "title": "SoundLevelTimeSeries",
      "description": "{{def(SampleEnum:SOUND_LEVEL)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "A_SCALE"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "SOUND_LEVEL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DECIBEL"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "SpindleSpeedTimeSeries": {
      "type": "object",
      "title": "SpindleSpeedTimeSeries",
      "description": "{{def(SampleEnum:SPINDLE_SPEED)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "SPINDLE_SPEED"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "REVOLUTION/MINUTE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "StrainTimeSeries": {
      "type": "object",
      "title": "StrainTimeSeries",
      "description": "{{def(SampleEnum:STRAIN)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "STRAIN"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "TemperatureTimeSeries": {
      "type": "object",
      "title": "TemperatureTimeSeries",
      "description": "{{def(SampleEnum:TEMPERATURE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "TEMPERATURE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CELSIUS"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "TensionTimeSeries": {
      "type": "object",
      "title": "TensionTimeSeries",
      "description": "{{def(SampleEnum:TENSION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "TENSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "NEWTON"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "TiltTimeSeries": {
      "type": "object",
      "title": "TiltTimeSeries",
      "description": "{{def(SampleEnum:TILT)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "TILT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MICRO_RADIAN"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "TorqueTimeSeries": {
      "type": "object",
      "title": "TorqueTimeSeries",
      "description": "{{def(SampleEnum:TORQUE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "TORQUE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "NEWTON_METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VelocityTimeSeries": {
      "type": "object",
      "title": "VelocityTimeSeries",
      "description": "{{def(SampleEnum:VELOCITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VELOCITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ViscosityTimeSeries": {
      "type": "object",
      "title": "ViscosityTimeSeries",
      "description": "{{def(SampleEnum:VISCOSITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VISCOSITY"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PASCAL_SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VoltageTimeSeries": {
      "type": "object",
      "title": "VoltageTimeSeries",
      "description": "{{def(SampleEnum:VOLTAGE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLTAGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "VoltAmpereTimeSeries": {
      "type": "object",
      "title": "VoltAmpereTimeSeries",
      "description": "{{def(SampleEnum:VOLT_AMPERE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLT_AMPERE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT_AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VoltAmpereReactiveTimeSeries": {
      "type": "object",
      "title": "VoltAmpereReactiveTimeSeries",
      "description": "{{def(SampleEnum:VOLT_AMPERE_REACTIVE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLT_AMPERE_REACTIVE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT_AMPERE_REACTIVE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VolumeFluidTimeSeries": {
      "type": "object",
      "title": "VolumeFluidTimeSeries",
      "description": "{{def(SampleEnum:VOLUME_FLUID)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLUME_FLUID"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLILITER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "VolumeSpatialTimeSeries": {
      "type": "object",
      "title": "VolumeSpatialTimeSeries",
      "description": "{{def(SampleEnum:VOLUME_SPATIAL)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLUME_SPATIAL"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "WattageTimeSeries": {
      "type": "object",
      "title": "WattageTimeSeries",
      "description": "{{def(SampleEnum:WATTAGE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "WATTAGE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "WATT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AmperageDCTimeSeries": {
      "type": "object",
      "title": "AmperageDCTimeSeries",
      "description": "{{def(SampleEnum:AMPERAGE_DC)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "AMPERAGE_DC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "AmperageACTimeSeries": {
      "type": "object",
      "title": "AmperageACTimeSeries",
      "description": "{{def(SampleEnum:AMPERAGE_AC)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "AMPERAGE_AC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "VoltageACTimeSeries": {
      "type": "object",
      "title": "VoltageACTimeSeries",
      "description": "{{def(SampleEnum:VOLTAGE_AC)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLTAGE_AC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "VoltageDCTimeSeries": {
      "type": "object",
      "title": "VoltageDCTimeSeries",
      "description": "{{def(SampleEnum:VOLTAGE_DC)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "VOLTAGE_DC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "VOLT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "subType",
        "timestamp",
        "value"
      ]
    },
    "XDimensionTimeSeries": {
      "type": "object",
      "title": "XDimensionTimeSeries",
      "description": "{{def(SampleEnum:X_DIMENSION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "X_DIMENSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "YDimensionTimeSeries": {
      "type": "object",
      "title": "YDimensionTimeSeries",
      "description": "{{def(SampleEnum:Y_DIMENSION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "Y_DIMENSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ZDimensionTimeSeries": {
      "type": "object",
      "title": "ZDimensionTimeSeries",
      "description": "{{def(SampleEnum:Z_DIMENSION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "Z_DIMENSION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DiameterTimeSeries": {
      "type": "object",
      "title": "DiameterTimeSeries",
      "description": "{{def(SampleEnum:DIAMETER)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DIAMETER"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "HumidityRelativeTimeSeries": {
      "type": "object",
      "title": "HumidityRelativeTimeSeries",
      "description": "{{def(SampleEnum:HUMIDITY_RELATIVE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "HUMIDITY_RELATIVE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "HumidityAbsoluteTimeSeries": {
      "type": "object",
      "title": "HumidityAbsoluteTimeSeries",
      "description": "{{def(SampleEnum:HUMIDITY_ABSOLUTE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "HUMIDITY_ABSOLUTE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "GRAM/CUBIC_METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "HumiditySpecificTimeSeries": {
      "type": "object",
      "title": "HumiditySpecificTimeSeries",
      "description": "{{def(SampleEnum:HUMIDITY_SPECIFIC)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "HUMIDITY_SPECIFIC"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "PressurizationRateTimeSeries": {
      "type": "object",
      "title": "PressurizationRateTimeSeries",
      "description": "{{def(SampleEnum:PRESSURIZATION_RATE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PRESSURIZATION_RATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PASCAL/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "DecelerationTimeSeries": {
      "type": "object",
      "title": "DecelerationTimeSeries",
      "description": "{{def(SampleEnum:DECELERATION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ACTUAL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "DECELERATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "MILLIMETER/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "AssetUpdateRateTimeSeries": {
      "type": "object",
      "title": "AssetUpdateRateTimeSeries",
      "description": "{{def(SampleEnum:ASSET_UPDATE_RATE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ASSET_UPDATE_RATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "COUNT/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AngularDecelerationTimeSeries": {
      "type": "object",
      "title": "AngularDecelerationTimeSeries",
      "description": "{{def(SampleEnum:ANGULAR_DECELERATION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "default": "ACTUAL"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "ANGULAR_DECELERATION"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "DEGREE/SECOND\\^2"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ]
    },
    "ObservationUpdateRateTimeSeries": {
      "type": "object",
      "title": "ObservationUpdateRateTimeSeries",
      "description": "{{def(SampleEnum:OBSERVATION_UPDATE_RATE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "OBSERVATION_UPDATE_RATE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "COUNT/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PressureAbsoluteTimeSeries": {
      "type": "object",
      "title": "PressureAbsoluteTimeSeries",
      "description": "The force per unit area measured relative to a vacuum.",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "PRESSURE_ABSOLUTE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PASCAL"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "OpennessTimeSeries": {
      "type": "object",
      "title": "OpennessTimeSeries",
      "description": "{{def(SampleEnum:OPENNESS)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/SampleEnum",
          "default": "OPENNESS"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ClockTimeTimeSeries": {
      "type": "object",
      "title": "ClockTimeTimeSeries",
      "description": "{{def(EventEnum:CLOCK_TIME)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "CLOCK_TIME"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum"
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(duration)}} **MUST** be provided when the {{property(statistic)}} attribute of the {{block(DataItem)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/definitions/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(statistic)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VariableDataSet": {
      "type": "object",
      "title": "VariableDataSet",
      "description": "{{def(EventEnum:VARIABLE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "VARIABLE"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WorkOffsetTable": {
      "type": "object",
      "title": "WorkOffsetTable",
      "description": "{{def(EventEnum:WORK_OFFSET)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "WORK_OFFSET"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AssetCountDataSet": {
      "type": "object",
      "title": "AssetCountDataSet",
      "description": "{{def(EventEnum:ASSET_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(dataItemId)}} **MUST** match the {{property(id)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(name)}} **MUST** match the {{property(name)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/definitions/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(subType)}} **MUST** match the {{property(subType)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/definitions/EventEnum",
          "default": "ASSET_COUNT"
        },
        "units": {
          "$ref": "#/definitions/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(units)}} **MUST** match the {{property(units)}} attribute of the {{block(DataItem)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "default": "true"
        },
        "resetTriggered": {
          "$ref": "#/definitions/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. {{property(resetTriggered)}} **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    }
  }
}