{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.mtconnect.org/schemas/MTConnectStreams_2.5.schema.json",
  "title": "MTConnectStreams",
  "description": "MTConnect MTConnectStreams response, v2 JSON, from SysML model v2.5.",
  "type": "object",
  "required": [
    "MTConnectStreams"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "MTConnectStreams": {
      "$ref": "#/$defs/MTConnectStreams"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "DeviceStream": {
      "type": "object",
      "title": "DeviceStream",
      "description": "{{termplural(organize)}} data reported from a {{block(Device)}}.",
      "properties": {
        "name": {
          "type": "string",
          "description": "name of the {{block(Device)}}. The value reported for {{property(DeviceStream::name)}} **MUST** be the same as the value defined for the {{property(Device::name)}} in the {{term(MTConnectDevices Response Document)}}."
        },
        "uuid": {
          "type": "string",
          "description": "uuid of the {{block(Device)}}. The value reported for {{property(DeviceStream::uuid)}} **MUST** be the same as the value defined for the {{property(Device::uuid)}} of the same {{block(Device)}} in the {{term(MTConnectDevices Response Document)}}."
        },
        "ComponentStream": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ComponentStream"
          }
        }
      },
      "required": [
        "name",
        "uuid"
      ],
      "additionalProperties": false
    },
    "ComponentStream": {
      "type": "object",
      "title": "ComponentStream",
      "description": "organizes the data associated with each {{block(Component)}} entity defined for a {{block(Device)}} in the associated {{term(MTConnectDevices Response Document)}}.",
      "properties": {
        "component": {
          "type": "string",
          "description": "identifies the {{block(Component)}} type associated with the {{block(ComponentStream)}}. Examples of {{property(ComponentStream::component)}} are {{block(Device)}}, {{block(Controller)}}, {{block(Linear)}} and {{block(Loader)}}."
        },
        "componentId": {
          "type": "string",
          "description": "identifier of the {{block(Component)}} as defined by the {{property(Component::id)}} in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(Component)}} associated with the {{block(ComponentStream)}}."
        },
        "nativeName": {
          "type": "string",
          "description": "common name of the {{block(Component)}} associated with the {{block(ComponentStream)}}."
        },
        "uuid": {
          "type": "string",
          "description": "uuid of the {{block(Component)}} associated with the {{block(ComponentStream)}}."
        },
        "Events": {
          "type": "object",
          "properties": {
            "Event": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Event"
              }
            },
            "ActiveAxes": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ActiveAxes"
              }
            },
            "ActuatorState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ActuatorState"
              }
            },
            "Alarm": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Alarm"
              }
            },
            "AssetChanged": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AssetChanged"
              }
            },
            "AssetRemoved": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AssetRemoved"
              }
            },
            "Availability": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Availability"
              }
            },
            "AxisCoupling": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AxisCoupling"
              }
            },
            "AxisFeedrateOverride": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AxisFeedrateOverride"
              }
            },
            "AxisInterlock": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AxisInterlock"
              }
            },
            "AxisState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AxisState"
              }
            },
            "Block": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Block"
              }
            },
            "BlockCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/BlockCount"
              }
            },
            "ChuckInterlock": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ChuckInterlock"
              }
            },
            "ChuckState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ChuckState"
              }
            },
            "Code": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Code"
              }
            },
            "CompositionState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CompositionState"
              }
            },
            "ControllerMode": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ControllerMode"
              }
            },
            "ControllerModeOverride": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ControllerModeOverride"
              }
            },
            "CoupledAxes": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CoupledAxes"
              }
            },
            "DateCode": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DateCode"
              }
            },
            "DeviceUuid": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DeviceUuid"
              }
            },
            "Direction": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Direction"
              }
            },
            "DoorState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DoorState"
              }
            },
            "EmergencyStop": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/EmergencyStop"
              }
            },
            "EndOfBar": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/EndOfBar"
              }
            },
            "EquipmentMode": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/EquipmentMode"
              }
            },
            "Execution": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Execution"
              }
            },
            "FunctionalMode": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FunctionalMode"
              }
            },
            "Hardness": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Hardness"
              }
            },
            "Line": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Line"
              }
            },
            "LineLabel": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LineLabel"
              }
            },
            "LineNumber": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LineNumber"
              }
            },
            "Material": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Material"
              }
            },
            "MaterialLayer": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MaterialLayer"
              }
            },
            "Message": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Message"
              }
            },
            "OperatorId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/OperatorId"
              }
            },
            "PalletId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PalletId"
              }
            },
            "PartCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartCount"
              }
            },
            "PartDetect": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartDetect"
              }
            },
            "PartId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartId"
              }
            },
            "PartNumber": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartNumber"
              }
            },
            "PathFeedrateOverride": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PathFeedrateOverride"
              }
            },
            "PathMode": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PathMode"
              }
            },
            "PowerState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PowerState"
              }
            },
            "PowerStatus": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PowerStatus"
              }
            },
            "ProcessTime": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProcessTime"
              }
            },
            "Program": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Program"
              }
            },
            "ProgramComment": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProgramComment"
              }
            },
            "ProgramEdit": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProgramEdit"
              }
            },
            "ProgramEditName": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProgramEditName"
              }
            },
            "ProgramHeader": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProgramHeader"
              }
            },
            "ProgramLocation": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProgramLocation"
              }
            },
            "ProgramLocationType": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProgramLocationType"
              }
            },
            "ProgramNestLevel": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProgramNestLevel"
              }
            },
            "RotaryMode": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/RotaryMode"
              }
            },
            "RotaryVelocityOverride": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/RotaryVelocityOverride"
              }
            },
            "SerialNumber": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SerialNumber"
              }
            },
            "SpindleInterlock": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpindleInterlock"
              }
            },
            "ToolAssetId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ToolAssetId"
              }
            },
            "ToolGroup": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ToolGroup"
              }
            },
            "ToolId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ToolId"
              }
            },
            "ToolNumber": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ToolNumber"
              }
            },
            "ToolOffset": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ToolOffset"
              }
            },
            "User": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/User"
              }
            },
            "Variable": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Variable"
              }
            },
            "WaitState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/WaitState"
              }
            },
            "Wire": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Wire"
              }
            },
            "WorkholdingId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/WorkholdingId"
              }
            },
            "WorkOffset": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/WorkOffset"
              }
            },
            "OperatingSystem": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/OperatingSystem"
              }
            },
            "Firmware": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Firmware"
              }
            },
            "Application": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Application"
              }
            },
            "Library": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Library"
              }
            },
            "Hardware": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Hardware"
              }
            },
            "Network": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Network"
              }
            },
            "Rotation": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Rotation"
              }
            },
            "Translation": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Translation"
              }
            },
            "ProcessKindId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProcessKindId"
              }
            },
            "PartStatus": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartStatus"
              }
            },
            "AlarmLimit": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AlarmLimit"
              }
            },
            "ProcessAggregateId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProcessAggregateId"
              }
            },
            "PartKindId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartKindId"
              }
            },
            "AdapterURI": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AdapterURI"
              }
            },
            "DeviceRemoved": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DeviceRemoved"
              }
            },
            "DeviceChanged": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DeviceChanged"
              }
            },
            "SpecificationLimit": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpecificationLimit"
              }
            },
            "ConnectionStatus": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ConnectionStatus"
              }
            },
            "AdapterSoftwareVersion": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AdapterSoftwareVersion"
              }
            },
            "SensorAttachment": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SensorAttachment"
              }
            },
            "ControlLimit": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ControlLimit"
              }
            },
            "DeviceAdded": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DeviceAdded"
              }
            },
            "MTConnectVersion": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MTConnectVersion"
              }
            },
            "ProcessOccurrenceId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProcessOccurrenceId"
              }
            },
            "PartGroupId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartGroupId"
              }
            },
            "PartUniqueId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartUniqueId"
              }
            },
            "ActivationCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ActivationCount"
              }
            },
            "DeactivationCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DeactivationCount"
              }
            },
            "TransferCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/TransferCount"
              }
            },
            "LoadCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LoadCount"
              }
            },
            "PartProcessingState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartProcessingState"
              }
            },
            "ProcessState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProcessState"
              }
            },
            "ValveState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ValveState"
              }
            },
            "LockState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LockState"
              }
            },
            "UnloadCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/UnloadCount"
              }
            },
            "CycleCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CycleCount"
              }
            },
            "OperatingMode": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/OperatingMode"
              }
            },
            "AssetCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AssetCount"
              }
            },
            "MaintenanceList": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MaintenanceList"
              }
            },
            "FixtureId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FixtureId"
              }
            },
            "PartCountType": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartCountType"
              }
            },
            "ClockTime": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ClockTime"
              }
            },
            "NetworkPort": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/NetworkPort"
              }
            },
            "HostName": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/HostName"
              }
            },
            "LeakDetect": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LeakDetect"
              }
            },
            "BatteryState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/BatteryState"
              }
            },
            "FeaturePersisitentId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FeaturePersisitentId"
              }
            },
            "SensorState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SensorState"
              }
            },
            "ComponentData": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ComponentData"
              }
            },
            "WorkOffsets": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/WorkOffsets"
              }
            },
            "ToolOffsets": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ToolOffsets"
              }
            },
            "FeatureMeasurement": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FeatureMeasurement"
              }
            },
            "CharacteristicPersistentId": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CharacteristicPersistentId"
              }
            },
            "MeasurementType": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MeasurementType"
              }
            },
            "MeasurementValue": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MeasurementValue"
              }
            },
            "MeasurementUnits": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MeasurementUnits"
              }
            },
            "CharacteristicStatus": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CharacteristicStatus"
              }
            },
            "UncertaintyType": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/UncertaintyType"
              }
            },
            "Uncertainty": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Uncertainty"
              }
            },
            "AlarmLimits": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AlarmLimits"
              }
            },
            "ControlLimits": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ControlLimits"
              }
            },
            "SpecificationLimits": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpecificationLimits"
              }
            },
            "ToolCuttingItem": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ToolCuttingItem"
              }
            },
            "LocationAddress": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LocationAddress"
              }
            },
            "ActivePowerSource": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ActivePowerSource"
              }
            },
            "LocationNarrative": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LocationNarrative"
              }
            },
            "Thickness": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Thickness"
              }
            },
            "LocationSpatialGeographic": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LocationSpatialGeographic"
              }
            },
            "PartIndex": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartIndex"
              }
            },
            "MaterialFeed": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MaterialFeed"
              }
            },
            "MaterialChange": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MaterialChange"
              }
            },
            "MaterialRetract": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MaterialRetract"
              }
            },
            "MaterialLoad": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MaterialLoad"
              }
            },
            "MaterialUnload": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MaterialUnload"
              }
            },
            "OpenChuck": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/OpenChuck"
              }
            },
            "OpenDoor": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/OpenDoor"
              }
            },
            "PartChange": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PartChange"
              }
            },
            "CloseDoor": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CloseDoor"
              }
            },
            "CloseChuck": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CloseChuck"
              }
            },
            "InterfaceState": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/InterfaceState"
              }
            },
            "TranslationDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/TranslationDataSet"
              }
            },
            "RotationDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/RotationDataSet"
              }
            },
            "VariableDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VariableDataSet"
              }
            },
            "WorkOffsetTable": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/WorkOffsetTable"
              }
            },
            "AlarmLimitDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AlarmLimitDataSet"
              }
            },
            "SpecificationLimitDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpecificationLimitDataSet"
              }
            },
            "SensorAttachmentDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SensorAttachmentDataSet"
              }
            },
            "ControlLimitDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ControlLimitDataSet"
              }
            },
            "AssetCountDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AssetCountDataSet"
              }
            },
            "ComponentDataTable": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ComponentDataTable"
              }
            },
            "WorkOffsetsTable": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/WorkOffsetsTable"
              }
            },
            "ToolOffsetsTable": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ToolOffsetsTable"
              }
            },
            "FeatureMeasurementTable": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FeatureMeasurementTable"
              }
            },
            "AlarmLimitsDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AlarmLimitsDataSet"
              }
            },
            "ControlLimitsDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ControlLimitsDataSet"
              }
            },
            "SpecificationLimitsDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpecificationLimitsDataSet"
              }
            },
            "LocationAddressDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LocationAddressDataSet"
              }
            },
            "LocationSpatialGeographicDataSet": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LocationSpatialGeographicDataSet"
              }
            }
          },
          "patternProperties": {
            "^[a-ln-zA-LN-Z]:[A-Za-z_][A-Za-z0-9_]*$": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Event"
              }
            }
          },
          "additionalProperties": false
        },
        "Samples": {
          "type": "object",
          "properties": {
            "Sample": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Sample"
              }
            },
            "SampleTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SampleTimeSeries"
              }
            },
            "Acceleration": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Acceleration"
              }
            },
            "AccelerationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AccelerationTimeSeries"
              }
            },
            "AccumulatedTime": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AccumulatedTime"
              }
            },
            "AccumulatedTimeTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AccumulatedTimeTimeSeries"
              }
            },
            "Amperage": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Amperage"
              }
            },
            "AmperageTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AmperageTimeSeries"
              }
            },
            "Angle": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Angle"
              }
            },
            "AngleTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AngleTimeSeries"
              }
            },
            "AngularAcceleration": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AngularAcceleration"
              }
            },
            "AngularAccelerationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AngularAccelerationTimeSeries"
              }
            },
            "AngularVelocity": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AngularVelocity"
              }
            },
            "AngularVelocityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AngularVelocityTimeSeries"
              }
            },
            "AxisFeedrate": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AxisFeedrate"
              }
            },
            "AxisFeedrateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AxisFeedrateTimeSeries"
              }
            },
            "CapacityFluid": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CapacityFluid"
              }
            },
            "CapacityFluidTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CapacityFluidTimeSeries"
              }
            },
            "CapacitySpatial": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CapacitySpatial"
              }
            },
            "CapacitySpatialTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CapacitySpatialTimeSeries"
              }
            },
            "Concentration": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Concentration"
              }
            },
            "ConcentrationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ConcentrationTimeSeries"
              }
            },
            "Conductivity": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Conductivity"
              }
            },
            "ConductivityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ConductivityTimeSeries"
              }
            },
            "CuttingSpeed": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CuttingSpeed"
              }
            },
            "CuttingSpeedTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/CuttingSpeedTimeSeries"
              }
            },
            "Density": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Density"
              }
            },
            "DensityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DensityTimeSeries"
              }
            },
            "DepositionAccelerationVolumetric": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionAccelerationVolumetric"
              }
            },
            "DepositionAccelerationVolumetricTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionAccelerationVolumetricTimeSeries"
              }
            },
            "DepositionDensity": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionDensity"
              }
            },
            "DepositionDensityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionDensityTimeSeries"
              }
            },
            "DepositionMass": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionMass"
              }
            },
            "DepositionMassTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionMassTimeSeries"
              }
            },
            "DepositionRateVolumetric": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionRateVolumetric"
              }
            },
            "DepositionRateVolumetricTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionRateVolumetricTimeSeries"
              }
            },
            "DepositionVolume": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionVolume"
              }
            },
            "DepositionVolumeTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DepositionVolumeTimeSeries"
              }
            },
            "Displacement": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Displacement"
              }
            },
            "DisplacementTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DisplacementTimeSeries"
              }
            },
            "ElectricalEnergy": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ElectricalEnergy"
              }
            },
            "ElectricalEnergyTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ElectricalEnergyTimeSeries"
              }
            },
            "EquipmentTimer": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/EquipmentTimer"
              }
            },
            "EquipmentTimerTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/EquipmentTimerTimeSeries"
              }
            },
            "FillLevel": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FillLevel"
              }
            },
            "FillLevelTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FillLevelTimeSeries"
              }
            },
            "Flow": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Flow"
              }
            },
            "FlowTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FlowTimeSeries"
              }
            },
            "Frequency": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Frequency"
              }
            },
            "FrequencyTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FrequencyTimeSeries"
              }
            },
            "GlobalPosition": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/GlobalPosition"
              }
            },
            "GlobalPositionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/GlobalPositionTimeSeries"
              }
            },
            "Length": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Length"
              }
            },
            "LengthTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LengthTimeSeries"
              }
            },
            "Level": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Level"
              }
            },
            "LevelTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LevelTimeSeries"
              }
            },
            "LinearForce": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LinearForce"
              }
            },
            "LinearForceTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LinearForceTimeSeries"
              }
            },
            "Load": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Load"
              }
            },
            "LoadTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/LoadTimeSeries"
              }
            },
            "Mass": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Mass"
              }
            },
            "MassTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/MassTimeSeries"
              }
            },
            "PathFeedrate": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PathFeedrate"
              }
            },
            "PathFeedrateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PathFeedrateTimeSeries"
              }
            },
            "PathFeedratePerRevolution": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PathFeedratePerRevolution"
              }
            },
            "PathFeedratePerRevolutionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PathFeedratePerRevolutionTimeSeries"
              }
            },
            "PathPosition": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PathPosition"
              }
            },
            "PH": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PH"
              }
            },
            "PHTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PHTimeSeries"
              }
            },
            "Position": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Position"
              }
            },
            "PositionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PositionTimeSeries"
              }
            },
            "PowerFactor": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PowerFactor"
              }
            },
            "PowerFactorTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PowerFactorTimeSeries"
              }
            },
            "Pressure": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Pressure"
              }
            },
            "ProcessTimer": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProcessTimer"
              }
            },
            "ProcessTimerTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ProcessTimerTimeSeries"
              }
            },
            "Resistance": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Resistance"
              }
            },
            "ResistanceTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ResistanceTimeSeries"
              }
            },
            "RotaryVelocity": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/RotaryVelocity"
              }
            },
            "RotaryVelocityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/RotaryVelocityTimeSeries"
              }
            },
            "SoundLevel": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SoundLevel"
              }
            },
            "SoundLevelTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SoundLevelTimeSeries"
              }
            },
            "SpindleSpeed": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpindleSpeed"
              }
            },
            "SpindleSpeedTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SpindleSpeedTimeSeries"
              }
            },
            "Strain": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Strain"
              }
            },
            "StrainTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/StrainTimeSeries"
              }
            },
            "Temperature": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Temperature"
              }
            },
            "TemperatureTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/TemperatureTimeSeries"
              }
            },
            "Tension": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Tension"
              }
            },
            "TensionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/TensionTimeSeries"
              }
            },
            "Tilt": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Tilt"
              }
            },
            "TiltTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/TiltTimeSeries"
              }
            },
            "Torque": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Torque"
              }
            },
            "TorqueTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/TorqueTimeSeries"
              }
            },
            "Velocity": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Velocity"
              }
            },
            "VelocityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VelocityTimeSeries"
              }
            },
            "Viscosity": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Viscosity"
              }
            },
            "ViscosityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ViscosityTimeSeries"
              }
            },
            "Voltage": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Voltage"
              }
            },
            "VoltageTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VoltageTimeSeries"
              }
            },
            "VoltAmpere": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VoltAmpere"
              }
            },
            "VoltAmpereTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VoltAmpereTimeSeries"
              }
            },
            "VoltAmpereReactive": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VoltAmpereReactive"
              }
            },
            "VoltAmpereReactiveTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VoltAmpereReactiveTimeSeries"
              }
            },
            "VolumeFluid": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VolumeFluid"
              }
            },
            "VolumeFluidTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VolumeFluidTimeSeries"
              }
            },
            "VolumeSpatial": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VolumeSpatial"
              }
            },
            "VolumeSpatialTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VolumeSpatialTimeSeries"
              }
            },
            "Wattage": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Wattage"
              }
            },
            "WattageTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/WattageTimeSeries"
              }
            },
            "AmperageDC": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AmperageDC"
              }
            },
            "AmperageDCTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AmperageDCTimeSeries"
              }
            },
            "AmperageAC": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AmperageAC"
              }
            },
            "AmperageACTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AmperageACTimeSeries"
              }
            },
            "VoltageAC": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VoltageAC"
              }
            },
            "VoltageACTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VoltageACTimeSeries"
              }
            },
            "VoltageDC": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VoltageDC"
              }
            },
            "VoltageDCTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/VoltageDCTimeSeries"
              }
            },
            "XDimension": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/XDimension"
              }
            },
            "XDimensionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/XDimensionTimeSeries"
              }
            },
            "YDimension": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/YDimension"
              }
            },
            "YDimensionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/YDimensionTimeSeries"
              }
            },
            "ZDimension": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ZDimension"
              }
            },
            "ZDimensionTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ZDimensionTimeSeries"
              }
            },
            "Diameter": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Diameter"
              }
            },
            "DiameterTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DiameterTimeSeries"
              }
            },
            "Orientation": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Orientation"
              }
            },
            "HumidityRelative": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/HumidityRelative"
              }
            },
            "HumidityRelativeTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/HumidityRelativeTimeSeries"
              }
            },
            "HumidityAbsolute": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/HumidityAbsolute"
              }
            },
            "HumidityAbsoluteTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/HumidityAbsoluteTimeSeries"
              }
            },
            "HumiditySpecific": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/HumiditySpecific"
              }
            },
            "HumiditySpecificTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/HumiditySpecificTimeSeries"
              }
            },
            "PressurizationRate": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PressurizationRate"
              }
            },
            "PressurizationRateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PressurizationRateTimeSeries"
              }
            },
            "Deceleration": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Deceleration"
              }
            },
            "DecelerationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DecelerationTimeSeries"
              }
            },
            "AssetUpdateRate": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AssetUpdateRate"
              }
            },
            "AssetUpdateRateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AssetUpdateRateTimeSeries"
              }
            },
            "AngularDeceleration": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AngularDeceleration"
              }
            },
            "AngularDecelerationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/AngularDecelerationTimeSeries"
              }
            },
            "ObservationUpdateRate": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ObservationUpdateRate"
              }
            },
            "ObservationUpdateRateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ObservationUpdateRateTimeSeries"
              }
            },
            "PressureAbsolute": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PressureAbsolute"
              }
            },
            "PressureAbsoluteTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PressureAbsoluteTimeSeries"
              }
            },
            "Openness": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Openness"
              }
            },
            "OpennessTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/OpennessTimeSeries"
              }
            },
            "DewPoint": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DewPoint"
              }
            },
            "DewPointTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DewPointTimeSeries"
              }
            },
            "GravitationalForce": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/GravitationalForce"
              }
            },
            "GravitationalForceTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/GravitationalForceTimeSeries"
              }
            },
            "GravitationalAcceleration": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/GravitationalAcceleration"
              }
            },
            "GravitationalAccelerationTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/GravitationalAccelerationTimeSeries"
              }
            },
            "BatteryCapacity": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/BatteryCapacity"
              }
            },
            "BatteryCapacityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/BatteryCapacityTimeSeries"
              }
            },
            "DischargeRate": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DischargeRate"
              }
            },
            "DischargeRateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DischargeRateTimeSeries"
              }
            },
            "ChargeRate": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ChargeRate"
              }
            },
            "ChargeRateTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ChargeRateTimeSeries"
              }
            },
            "BatteryCharge": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/BatteryCharge"
              }
            },
            "BatteryChargeTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/BatteryChargeTimeSeries"
              }
            },
            "SettlingError": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SettlingError"
              }
            },
            "SettlingErrorTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SettlingErrorTimeSeries"
              }
            },
            "SettlingErrorLinear": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SettlingErrorLinear"
              }
            },
            "SettlingErrorLinearTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SettlingErrorLinearTimeSeries"
              }
            },
            "SettlingErrorAngular": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SettlingErrorAngular"
              }
            },
            "SettlingErrorAngularTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/SettlingErrorAngularTimeSeries"
              }
            },
            "FollowingError": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FollowingError"
              }
            },
            "FollowingErrorTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FollowingErrorTimeSeries"
              }
            },
            "FollowingErrorAngular": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FollowingErrorAngular"
              }
            },
            "FollowingErrorAngularTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FollowingErrorAngularTimeSeries"
              }
            },
            "FollowingErrorLinear": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FollowingErrorLinear"
              }
            },
            "FollowingErrorLinearTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FollowingErrorLinearTimeSeries"
              }
            },
            "DisplacementLinear": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DisplacementLinear"
              }
            },
            "DisplacementLinearTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DisplacementLinearTimeSeries"
              }
            },
            "DisplacementAngular": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DisplacementAngular"
              }
            },
            "DisplacementAngularTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DisplacementAngularTimeSeries"
              }
            },
            "PositionCartesian": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PositionCartesian"
              }
            },
            "Resistivity": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Resistivity"
              }
            },
            "ResistivityTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ResistivityTimeSeries"
              }
            },
            "FillHeight": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FillHeight"
              }
            },
            "FillHeightTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/FillHeightTimeSeries"
              }
            },
            "ParticleCount": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ParticleCount"
              }
            },
            "ParticleCountTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ParticleCountTimeSeries"
              }
            },
            "ParticleSize": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ParticleSize"
              }
            },
            "ParticleSizeTimeSeries": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/ParticleSizeTimeSeries"
              }
            }
          },
          "patternProperties": {
            "^[a-ln-zA-LN-Z]:[A-Za-z_][A-Za-z0-9_]*$": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Sample"
              }
            }
          },
          "additionalProperties": false
        },
        "Condition": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/Condition"
            }
          }
        }
      },
      "required": [
        "component",
        "componentId"
      ],
      "additionalProperties": false
    },
    "Observation": {
      "type": "object",
      "title": "Observation",
      "description": "abstract entity that provides telemetry data for a {{block(DataItem)}} at a point in time.",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/DataItemTypeEnum",
          "description": "type of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::type)}} **MUST** match the associated {{property(DataItem::type)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ]
    },
    "Sample": {
      "allOf": [
        {
          "$ref": "#/$defs/Observation"
        },
        {
          "type": "object",
          "title": "Sample",
          "description": "{{block(Observation)}} that is continuously changing or analog data value.",
          "properties": {
            "duration": {
              "type": "number",
              "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
            },
            "resetTriggered": {
              "$ref": "#/$defs/ResetTriggeredEnum",
              "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
            },
            "sampleRate": {
              "type": "number",
              "description": "rate at which successive samples of the value are recorded."
            },
            "statistic": {
              "$ref": "#/$defs/StatisticEnum",
              "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
            },
            "value": {},
            "units": {
              "$ref": "#/$defs/UnitEnum"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum"
            }
          },
          "required": [
            "value"
          ]
        }
      ]
    },
    "Event": {
      "allOf": [
        {
          "$ref": "#/$defs/Observation"
        },
        {
          "type": "object",
          "title": "Event",
          "description": "{{block(Observation)}} that is a discrete piece of information from a piece of equipment.",
          "properties": {
            "resetTriggered": {
              "$ref": "#/$defs/ResetTriggeredEnum",
              "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
            },
            "type": {
              "$ref": "#/$defs/EventEnum"
            }
          }
        }
      ]
    },
    "Condition": {
      "allOf": [
        {
          "$ref": "#/$defs/Observation"
        },
        {
          "type": "object",
          "title": "Condition",
          "description": "{{block(Observation)}} that provides the {{term(condition)}} of a piece of equipment or a {{term(Component)}}.",
          "properties": {
            "nativeCode": {
              "type": "string",
              "description": "native code is the proprietary identifier designating a specific alarm, fault or warning code provided by the piece of equipment."
            },
            "nativeSeverity": {
              "type": "string",
              "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
            },
            "qualifier": {
              "$ref": "#/$defs/QualifierEnum",
              "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(Condition::qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
            },
            "statistic": {
              "$ref": "#/$defs/StatisticEnum",
              "description": "{{property(Condition::statistic)}} provides additional information describing the meaning of the {{block(Condition)}} entity. {{property(Condition::statistic)}} **MUST** match the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
            },
            "xs:lang": {
              "type": "string",
              "description": "specifies the language of the {{property(Observation::result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
            },
            "state": {
              "$ref": "#/$defs/ConditionStateEnum",
              "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
            },
            "type": {
              "type": "string"
            },
            "conditionId": {
              "type": "string",
              "description": "identifier of an individual {{term(condition activation)}} provided by a piece of equipment. {{property(Condition::conditionId)}} **MUST** be unique for all concurrent {{termpl(condition activation)}}. {{property(Condition::conditionId)}} **MUST** be maintained for all state transitions related to the same {{term(condition activation)}}. Multiple {{property(Condition::conditionId)}}s **MAY** exist for the same {{property(nativeCode)}}. If {{property(Condition::conditionId)}} is not given, the value is the {{property(Condition::nativeCode)}}. If {{property(Condition::nativeCode)}} and {{property(Condition::conditionId)}} are not given, {{property(Condition::conditionId)}} **MUST** be generated."
            }
          },
          "required": [
            "type"
          ]
        }
      ]
    },
    "Representation": {
      "type": "object",
      "title": "Representation",
      "description": "specifies the format and structure of {{property(Observation::result)}}."
    },
    "Value": {
      "type": "object",
      "title": "Value",
      "description": "default {{block(Representation)}} type for all {{block(Observation)}} types where {{property(Observation::result)}} type is an MTConnect data type. See {{package(DataTypes)}}.",
      "additionalProperties": false
    },
    "TimeSeries": {
      "type": "object",
      "title": "TimeSeries",
      "description": "{{block(Representation)}} for an {{block(Observation)}} composed of a series of sampled data.",
      "properties": {
        "sampleCount": {
          "type": "integer",
          "description": "number of values given for the {{block(Observation)}}."
        },
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        }
      },
      "required": [
        "sampleCount",
        "value"
      ],
      "additionalProperties": false
    },
    "Discrete": {
      "type": "object",
      "title": "Discrete",
      "description": "**DEPRECATED** {{block(Representation)}} for an {{block(Observation)}} where each discrete occurrence of the data may have the same value as the previous occurrence of the data.",
      "properties": {
        "value": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "DataSet": {
      "allOf": [
        {
          "$ref": "#/$defs/Representation"
        },
        {
          "type": "object",
          "title": "DataSet",
          "description": "{{block(Representation)}} for an {{block(Observation)}} composed of value(s) represented as a set of {{termplural(key-value pair)}}.",
          "properties": {
            "count": {
              "type": "integer",
              "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
            }
          },
          "required": [
            "count"
          ]
        }
      ]
    },
    "Entry": {
      "type": "object",
      "title": "Entry",
      "description": "{{term(key-value pair)}} published as part of a {{block(DataSet)}}.",
      "properties": {
        "key": {
          "type": "string",
          "description": "unique identifier for each {{term(key-value pair)}}."
        },
        "removed": {
          "type": "boolean",
          "description": "removal indicator of a {{term(key-value pair)}}."
        },
        "value": {
          "type": "string",
          "description": "value of the {{block(Entry)}}."
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "Table": {
      "allOf": [
        {
          "$ref": "#/$defs/Representation"
        },
        {
          "type": "object",
          "title": "Table",
          "description": "{{block(Representation)}} for an {{block(Observation)}} composed of two-dimensional sets of {{termplural(key-value pair)}} where the {{block(Entry)}} represents rows containing sets of {{termplural(key-value pair)}} given by {{block(Cell)}} entities.",
          "properties": {
            "count": {
              "type": "integer",
              "description": "number of {{termplural(key-value pair)}} represented as {{block(Entry)}} entities."
            }
          },
          "required": [
            "count"
          ]
        }
      ]
    },
    "TableEntry": {
      "type": "object",
      "title": "TableEntry",
      "description": "{{term(key-value pair)}} published as part of a {{block(Table)}}. > Note: In the {{term(XML)}} representation, {{block(TableEntry)}} **MUST** appear as {{block(Entry)}}.",
      "properties": {
        "key": {
          "type": "string",
          "description": "unique identifier for each {{term(key-value pair)}}."
        },
        "removed": {
          "type": "boolean",
          "description": "removal indicator of a {{term(key-value pair)}}."
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "Cell": {
      "type": "object",
      "title": "Cell",
      "description": "{{term(key-value pair)}} published as part of a {{block(TableEntry)}}.",
      "properties": {
        "key": {
          "type": "string",
          "description": "unique identifier for each {{term(key-value pair)}}."
        },
        "value": {
          "type": "string",
          "description": "value of the {{block(Cell)}}."
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "Acceleration": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Acceleration",
          "description": "{{def(SampleEnum::ACCELERATION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ACCELERATION"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "AccumulatedTime": {
      "type": "object",
      "title": "AccumulatedTime",
      "description": "{{def(SampleEnum::ACCUMULATED_TIME)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "ACCUMULATED_TIME"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Amperage": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Amperage",
          "description": "{{def(SampleEnum::AMPERAGE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "AMPERAGE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "Angle": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Angle",
          "description": "{{def(SampleEnum::ANGLE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ANGLE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "AngularAcceleration": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AngularAcceleration",
          "description": "{{def(SampleEnum::ANGULAR_ACCELERATION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ANGULAR_ACCELERATION"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "AngularVelocity": {
      "type": "object",
      "title": "AngularVelocity",
      "description": "{{def(SampleEnum::ANGULAR_VELOCITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "ANGULAR_VELOCITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "DEGREE/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AxisFeedrate": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AxisFeedrate",
          "description": "{{def(SampleEnum::AXIS_FEEDRATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "AXIS_FEEDRATE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "CapacityFluid": {
      "type": "object",
      "title": "CapacityFluid",
      "description": "{{def(SampleEnum::CAPACITY_FLUID)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "CAPACITY_FLUID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLILITER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "CapacitySpatial": {
      "type": "object",
      "title": "CapacitySpatial",
      "description": "{{def(SampleEnum::CAPACITY_SPATIAL)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "CAPACITY_SPATIAL"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Concentration": {
      "type": "object",
      "title": "Concentration",
      "description": "{{def(SampleEnum::CONCENTRATION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "CONCENTRATION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Conductivity": {
      "type": "object",
      "title": "Conductivity",
      "description": "{{def(SampleEnum::CONDUCTIVITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "CONDUCTIVITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "SIEMENS/METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "CuttingSpeed": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "CuttingSpeed",
          "description": "{{def(SampleEnum::CUTTING_SPEED)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "CUTTING_SPEED"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "Density": {
      "type": "object",
      "title": "Density",
      "description": "{{def(SampleEnum::DENSITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DENSITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIGRAM/CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DepositionAccelerationVolumetric": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionAccelerationVolumetric",
          "description": "{{def(SampleEnum::DEPOSITION_ACCELERATION_VOLUMETRIC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "CUBIC_MILLIMETER/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_ACCELERATION_VOLUMETRIC"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "DepositionDensity": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionDensity",
          "description": "{{def(SampleEnum::DEPOSITION_DENSITY)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIGRAM/CUBIC_MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_DENSITY"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "DepositionMass": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionMass",
          "description": "{{def(SampleEnum::DEPOSITION_MASS)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIGRAM"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_MASS"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "DepositionRateVolumetric": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionRateVolumetric",
          "description": "{{def(SampleEnum::DEPOSITION_RATE_VOLUMETRIC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "CUBIC_MILLIMETER/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_RATE_VOLUMETRIC"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "DepositionVolume": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionVolume",
          "description": "{{def(SampleEnum::DEPOSITION_VOLUME)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "CUBIC_MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_VOLUME"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "Displacement": {
      "type": "object",
      "title": "Displacement",
      "description": "{{def(SampleEnum::DISPLACEMENT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DISPLACEMENT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ElectricalEnergy": {
      "type": "object",
      "title": "ElectricalEnergy",
      "description": "{{def(SampleEnum::ELECTRICAL_ENERGY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "ELECTRICAL_ENERGY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "WATT_SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "EquipmentTimer": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "EquipmentTimer",
          "description": "{{def(SampleEnum::EQUIPMENT_TIMER)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "EQUIPMENT_TIMER"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "FillLevel": {
      "type": "object",
      "title": "FillLevel",
      "description": "{{def(SampleEnum::FILL_LEVEL)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "FILL_LEVEL"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Flow": {
      "type": "object",
      "title": "Flow",
      "description": "{{def(SampleEnum::FLOW)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "FLOW"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "LITER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Frequency": {
      "type": "object",
      "title": "Frequency",
      "description": "{{def(SampleEnum::FREQUENCY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "FREQUENCY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "HERTZ"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "GlobalPosition": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "GlobalPosition",
          "description": "{{def(SampleEnum::GLOBAL_POSITION)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "GLOBAL_POSITION"
            },
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "Length": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Length",
          "description": "{{def(SampleEnum::LENGTH)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "LENGTH"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "Level": {
      "type": "object",
      "title": "Level",
      "description": "{{def(SampleEnum::LEVEL)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "LEVEL"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "LinearForce": {
      "type": "object",
      "title": "LinearForce",
      "description": "{{def(SampleEnum::LINEAR_FORCE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "LINEAR_FORCE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "NEWTON"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Load": {
      "type": "object",
      "title": "Load",
      "description": "{{def(SampleEnum::LOAD)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "LOAD"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Mass": {
      "type": "object",
      "title": "Mass",
      "description": "{{def(SampleEnum::MASS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "MASS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "KILOGRAM"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PathFeedrate": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "PathFeedrate",
          "description": "{{def(SampleEnum::PATH_FEEDRATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PATH_FEEDRATE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "PathFeedratePerRevolution": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "PathFeedratePerRevolution",
          "description": "{{def(SampleEnum::PATH_FEEDRATE_PER_REVOLUTION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/REVOLUTION"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PATH_FEEDRATE_PER_REVOLUTION"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "PathPosition": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "PathPosition",
          "description": "{{def(SampleEnum::PATH_POSITION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER_3D"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PATH_POSITION"
            },
            "value": {
              "type": "array",
              "items": {
                "type": "number"
              },
              "minItems": 3,
              "maxItems": 3
            }
          }
        }
      ]
    },
    "PH": {
      "type": "object",
      "title": "PH",
      "description": "{{def(SampleEnum::PH)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "PH"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PH"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Position": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Position",
          "description": "{{def(SampleEnum::POSITION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "POSITION"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "PowerFactor": {
      "type": "object",
      "title": "PowerFactor",
      "description": "{{def(SampleEnum::POWER_FACTOR)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "POWER_FACTOR"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Pressure": {
      "type": "object",
      "title": "Pressure",
      "description": "{{def(SampleEnum::PRESSURE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "PRESSURE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PASCAL"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ProcessTimer": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "ProcessTimer",
          "description": "{{def(SampleEnum::PROCESS_TIMER)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PROCESS_TIMER"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Resistance": {
      "type": "object",
      "title": "Resistance",
      "description": "{{def(SampleEnum::RESISTANCE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "RESISTANCE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "OHM"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "RotaryVelocity": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "RotaryVelocity",
          "description": "{{def(SampleEnum::ROTARY_VELOCITY)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "REVOLUTION/MINUTE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ROTARY_VELOCITY"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "SoundLevel": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SoundLevel",
          "description": "{{def(SampleEnum::SOUND_LEVEL)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DECIBEL"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SOUND_LEVEL"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "A_SCALE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "SpindleSpeed": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SpindleSpeed",
          "description": "{{def(SampleEnum::SPINDLE_SPEED)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "REVOLUTION/MINUTE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SPINDLE_SPEED"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "Strain": {
      "type": "object",
      "title": "Strain",
      "description": "{{def(SampleEnum::STRAIN)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "STRAIN"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Temperature": {
      "type": "object",
      "title": "Temperature",
      "description": "{{def(SampleEnum::TEMPERATURE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "TEMPERATURE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "CELSIUS"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Tension": {
      "type": "object",
      "title": "Tension",
      "description": "{{def(SampleEnum::TENSION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "TENSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "NEWTON"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Tilt": {
      "type": "object",
      "title": "Tilt",
      "description": "{{def(SampleEnum::TILT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "TILT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MICRO_RADIAN"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Torque": {
      "type": "object",
      "title": "Torque",
      "description": "{{def(SampleEnum::TORQUE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "TORQUE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "NEWTON_METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Velocity": {
      "type": "object",
      "title": "Velocity",
      "description": "{{def(SampleEnum::VELOCITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "VELOCITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Viscosity": {
      "type": "object",
      "title": "Viscosity",
      "description": "{{def(SampleEnum::VISCOSITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "VISCOSITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PASCAL_SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Voltage": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Voltage",
          "description": "{{def(SampleEnum::VOLTAGE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "VOLT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLTAGE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "VoltAmpere": {
      "type": "object",
      "title": "VoltAmpere",
      "description": "{{def(SampleEnum::VOLT_AMPERE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "VOLT_AMPERE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "VOLT_AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VoltAmpereReactive": {
      "type": "object",
      "title": "VoltAmpereReactive",
      "description": "{{def(SampleEnum::VOLT_AMPERE_REACTIVE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "VOLT_AMPERE_REACTIVE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "VOLT_AMPERE_REACTIVE"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VolumeFluid": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "VolumeFluid",
          "description": "{{def(SampleEnum::VOLUME_FLUID)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLILITER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLUME_FLUID"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "VolumeSpatial": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "VolumeSpatial",
          "description": "{{def(SampleEnum::VOLUME_SPATIAL)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "CUBIC_MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLUME_SPATIAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "Wattage": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Wattage",
          "description": "{{def(SampleEnum::WATTAGE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "WATT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "WATTAGE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "AmperageDC": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AmperageDC",
          "description": "{{def(SampleEnum::AMPERAGE_DC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "AMPERAGE_DC"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "AmperageAC": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AmperageAC",
          "description": "{{def(SampleEnum::AMPERAGE_AC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "AMPERAGE_AC"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "VoltageAC": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "VoltageAC",
          "description": "{{def(SampleEnum::VOLTAGE_AC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "VOLT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLTAGE_AC"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "VoltageDC": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "VoltageDC",
          "description": "{{def(SampleEnum::VOLTAGE_DC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "VOLT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLTAGE_DC"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "XDimension": {
      "type": "object",
      "title": "XDimension",
      "description": "{{def(SampleEnum::X_DIMENSION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "X_DIMENSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "YDimension": {
      "type": "object",
      "title": "YDimension",
      "description": "{{def(SampleEnum::Y_DIMENSION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "Y_DIMENSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ZDimension": {
      "type": "object",
      "title": "ZDimension",
      "description": "{{def(SampleEnum::Z_DIMENSION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "Z_DIMENSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Diameter": {
      "type": "object",
      "title": "Diameter",
      "description": "{{def(SampleEnum::DIAMETER)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DIAMETER"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Orientation": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Orientation",
          "description": "{{def(SampleEnum::ORIENTATION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE_3D"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ORIENTATION"
            },
            "value": {
              "type": "array",
              "items": {
                "type": "number"
              },
              "minItems": 3,
              "maxItems": 3
            }
          }
        }
      ]
    },
    "HumidityRelative": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "HumidityRelative",
          "description": "{{def(SampleEnum::HUMIDITY_RELATIVE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "PERCENT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "HUMIDITY_RELATIVE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "HumidityAbsolute": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "HumidityAbsolute",
          "description": "{{def(SampleEnum::HUMIDITY_ABSOLUTE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "GRAM/CUBIC_METER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "HUMIDITY_ABSOLUTE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "HumiditySpecific": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "HumiditySpecific",
          "description": "{{def(SampleEnum::HUMIDITY_SPECIFIC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "PERCENT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "HUMIDITY_SPECIFIC"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "PressurizationRate": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "PressurizationRate",
          "description": "{{def(SampleEnum::PRESSURIZATION_RATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "PASCAL/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PRESSURIZATION_RATE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "Deceleration": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Deceleration",
          "description": "{{def(SampleEnum::DECELERATION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DECELERATION"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "AssetUpdateRate": {
      "type": "object",
      "title": "AssetUpdateRate",
      "description": "{{def(SampleEnum::ASSET_UPDATE_RATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "ASSET_UPDATE_RATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "COUNT/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AngularDeceleration": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AngularDeceleration",
          "description": "{{def(SampleEnum::ANGULAR_DECELERATION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ANGULAR_DECELERATION"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "ObservationUpdateRate": {
      "type": "object",
      "title": "ObservationUpdateRate",
      "description": "{{def(SampleEnum::OBSERVATION_UPDATE_RATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "OBSERVATION_UPDATE_RATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "COUNT/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PressureAbsolute": {
      "type": "object",
      "title": "PressureAbsolute",
      "description": "The force per unit area measured relative to a vacuum.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "PRESSURE_ABSOLUTE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PASCAL"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Openness": {
      "type": "object",
      "title": "Openness",
      "description": "{{def(SampleEnum::OPENNESS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "OPENNESS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DewPoint": {
      "type": "object",
      "title": "DewPoint",
      "description": "{{def(SampleEnum::DEW_POINT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DEW_POINT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "CELSIUS"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "GravitationalForce": {
      "type": "object",
      "title": "GravitationalForce",
      "description": "{{def(SampleEnum::GRAVITATIONAL_FORCE)}} > Note: $$Mass\\times GravitationalAcceleration$$",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "GRAVITATIONAL_FORCE"
        },
        "units": {
          "$ref": "#/$defs/NativeUnitEnum",
          "default": "GRAVITATIONAL_FORCE"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "GravitationalAcceleration": {
      "type": "object",
      "title": "GravitationalAcceleration",
      "description": "{{def(SampleEnum::GRAVITATIONAL_ACCELERATION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "GRAVITATIONAL_ACCELERATION"
        },
        "units": {
          "$ref": "#/$defs/NativeUnitEnum",
          "default": "GRAVITATIONAL_ACCELERATION"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "BatteryCapacity": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "BatteryCapacity",
          "description": "{{def(SampleEnum::BATTERY_CAPACITY)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "COULOMB"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "BATTERY_CAPACITY"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "DischargeRate": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DischargeRate",
          "description": "{{def(SampleEnum::DISCHARGE_RATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DISCHARGE_RATE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "ChargeRate": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "ChargeRate",
          "description": "{{def(SampleEnum::CHARGE_RATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "CHARGE_RATE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "BatteryCharge": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "BatteryCharge",
          "description": "{{def(SampleEnum::BATTERY_CHARGE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "PERCENT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "BATTERY_CHARGE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "SettlingError": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SettlingError",
          "description": "{{def(SampleEnum::SETTLING_ERROR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "COUNT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SETTLING_ERROR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "SettlingErrorLinear": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SettlingErrorLinear",
          "description": "{{def(SampleEnum::SETTLING_ERROR_LINEAR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SETTLING_ERROR_LINEAR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "SettlingErrorAngular": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SettlingErrorAngular",
          "description": "{{def(SampleEnum::SETTLING_ERROR_ANGULAR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SETTLING_ERROR_ANGULAR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "FollowingError": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "FollowingError",
          "description": "{{def(SampleEnum::FOLLOWING_ERROR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "COUNT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "FOLLOWING_ERROR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "FollowingErrorAngular": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "FollowingErrorAngular",
          "description": "{{def(SampleEnum::FOLLOWING_ERROR_ANGULAR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "FOLLOWING_ERROR_ANGULAR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "FollowingErrorLinear": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "FollowingErrorLinear",
          "description": "{{def(SampleEnum::FOLLOWING_ERROR_LINEAR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "FOLLOWING_ERROR_LINEAR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "DisplacementLinear": {
      "type": "object",
      "title": "DisplacementLinear",
      "description": "{{def(SampleEnum::DISPLACEMENT_LINEAR)}} > Note: The displacement vector **MAY** be defined by the motion of the owning {{block(Component)}}.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DISPLACEMENT_LINEAR"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DisplacementAngular": {
      "type": "object",
      "title": "DisplacementAngular",
      "description": "{{def(SampleEnum::DISPLACEMENT_ANGULAR)}} > Note: The displacement vector **MAY** be defined by the motion of the owning {{block(Component)}}.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DISPLACEMENT_ANGULAR"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "DEGREE"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PositionCartesian": {
      "type": "object",
      "title": "PositionCartesian",
      "description": "{{def(SampleEnum::POSITION_CARTESIAN)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "POSITION_CARTESIAN"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER_3D"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "Resistivity": {
      "type": "object",
      "title": "Resistivity",
      "description": "{{def(SampleEnum::RESISTIVITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "RESISTIVITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "OHM_METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "FillHeight": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "FillHeight",
          "description": "{{def(SampleEnum::FILL_HEIGHT)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "FILL_HEIGHT"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "ParticleCount": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "ParticleCount",
          "description": "{{def(SampleEnum::PARTICLE_COUNT)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "COUNT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PARTICLE_COUNT"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "ParticleSize": {
      "type": "object",
      "title": "ParticleSize",
      "description": "{{def(SampleEnum::PARTICLE_SIZE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "PARTICLE_SIZE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ActiveAxes": {
      "type": "object",
      "title": "ActiveAxes",
      "description": "{{def(EventEnum::ACTIVE_AXES)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ACTIVE_AXES"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ActuatorState": {
      "type": "object",
      "title": "ActuatorState",
      "description": "{{def(EventEnum::ACTUATOR_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/ActuatorStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ACTUATOR_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Alarm": {
      "type": "object",
      "title": "Alarm",
      "description": "{{def(EventEnum::ALARM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ALARM"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "code": {
          "$ref": "#/$defs/AlarmCodeEnum",
          "description": "type of alarm."
        },
        "severity": {
          "$ref": "#/$defs/AlarmSeverityEnum",
          "description": "severity of the alarm."
        },
        "nativeCode": {
          "type": "string",
          "description": "native code for the piece of equipment."
        },
        "state": {
          "$ref": "#/$defs/AlarmStateEnum",
          "description": "state of the alarm."
        },
        "lang": {
          "type": "string",
          "description": "specifies the language of the alarm text. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        }
      },
      "required": [
        "code",
        "dataItemId",
        "nativeCode",
        "sequence",
        "severity",
        "state",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AssetChanged": {
      "type": "object",
      "title": "AssetChanged",
      "description": "{{def(EventEnum::ASSET_CHANGED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string",
          "description": "type of {{block(Asset)}} changed. See {{package(Asset Information Model)}} for details on the {{block(Asset)}} model."
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ASSET_CHANGED"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "hash": {
          "type": "string",
          "description": "condensed message digest from a secure one-way hash function. {{cite(FIPS PUB 180-4)}}"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AssetRemoved": {
      "type": "object",
      "title": "AssetRemoved",
      "description": "{{def(EventEnum::ASSET_REMOVED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string",
          "description": "type of {{block(Asset)}} removed. See {{package(Asset Information Model)}} for details on the {{block(Asset)}} model."
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ASSET_REMOVED"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "hash": {
          "type": "string",
          "description": "condensed message digest from a secure one-way hash function. {{cite(FIPS PUB 180-4)}}"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Availability": {
      "type": "object",
      "title": "Availability",
      "description": "{{def(EventEnum::AVAILABILITY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/AvailabilityEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "AVAILABILITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AxisCoupling": {
      "type": "object",
      "title": "AxisCoupling",
      "description": "{{def(EventEnum::AXIS_COUPLING)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/AxisCouplingEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "AXIS_COUPLING"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AxisFeedrateOverride": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "AxisFeedrateOverride",
          "description": "{{def(EventEnum::AXIS_FEEDRATE_OVERRIDE)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "AXIS_FEEDRATE_OVERRIDE"
            }
          }
        }
      ]
    },
    "AxisInterlock": {
      "type": "object",
      "title": "AxisInterlock",
      "description": "{{def(EventEnum::AXIS_INTERLOCK)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/AxisInterlockEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "AXIS_INTERLOCK"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AxisState": {
      "type": "object",
      "title": "AxisState",
      "description": "{{def(EventEnum::AXIS_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/AxisStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "AXIS_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Block": {
      "type": "object",
      "title": "Block",
      "description": "{{def(EventEnum::BLOCK)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "BLOCK"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "BlockCount": {
      "type": "object",
      "title": "BlockCount",
      "description": "{{def(EventEnum::BLOCK_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "BLOCK_COUNT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ChuckInterlock": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ChuckInterlock",
          "description": "{{def(EventEnum::CHUCK_INTERLOCK)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "$ref": "#/$defs/ChuckInterlockEnum"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "CHUCK_INTERLOCK"
            }
          }
        }
      ]
    },
    "ChuckState": {
      "type": "object",
      "title": "ChuckState",
      "description": "{{def(EventEnum::CHUCK_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/ChuckStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "CHUCK_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Code": {
      "type": "object",
      "title": "Code",
      "description": "{{def(EventEnum::CODE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "CODE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "CompositionState": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "CompositionState",
          "description": "{{def(EventEnum::COMPOSITION_STATE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "COMPOSITION_STATE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "ControllerMode": {
      "type": "object",
      "title": "ControllerMode",
      "description": "{{def(EventEnum::CONTROLLER_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/ControllerModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "CONTROLLER_MODE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ControllerModeOverride": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ControllerModeOverride",
          "description": "{{def(EventEnum::CONTROLLER_MODE_OVERRIDE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "CONTROLLER_MODE_OVERRIDE"
            },
            "value": {
              "oneOf": [
                {
                  "$ref": "#/$defs/ControllerModeOverrideEnum"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "CoupledAxes": {
      "type": "object",
      "title": "CoupledAxes",
      "description": "{{def(EventEnum::COUPLED_AXES)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "COUPLED_AXES"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "DateCode": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "DateCode",
          "description": "{{def(EventEnum::DATE_CODE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "DATE_CODE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "DeviceUuid": {
      "type": "object",
      "title": "DeviceUuid",
      "description": "{{def(EventEnum::DEVICE_UUID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "DEVICE_UUID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Direction": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Direction",
          "description": "{{def(EventEnum::DIRECTION)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "DIRECTION"
            },
            "value": {
              "oneOf": [
                {
                  "$ref": "#/$defs/DirectionEnum"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "DoorState": {
      "type": "object",
      "title": "DoorState",
      "description": "{{def(EventEnum::DOOR_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/DoorStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "DOOR_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "EmergencyStop": {
      "type": "object",
      "title": "EmergencyStop",
      "description": "{{def(EventEnum::EMERGENCY_STOP)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/EmergencyStopEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "EMERGENCY_STOP"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "EndOfBar": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "EndOfBar",
          "description": "{{def(EventEnum::END_OF_BAR)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "$ref": "#/$defs/EndOfBarEnum"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "END_OF_BAR"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "PRIMARY"
            }
          }
        }
      ]
    },
    "EquipmentMode": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "EquipmentMode",
          "description": "{{def(EventEnum::EQUIPMENT_MODE)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "$ref": "#/$defs/EquipmentModeEnum"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "EQUIPMENT_MODE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Execution": {
      "type": "object",
      "title": "Execution",
      "description": "{{def(EventEnum::EXECUTION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/ExecutionEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "EXECUTION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "FunctionalMode": {
      "type": "object",
      "title": "FunctionalMode",
      "description": "{{def(EventEnum::FUNCTIONAL_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/FunctionalModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "FUNCTIONAL_MODE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Hardness": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Hardness",
          "description": "{{def(EventEnum::HARDNESS)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "HARDNESS"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Line": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Line",
          "description": "{{def(EventEnum::LINE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "LINE"
            }
          }
        }
      ]
    },
    "LineLabel": {
      "type": "object",
      "title": "LineLabel",
      "description": "{{def(EventEnum::LINE_LABEL)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "LINE_LABEL"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "LineNumber": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "LineNumber",
          "description": "{{def(EventEnum::LINE_NUMBER)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "LINE_NUMBER"
            }
          }
        }
      ]
    },
    "Material": {
      "type": "object",
      "title": "Material",
      "description": "{{def(EventEnum::MATERIAL)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "MATERIAL"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "MaterialLayer": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "MaterialLayer",
          "description": "{{def(EventEnum::MATERIAL_LAYER)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "MATERIAL_LAYER"
            }
          }
        }
      ]
    },
    "Message": {
      "type": "object",
      "title": "Message",
      "description": "{{def(EventEnum::MESSAGE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "MESSAGE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "nativeCode": {
          "type": "string",
          "description": "control system local identification of the information being transferred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "OperatorId": {
      "type": "object",
      "title": "OperatorId",
      "description": "{{def(EventEnum::OPERATOR_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "OPERATOR_ID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PalletId": {
      "type": "object",
      "title": "PalletId",
      "description": "{{def(EventEnum::PALLET_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PALLET_ID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PartCount": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "PartCount",
          "description": "{{def(EventEnum::PART_COUNT)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PART_COUNT"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ALL"
            }
          }
        }
      ]
    },
    "PartDetect": {
      "type": "object",
      "title": "PartDetect",
      "description": "{{def(EventEnum::PART_DETECT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/PartDetectEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PART_DETECT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PartId": {
      "type": "object",
      "title": "PartId",
      "description": "{{def(EventEnum::PART_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PART_ID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PartNumber": {
      "type": "object",
      "title": "PartNumber",
      "description": "{{def(EventEnum::PART_NUMBER)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PART_NUMBER"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PathFeedrateOverride": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "PathFeedrateOverride",
          "description": "{{def(EventEnum::PATH_FEEDRATE_OVERRIDE)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PATH_FEEDRATE_OVERRIDE"
            }
          }
        }
      ]
    },
    "PathMode": {
      "type": "object",
      "title": "PathMode",
      "description": "{{def(EventEnum::PATH_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/PathModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ],
          "default": "INDEPENDENT"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PATH_MODE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PowerState": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "PowerState",
          "description": "{{def(EventEnum::POWER_STATE)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "$ref": "#/$defs/PowerStateEnum"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "POWER_STATE"
            }
          }
        }
      ]
    },
    "PowerStatus": {
      "type": "object",
      "title": "PowerStatus",
      "description": "{{def(EventEnum::POWER_STATUS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/PowerStatusEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "POWER_STATUS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessTime": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ProcessTime",
          "description": "{{def(EventEnum::PROCESS_TIME)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PROCESS_TIME"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Program": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Program",
          "description": "{{def(EventEnum::PROGRAM)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PROGRAM"
            }
          }
        }
      ]
    },
    "ProgramComment": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ProgramComment",
          "description": "{{def(EventEnum::PROGRAM_COMMENT)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PROGRAM_COMMENT"
            }
          }
        }
      ]
    },
    "ProgramEdit": {
      "type": "object",
      "title": "ProgramEdit",
      "description": "{{def(EventEnum::PROGRAM_EDIT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/ProgramEditEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PROGRAM_EDIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProgramEditName": {
      "type": "object",
      "title": "ProgramEditName",
      "description": "{{def(EventEnum::PROGRAM_EDIT_NAME)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PROGRAM_EDIT_NAME"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProgramHeader": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ProgramHeader",
          "description": "{{def(EventEnum::PROGRAM_HEADER)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PROGRAM_HEADER"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "MAIN"
            }
          }
        }
      ]
    },
    "ProgramLocation": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ProgramLocation",
          "description": "{{def(EventEnum::PROGRAM_LOCATION)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PROGRAM_LOCATION"
            }
          }
        }
      ]
    },
    "ProgramLocationType": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ProgramLocationType",
          "description": "{{def(EventEnum::PROGRAM_LOCATION_TYPE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PROGRAM_LOCATION_TYPE"
            },
            "value": {
              "oneOf": [
                {
                  "$ref": "#/$defs/ProgramLocationTypeEnum"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "ProgramNestLevel": {
      "type": "object",
      "title": "ProgramNestLevel",
      "description": "{{def(EventEnum::PROGRAM_NEST_LEVEL)}} If an initial value is not defined, the nesting level associated with the highest or initial nesting level of the program **MUST** default to zero (0).",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PROGRAM_NEST_LEVEL"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "RotaryMode": {
      "type": "object",
      "title": "RotaryMode",
      "description": "{{def(EventEnum::ROTARY_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/RotaryModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ROTARY_MODE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "RotaryVelocityOverride": {
      "type": "object",
      "title": "RotaryVelocityOverride",
      "description": "{{def(EventEnum::ROTARY_VELOCITY_OVERRIDE)}} This command represents a percentage change to the velocity calculated by a logic or motion program or set by a switch for a {{block(Rotary)}} type axis.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ROTARY_VELOCITY_OVERRIDE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SerialNumber": {
      "type": "object",
      "title": "SerialNumber",
      "description": "{{def(EventEnum::SERIAL_NUMBER)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "SERIAL_NUMBER"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SpindleInterlock": {
      "type": "object",
      "title": "SpindleInterlock",
      "description": "{{def(EventEnum::SPINDLE_INTERLOCK)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/SpindleInterlockEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "SPINDLE_INTERLOCK"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolAssetId": {
      "type": "object",
      "title": "ToolAssetId",
      "description": "{{def(EventEnum::TOOL_ASSET_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "TOOL_ASSET_ID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolGroup": {
      "type": "object",
      "title": "ToolGroup",
      "description": "{{def(EventEnum::TOOL_GROUP)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "TOOL_GROUP"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolId": {
      "type": "object",
      "title": "ToolId",
      "description": "{{def(EventEnum::TOOL_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "TOOL_ID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolNumber": {
      "type": "object",
      "title": "ToolNumber",
      "description": "{{def(EventEnum::TOOL_NUMBER)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "TOOL_NUMBER"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolOffset": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ToolOffset",
          "description": "{{def(EventEnum::TOOL_OFFSET)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "TOOL_OFFSET"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "User": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "User",
          "description": "{{def(EventEnum::USER)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "USER"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Variable": {
      "type": "object",
      "title": "Variable",
      "description": "{{def(EventEnum::VARIABLE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "VARIABLE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WaitState": {
      "type": "object",
      "title": "WaitState",
      "description": "{{def(EventEnum::WAIT_STATE)}} When {{property(Execution::result)}} is not `WAIT`, {{property(Observation::isUnavailable)}} of {{block(WaitState)}} **MUST** be `true`.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/WaitStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "WAIT_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Wire": {
      "type": "object",
      "title": "Wire",
      "description": "{{def(EventEnum::WIRE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "WIRE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WorkholdingId": {
      "type": "object",
      "title": "WorkholdingId",
      "description": "{{def(EventEnum::WORKHOLDING_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "WORKHOLDING_ID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WorkOffset": {
      "type": "object",
      "title": "WorkOffset",
      "description": "{{def(EventEnum::WORK_OFFSET)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "WORK_OFFSET"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "OperatingSystem": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "OperatingSystem",
          "description": "{{def(EventEnum::OPERATING_SYSTEM)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "OPERATING_SYSTEM"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Firmware": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Firmware",
          "description": "{{def(EventEnum::FIRMWARE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "FIRMWARE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Application": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Application",
          "description": "{{def(EventEnum::APPLICATION)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "APPLICATION"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Library": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Library",
          "description": "{{def(EventEnum::LIBRARY)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "LIBRARY"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Hardware": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Hardware",
          "description": "{{def(EventEnum::HARDWARE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "HARDWARE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Network": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Network",
          "description": "{{def(EventEnum::NETWORK)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "NETWORK"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "Rotation": {
      "type": "object",
      "title": "Rotation",
      "description": "{{def(EventEnum::ROTATION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "number"
              },
              "minItems": 3,
              "maxItems": 3
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ROTATION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "DEGREE_3D"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Translation": {
      "type": "object",
      "title": "Translation",
      "description": "{{def(EventEnum::TRANSLATION)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "number"
              },
              "minItems": 3,
              "maxItems": 3
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "TRANSLATION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER_3D"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessKindId": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ProcessKindId",
          "description": "{{def(EventEnum::PROCESS_KIND_ID)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PROCESS_KIND_ID"
            }
          }
        }
      ]
    },
    "PartStatus": {
      "type": "object",
      "title": "PartStatus",
      "description": "{{def(EventEnum::PART_STATUS)}} If unique identifier is given, part status is for that individual. If group identifier is given without a unique identifier, then the status is assumed to be for the whole group.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/PartStatusEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PART_STATUS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AlarmLimit": {
      "type": "object",
      "title": "AlarmLimit",
      "description": "{{def(EventEnum::ALARM_LIMIT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/AlarmLimitResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ALARM_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessAggregateId": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ProcessAggregateId",
          "description": "{{def(EventEnum::PROCESS_AGGREGATE_ID)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PROCESS_AGGREGATE_ID"
            }
          }
        }
      ]
    },
    "PartKindId": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "PartKindId",
          "description": "{{def(EventEnum::PART_KIND_ID)}} If no {{property(DataItem::subType)}} is specified, `UUID` is default.",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PART_KIND_ID"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "UUID"
            }
          }
        }
      ]
    },
    "AdapterURI": {
      "type": "object",
      "title": "AdapterURI",
      "description": "{{def(EventEnum::ADAPTER_URI)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ADAPTER_URI"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "DeviceRemoved": {
      "type": "object",
      "title": "DeviceRemoved",
      "description": "{{def(EventEnum::DEVICE_REMOVED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "DEVICE_REMOVED"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "hash": {
          "type": "string",
          "description": "condensed message digest from a secure one-way hash function. {{cite(FIPS PUB 180-4)}}"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "DeviceChanged": {
      "type": "object",
      "title": "DeviceChanged",
      "description": "{{def(EventEnum::DEVICE_CHANGED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "DEVICE_CHANGED"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "hash": {
          "type": "string",
          "description": "condensed message digest from a secure one-way hash function. {{cite(FIPS PUB 180-4)}}"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SpecificationLimit": {
      "type": "object",
      "title": "SpecificationLimit",
      "description": "{{def(EventEnum::SPECIFICATION_LIMIT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/SpecificationLimitResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "SPECIFICATION_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ConnectionStatus": {
      "type": "object",
      "title": "ConnectionStatus",
      "description": "{{def(EventEnum::CONNECTION_STATUS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/ConnectionStatusEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "CONNECTION_STATUS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AdapterSoftwareVersion": {
      "type": "object",
      "title": "AdapterSoftwareVersion",
      "description": "{{def(EventEnum::ADAPTER_SOFTWARE_VERSION)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ADAPTER_SOFTWARE_VERSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SensorAttachment": {
      "type": "object",
      "title": "SensorAttachment",
      "description": "{{def(EventEnum::SENSOR_ATTACHMENT)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "SENSOR_ATTACHMENT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ControlLimit": {
      "type": "object",
      "title": "ControlLimit",
      "description": "{{def(EventEnum::CONTROL_LIMIT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/ControlLimitResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "CONTROL_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "DeviceAdded": {
      "type": "object",
      "title": "DeviceAdded",
      "description": "{{def(EventEnum::DEVICE_ADDED)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "DEVICE_ADDED"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "hash": {
          "type": "string",
          "description": "condensed message digest from a secure one-way hash function. {{cite(FIPS PUB 180-4)}}"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "MTConnectVersion": {
      "type": "object",
      "title": "MTConnectVersion",
      "description": "{{def(EventEnum::MTCONNECT_VERSION)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "MTCONNECT_VERSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessOccurrenceId": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ProcessOccurrenceId",
          "description": "{{def(EventEnum::PROCESS_OCCURRENCE_ID)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PROCESS_OCCURRENCE_ID"
            }
          }
        }
      ]
    },
    "PartGroupId": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "PartGroupId",
          "description": "{{def(EventEnum::PART_GROUP_ID)}} If no {{property(DataItem::subType)}} is specified, `UUID` is default.",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PART_GROUP_ID"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "UUID"
            }
          }
        }
      ]
    },
    "PartUniqueId": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "PartUniqueId",
          "description": "{{def(EventEnum::PART_UNIQUE_ID)}} If no {{property(DataItem::subType)}} is specified, `UUID` is default.",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "PART_UNIQUE_ID"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "UUID"
            }
          }
        }
      ]
    },
    "ActivationCount": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ActivationCount",
          "description": "{{def(EventEnum::ACTIVATION_COUNT)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "ACTIVATION_COUNT"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ALL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "DeactivationCount": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "DeactivationCount",
          "description": "{{def(EventEnum::DEACTIVATION_COUNT)}}",
          "properties": {
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ALL"
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "DEACTIVATION_COUNT"
            },
            "value": {
              "oneOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "TransferCount": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "TransferCount",
          "description": "{{def(EventEnum::TRANSFER_COUNT)}}",
          "properties": {
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ALL"
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "TRANSFER_COUNT"
            },
            "value": {
              "oneOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "LoadCount": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "LoadCount",
          "description": "{{def(EventEnum::LOAD_COUNT)}}",
          "properties": {
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ALL"
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "LOAD_COUNT"
            },
            "value": {
              "oneOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "PartProcessingState": {
      "type": "object",
      "title": "PartProcessingState",
      "description": "{{def(EventEnum::PART_PROCESSING_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/PartProcessingStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PART_PROCESSING_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ProcessState": {
      "type": "object",
      "title": "ProcessState",
      "description": "{{def(EventEnum::PROCESS_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/ProcessStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PROCESS_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ValveState": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "ValveState",
          "description": "{{def(EventEnum::VALVE_STATE)}}",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "$ref": "#/$defs/ValveStateEnum"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "VALVE_STATE"
            }
          }
        }
      ]
    },
    "LockState": {
      "type": "object",
      "title": "LockState",
      "description": "{{def(EventEnum::LOCK_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/LockStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "LOCK_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "UnloadCount": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "UnloadCount",
          "description": "{{def(EventEnum::UNLOAD_COUNT)}}",
          "properties": {
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ALL"
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "UNLOAD_COUNT"
            },
            "value": {
              "oneOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "CycleCount": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "CycleCount",
          "description": "{{def(EventEnum::CYCLE_COUNT)}}",
          "properties": {
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ALL"
            },
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "CYCLE_COUNT"
            },
            "value": {
              "oneOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "OperatingMode": {
      "type": "object",
      "title": "OperatingMode",
      "description": "{{def(EventEnum::OPERATING_MODE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/OperatingModeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "OPERATING_MODE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AssetCount": {
      "type": "object",
      "title": "AssetCount",
      "description": "{{def(EventEnum::ASSET_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ASSET_COUNT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "MaintenanceList": {
      "type": "object",
      "title": "MaintenanceList",
      "description": "{{def(EventEnum::MAINTENANCE_LIST)}} If {{property(MaintenanceList::result::Interval)}} `key` is not provided, it is assumed `ABSOLUTE`. If {{property(MaintenanceList::result::Direction)}} `key` is not provided, it is assumed `UP`. If {{property(MaintenanceList::result::Units)}} `key` is not provided, it is assumed to be `COUNT`.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/MaintenanceListResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "MAINTENANCE_LIST"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "FixtureId": {
      "type": "object",
      "title": "FixtureId",
      "description": "{{def(EventEnum::FIXTURE_ID)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "FIXTURE_ID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PartCountType": {
      "type": "object",
      "title": "PartCountType",
      "description": "{{def(EventEnum::PART_COUNT_TYPE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/PartCountTypeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PART_COUNT_TYPE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ClockTime": {
      "type": "object",
      "title": "ClockTime",
      "description": "{{def(EventEnum::CLOCK_TIME)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "CLOCK_TIME"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "NetworkPort": {
      "type": "object",
      "title": "NetworkPort",
      "description": "{{def(EventEnum::NETWORK_PORT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "NETWORK_PORT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "HostName": {
      "type": "object",
      "title": "HostName",
      "description": "{{def(EventEnum::HOST_NAME)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "HOST_NAME"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "LeakDetect": {
      "type": "object",
      "title": "LeakDetect",
      "description": "{{def(EventEnum::LEAK_DETECT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/LeakDetectEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "LEAK_DETECT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "BatteryState": {
      "type": "object",
      "title": "BatteryState",
      "description": "{{def(EventEnum::BATTERY_STATE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/BatteryStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "BATTERY_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "FeaturePersisitentId": {
      "type": "object",
      "title": "FeaturePersisitentId",
      "description": "{{def(EventEnum::FEATURE_PERSISTENT_ID)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "FEATURE_PERSISTENT_ID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SensorState": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "SensorState",
          "description": "{{def(EventEnum::SENSOR_STATE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "SENSOR_STATE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "BINARY"
            }
          }
        }
      ]
    },
    "ComponentData": {
      "type": "object",
      "title": "ComponentData",
      "description": "tabular {{def(EventEnum::COMPONENT_DATA)}} If the {{block(Component)}} multiplicity can be determined, the device model **MUST** use a fixed set of {{block(Component)}}s. {{block(ComponentData)}} **MUST** provide a {{block(DataItem)}} {{block(Definition)}}.",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "COMPONENT_DATA"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WorkOffsets": {
      "type": "object",
      "title": "WorkOffsets",
      "description": "tabular representation of {{def(EventEnum::WORK_OFFSETS)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "WORK_OFFSETS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolOffsets": {
      "type": "object",
      "title": "ToolOffsets",
      "description": "tabular representation of {{def(EventEnum::TOOL_OFFSETS)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "TOOL_OFFSETS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "FeatureMeasurement": {
      "type": "object",
      "title": "FeatureMeasurement",
      "description": "tabular representation of {{def(EventEnum::FEATURE_MEASUREMENT)}} {{block(FeatureMeasurement)}} **MAY** include a {{term(characteristic)}} in which case it **MAY** include a `CHARACTERISTIC_STATUS`.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/FeatureMeasurementResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "FEATURE_MEASUREMENT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "CharacteristicPersistentId": {
      "type": "object",
      "title": "CharacteristicPersistentId",
      "description": "{{def(EventEnum::CHARACTERISTIC_PERSISTENT_ID)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ADAPTER_URI"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "MeasurementType": {
      "type": "object",
      "title": "MeasurementType",
      "description": "{{def(EventEnum::MEASUREMENT_TYPE)}} Examples: `POINT`, `RADIUS`, `ANGLE`, `LENGTH`, etc.",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "MEASUREMENT_TYPE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "MeasurementValue": {
      "type": "object",
      "title": "MeasurementValue",
      "description": "{{def(EventEnum::MEASUREMENT_VALUE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "MEASUREMENT_VALUE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "MeasurementUnits": {
      "type": "object",
      "title": "MeasurementUnits",
      "description": "{{def(EventEnum::MEASUREMENT_UNITS)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "MEASUREMENT_UNITS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "CharacteristicStatus": {
      "type": "object",
      "title": "CharacteristicStatus",
      "description": "{{def(EventEnum::CHARACTERISTIC_STATUS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/CharacteristicStatusEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ADAPTER_URI"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "UncertaintyType": {
      "type": "object",
      "title": "UncertaintyType",
      "description": "{{def(EventEnum::UNCERTAINTY_TYPE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/UncertaintyTypeEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "UNCERTAINTY_TYPE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Uncertainty": {
      "type": "object",
      "title": "Uncertainty",
      "description": "{{def(EventEnum::UNCERTAINTY)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "number"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "UNCERTAINTY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AlarmLimits": {
      "type": "object",
      "title": "AlarmLimits",
      "description": "{{def(EventEnum::ALARM_LIMITS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/AlarmLimitResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ALARM_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ControlLimits": {
      "type": "object",
      "title": "ControlLimits",
      "description": "{{def(EventEnum::CONTROL_LIMITS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/ControlLimitResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "CONTROL_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SpecificationLimits": {
      "type": "object",
      "title": "SpecificationLimits",
      "description": "{{def(EventEnum::SPECIFICATION_LIMITS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/SpecificationLimitResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "SPECIFICATION_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolCuttingItem": {
      "type": "object",
      "title": "ToolCuttingItem",
      "description": "{{def(EventEnum::TOOL_CUTTING_ITEM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "TOOL_CUTTING_ITEM"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "LocationAddress": {
      "type": "object",
      "title": "LocationAddress",
      "description": "{{def(EventEnum::LOCATION_ADDRESS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/LocationAddressResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "LOCATION_ADDRESS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ActivePowerSource": {
      "type": "object",
      "title": "ActivePowerSource",
      "description": "{{def(EventEnum::ACTIVE_POWER_SOURCE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ],
          "description": "name of the power source referring to {{block(Configuration)}} {{property(PowerSource::value)}}."
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ACTIVE_POWER_SOURCE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "LocationNarrative": {
      "type": "object",
      "title": "LocationNarrative",
      "description": "{{def(EventEnum::LOCATION_NARRATIVE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "LOCATION_NARRATIVE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Thickness": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "Thickness",
          "description": "{{def(EventEnum::THICKNESS)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/EventEnum",
              "default": "THICKNESS"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "LocationSpatialGeographic": {
      "type": "object",
      "title": "LocationSpatialGeographic",
      "description": "{{def(EventEnum::LOCATION_SPATIAL_GEOGRAPHIC)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/LocationSpatialGeographicResult"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "LOCATION_SPATIAL_GEOGRAPHIC"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "PartIndex": {
      "type": "object",
      "title": "PartIndex",
      "description": "{{def(EventEnum::PART_INDEX)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "integer"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "PART_INDEX"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "Actuator": {
      "type": "object",
      "title": "Actuator",
      "description": "{{def(ConditionEnum::ACTUATOR)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/ConditionEnum",
          "default": "ACTUATOR"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code is the proprietary identifier designating a specific alarm, fault or warning code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/$defs/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(Condition::qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "{{property(Condition::statistic)}} provides additional information describing the meaning of the {{block(Condition)}} entity. {{property(Condition::statistic)}} **MUST** match the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(Observation::result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/$defs/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        },
        "conditionId": {
          "type": "string",
          "description": "identifier of an individual {{term(condition activation)}} provided by a piece of equipment. {{property(Condition::conditionId)}} **MUST** be unique for all concurrent {{termpl(condition activation)}}. {{property(Condition::conditionId)}} **MUST** be maintained for all state transitions related to the same {{term(condition activation)}}. Multiple {{property(Condition::conditionId)}}s **MAY** exist for the same {{property(nativeCode)}}. If {{property(Condition::conditionId)}} is not given, the value is the {{property(Condition::nativeCode)}}. If {{property(Condition::nativeCode)}} and {{property(Condition::conditionId)}} are not given, {{property(Condition::conditionId)}} **MUST** be generated."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "Communications": {
      "type": "object",
      "title": "Communications",
      "description": "{{def(ConditionEnum::COMMUNICATIONS)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/ConditionEnum",
          "default": "COMMUNICATIONS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code is the proprietary identifier designating a specific alarm, fault or warning code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/$defs/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(Condition::qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "{{property(Condition::statistic)}} provides additional information describing the meaning of the {{block(Condition)}} entity. {{property(Condition::statistic)}} **MUST** match the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(Observation::result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/$defs/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        },
        "conditionId": {
          "type": "string",
          "description": "identifier of an individual {{term(condition activation)}} provided by a piece of equipment. {{property(Condition::conditionId)}} **MUST** be unique for all concurrent {{termpl(condition activation)}}. {{property(Condition::conditionId)}} **MUST** be maintained for all state transitions related to the same {{term(condition activation)}}. Multiple {{property(Condition::conditionId)}}s **MAY** exist for the same {{property(nativeCode)}}. If {{property(Condition::conditionId)}} is not given, the value is the {{property(Condition::nativeCode)}}. If {{property(Condition::nativeCode)}} and {{property(Condition::conditionId)}} are not given, {{property(Condition::conditionId)}} **MUST** be generated."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "DataRange": {
      "type": "object",
      "title": "DataRange",
      "description": "{{def(ConditionEnum::DATA_RANGE)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/ConditionEnum",
          "default": "DATA_RANGE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code is the proprietary identifier designating a specific alarm, fault or warning code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/$defs/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(Condition::qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "{{property(Condition::statistic)}} provides additional information describing the meaning of the {{block(Condition)}} entity. {{property(Condition::statistic)}} **MUST** match the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(Observation::result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/$defs/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        },
        "conditionId": {
          "type": "string",
          "description": "identifier of an individual {{term(condition activation)}} provided by a piece of equipment. {{property(Condition::conditionId)}} **MUST** be unique for all concurrent {{termpl(condition activation)}}. {{property(Condition::conditionId)}} **MUST** be maintained for all state transitions related to the same {{term(condition activation)}}. Multiple {{property(Condition::conditionId)}}s **MAY** exist for the same {{property(nativeCode)}}. If {{property(Condition::conditionId)}} is not given, the value is the {{property(Condition::nativeCode)}}. If {{property(Condition::nativeCode)}} and {{property(Condition::conditionId)}} are not given, {{property(Condition::conditionId)}} **MUST** be generated."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "LogicProgram": {
      "type": "object",
      "title": "LogicProgram",
      "description": "{{def(ConditionEnum::LOGIC_PROGRAM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/ConditionEnum",
          "default": "LOGIC_PROGRAM"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code is the proprietary identifier designating a specific alarm, fault or warning code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/$defs/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(Condition::qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "{{property(Condition::statistic)}} provides additional information describing the meaning of the {{block(Condition)}} entity. {{property(Condition::statistic)}} **MUST** match the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(Observation::result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/$defs/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        },
        "conditionId": {
          "type": "string",
          "description": "identifier of an individual {{term(condition activation)}} provided by a piece of equipment. {{property(Condition::conditionId)}} **MUST** be unique for all concurrent {{termpl(condition activation)}}. {{property(Condition::conditionId)}} **MUST** be maintained for all state transitions related to the same {{term(condition activation)}}. Multiple {{property(Condition::conditionId)}}s **MAY** exist for the same {{property(nativeCode)}}. If {{property(Condition::conditionId)}} is not given, the value is the {{property(Condition::nativeCode)}}. If {{property(Condition::nativeCode)}} and {{property(Condition::conditionId)}} are not given, {{property(Condition::conditionId)}} **MUST** be generated."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "MotionProgram": {
      "type": "object",
      "title": "MotionProgram",
      "description": "{{def(ConditionEnum::MOTION_PROGRAM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/ConditionEnum",
          "default": "MOTION_PROGRAM"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code is the proprietary identifier designating a specific alarm, fault or warning code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/$defs/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(Condition::qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "{{property(Condition::statistic)}} provides additional information describing the meaning of the {{block(Condition)}} entity. {{property(Condition::statistic)}} **MUST** match the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(Observation::result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/$defs/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        },
        "conditionId": {
          "type": "string",
          "description": "identifier of an individual {{term(condition activation)}} provided by a piece of equipment. {{property(Condition::conditionId)}} **MUST** be unique for all concurrent {{termpl(condition activation)}}. {{property(Condition::conditionId)}} **MUST** be maintained for all state transitions related to the same {{term(condition activation)}}. Multiple {{property(Condition::conditionId)}}s **MAY** exist for the same {{property(nativeCode)}}. If {{property(Condition::conditionId)}} is not given, the value is the {{property(Condition::nativeCode)}}. If {{property(Condition::nativeCode)}} and {{property(Condition::conditionId)}} are not given, {{property(Condition::conditionId)}} **MUST** be generated."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "System": {
      "type": "object",
      "title": "System",
      "description": "{{def(ConditionEnum::SYSTEM)}}",
      "properties": {
        "value": {
          "description": "{{term(observation)}} of the {{block(Observation)}} entity. The default value type for {{property(Observation::result)}} is `string`. > Note 1 to entry: in {{term(XML)}} the {{property(Observation::result)}} is the `CDATA` of the Observation {{term(element)}}. ~~~~xml <Execution dataItemId=\"...\" ...>READY</Execution> ~~~~ > Note 2 to entry: in {{term(JSON)}} the {{property(Observation::result)}} is the member `value` of the Observation object. ~~~~json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"READY\" } ] ~~~~"
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/ConditionEnum",
          "default": "SYSTEM"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "nativeCode": {
          "type": "string",
          "description": "native code is the proprietary identifier designating a specific alarm, fault or warning code provided by the piece of equipment."
        },
        "nativeSeverity": {
          "type": "string",
          "description": "severity information to a client software application if the piece of equipment designates a severity level to a fault."
        },
        "qualifier": {
          "$ref": "#/$defs/QualifierEnum",
          "description": "additional information regarding a {{term(condition state)}} associated with the measured value of a process variable. {{property(Condition::qualifier)}} defines whether the {{term(condition state)}} represented indicates a measured value that is above or below an expected value of a process variable."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "{{property(Condition::statistic)}} provides additional information describing the meaning of the {{block(Condition)}} entity. {{property(Condition::statistic)}} **MUST** match the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "xs:lang": {
          "type": "string",
          "description": "specifies the language of the {{property(Observation::result)}} returned for the {{block(Condition)}}. See {{cite(IETF RFC 4646)}} (http://www.ietf.org/rfc/rfc4646.txt)."
        },
        "state": {
          "$ref": "#/$defs/ConditionStateEnum",
          "description": "{{term(condition state)}} of the piece of equipment or {{block(Component)}}."
        },
        "conditionId": {
          "type": "string",
          "description": "identifier of an individual {{term(condition activation)}} provided by a piece of equipment. {{property(Condition::conditionId)}} **MUST** be unique for all concurrent {{termpl(condition activation)}}. {{property(Condition::conditionId)}} **MUST** be maintained for all state transitions related to the same {{term(condition activation)}}. Multiple {{property(Condition::conditionId)}}s **MAY** exist for the same {{property(nativeCode)}}. If {{property(Condition::conditionId)}} is not given, the value is the {{property(Condition::nativeCode)}}. If {{property(Condition::nativeCode)}} and {{property(Condition::conditionId)}} are not given, {{property(Condition::conditionId)}} **MUST** be generated."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "type"
      ],
      "additionalProperties": false
    },
    "MaterialFeed": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "MaterialFeed",
          "description": "{{def(InterfaceEventEnum::MATERIAL_FEED)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "MATERIAL_FEED"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "MaterialChange": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "MaterialChange",
          "description": "{{def(InterfaceEventEnum::MATERIAL_CHANGE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "MATERIAL_CHANGE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "MaterialRetract": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "MaterialRetract",
          "description": "{{def(InterfaceEventEnum::MATERIAL_RETRACT)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "MATERIAL_RETRACT"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "MaterialLoad": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "MaterialLoad",
          "description": "{{def(InterfaceEventEnum::MATERIAL_LOAD)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "MATERIAL_LOAD"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "MaterialUnload": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "MaterialUnload",
          "description": "{{def(InterfaceEventEnum::MATERIAL_UNLOAD)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "MATERIAL_UNLOAD"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "OpenChuck": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "OpenChuck",
          "description": "{{def(InterfaceEventEnum::OPEN_CHUCK)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "OPEN_CHUCK"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "OpenDoor": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "OpenDoor",
          "description": "{{def(InterfaceEventEnum::OPEN_DOOR)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "OPEN_DOOR"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "PartChange": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "PartChange",
          "description": "{{def(InterfaceEventEnum::PART_CHANGE)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "PART_CHANGE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "CloseDoor": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "CloseDoor",
          "description": "{{def(InterfaceEventEnum::CLOSE_DOOR)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "CLOSE_DOOR"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "CloseChuck": {
      "allOf": [
        {
          "$ref": "#/$defs/Event"
        },
        {
          "type": "object",
          "title": "CloseChuck",
          "description": "{{def(InterfaceEventEnum::CLOSE_CHUCK)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/InterfaceEventEnum",
              "default": "CLOSE_CHUCK"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            }
          },
          "required": [
            "subType"
          ]
        }
      ]
    },
    "InterfaceState": {
      "type": "object",
      "title": "InterfaceState",
      "description": "{{def(InterfaceEventEnum::INTERFACE_STATE)}} When the {{block(InterfaceState)}} is `DISABLED`, the state of all data items that are specific for the {{term(interaction model)}} associated with that {{block(Interface)}} **MUST** be set to `NOT_READY`.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "$ref": "#/$defs/InterfaceStateEnum"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/InterfaceEventEnum",
          "default": "INTERFACE_STATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "CoordinateSystemEnum": {
      "title": "CoordinateSystemEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MACHINE",
            "WORK"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ResetTriggerEnum": {
      "title": "ResetTriggerEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTION_COMPLETE",
            "ANNUAL",
            "DAY",
            "LIFE",
            "MAINTENANCE",
            "MONTH",
            "POWER_ON",
            "SHIFT",
            "WEEK"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "StatisticEnum": {
      "title": "StatisticEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AVERAGE",
            "KURTOSIS",
            "MAXIMUM",
            "MEDIAN",
            "MINIMUM",
            "MODE",
            "RANGE",
            "ROOT_MEAN_SQUARE",
            "STANDARD_DEVIATION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "UnitEnum": {
      "title": "UnitEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AMPERE",
            "CELSIUS",
            "COUNT",
            "DECIBEL",
            "DEGREE",
            "DEGREE_3D",
            "DEGREE/SECOND",
            "DEGREE/SECOND\\^2",
            "HERTZ",
            "JOULE",
            "KILOGRAM",
            "LITER",
            "LITER/SECOND",
            "MICRO_RADIAN",
            "MILLIMETER",
            "MILLIMETER_3D",
            "MILLIMETER/REVOLUTION",
            "MILLIMETER/SECOND",
            "MILLIMETER/SECOND\\^2",
            "NEWTON",
            "NEWTON_METER",
            "OHM",
            "PASCAL",
            "PASCAL_SECOND",
            "PERCENT",
            "PH",
            "REVOLUTION/MINUTE",
            "SECOND",
            "SIEMENS/METER",
            "VOLT",
            "VOLT_AMPERE",
            "VOLT_AMPERE_REACTIVE",
            "WATT",
            "WATT_SECOND",
            "GRAM/CUBIC_METER",
            "CUBIC_MILLIMETER",
            "CUBIC_MILLIMETER/SECOND",
            "CUBIC_MILLIMETER/SECOND\\^2",
            "MILLIGRAM",
            "MILLIGRAM/CUBIC_MILLIMETER",
            "MILLILITER",
            "COUNT/SECOND",
            "PASCAL/SECOND",
            "UNIT_VECTOR_3D",
            "REVOLUTION/SECOND\\^2",
            "REVOLUTION/SECOND",
            "GRAM",
            "METER/SECOND\\^2",
            "COULOMB",
            "CUBIC_METER",
            "SQUARE_MILLIMETER",
            "OHM_METER"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DataItemTypeEnum": {
      "type": "string",
      "title": "DataItemTypeEnum"
    },
    "CategoryEnum": {
      "title": "CategoryEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "SAMPLE",
            "EVENT",
            "CONDITION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DataItemSubTypeEnum": {
      "title": "DataItemSubTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ABSOLUTE",
            "ACTION",
            "ACTUAL",
            "ALL",
            "ALTERNATING",
            "A_SCALE",
            "AUXILIARY",
            "BAD",
            "BRINELL",
            "B_SCALE",
            "COMMANDED",
            "CONSUMED",
            "CONTROL",
            "C_SCALE",
            "DELAY",
            "DIRECT",
            "DRY_RUN",
            "D_SCALE",
            "EXPIRATION",
            "FIRST_USE",
            "GOOD",
            "INCREMENTAL",
            "JOG",
            "LATERAL",
            "LEEB",
            "LENGTH",
            "LINE",
            "LINEAR",
            "LOADED",
            "MACHINE_AXIS_LOCK",
            "MAIN",
            "MAINTENANCE",
            "MANUAL_UNCLAMP",
            "MANUFACTURE",
            "MAXIMUM",
            "MINIMUM",
            "MOHS",
            "MOTION",
            "NO_SCALE",
            "OPERATING",
            "OPERATOR",
            "OPTIONAL_STOP",
            "OVERRIDE",
            "POWERED",
            "PRIMARY",
            "PROBE",
            "PROCESS",
            "PROGRAMMED",
            "RADIAL",
            "RAPID",
            "REMAINING",
            "ROCKWELL",
            "ROTARY",
            "SCHEDULE",
            "SET_UP",
            "SHORE",
            "SINGLE_BLOCK",
            "STANDARD",
            "START",
            "SWITCHED",
            "TARGET",
            "TARGET_COMPLETION",
            "TOOL_CHANGE_STOP",
            "USEABLE",
            "VERTICAL",
            "VICKERS",
            "WORKING",
            "IPV4_ADDRESS",
            "IPV6_ADDRESS",
            "GATEWAY",
            "SUBNET_MASK",
            "VLAN_ID",
            "MAC_ADDRESS",
            "WIRELESS",
            "LICENSE",
            "VERSION",
            "RELEASE_DATE",
            "INSTALL_DATE",
            "MANUFACTURER",
            "UUID",
            "SERIAL_NUMBER",
            "RAW_MATERIAL",
            "LOT",
            "BATCH",
            "HEAT_TREAT",
            "PART_NUMBER",
            "PART_FAMILY",
            "PART_NAME",
            "PROCESS_STEP",
            "PROCESS_PLAN",
            "ORDER_NUMBER",
            "PROCESS_NAME",
            "ISO_STEP_EXECUTABLE",
            "COMPLETE",
            "ACTIVE",
            "FAILED",
            "ABORTED",
            "ENDED",
            "WASTE",
            "PART",
            "REQUEST",
            "RESPONSE",
            "ACTIVITY",
            "SEGMENT",
            "RECIPE",
            "OPERATION",
            "BINARY",
            "BOOLEAN",
            "ENUMERATED",
            "DETECT",
            "MODEL",
            "MEASURED",
            "GAS",
            "LIQUID",
            "SOLID"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "NativeUnitEnum": {
      "title": "NativeUnitEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CENTIPOISE",
            "DEGREE/MINUTE",
            "FAHRENHEIT",
            "FOOT",
            "FOOT/MINUTE",
            "FOOT/SECOND",
            "FOOT/SECOND\\^2",
            "FOOT_3D",
            "GALLON/MINUTE",
            "HOUR",
            "INCH",
            "INCH/MINUTE",
            "INCH/SECOND",
            "INCH/SECOND\\^2",
            "INCH_POUND",
            "INCH_3D",
            "KELVIN",
            "KILOWATT",
            "KILOWATT_HOUR",
            "LITER/MINUTE",
            "MILLIMETER/MINUTE",
            "MINUTE",
            "OTHER",
            "POUND",
            "POUND/INCH\\^2",
            "RADIAN",
            "RADIAN/MINUTE",
            "RADIAN/SECOND",
            "RADIAN/SECOND\\^2",
            "BAR",
            "TORR",
            "MILLIMETER_MERCURY",
            "PASCAL/MINUTE",
            "GRAVITATIONAL_FORCE",
            "GRAVITATIONAL_ACCELERATION",
            "AMPERE_HOUR",
            "CUBIC_FOOT/HOUR",
            "CUBIC_FOOT/MINUTE",
            "SQUARE_INCH",
            "CUBIC_FOOT",
            "INCH/REVOLUTION",
            "MICROMETER",
            "RANKINE",
            "MICROTORR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FilterEnum": {
      "title": "FilterEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MINIMUM_DELTA",
            "PERIOD"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RepresentationEnum": {
      "title": "RepresentationEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "TIME_SERIES",
            "VALUE",
            "DATA_SET",
            "DISCRETE",
            "TABLE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "boolean": {
      "type": "boolean",
      "title": "boolean",
      "description": "primitive type."
    },
    "ID": {
      "type": "string",
      "title": "ID",
      "description": "string that represents an {{term(ID)}}."
    },
    "string": {
      "type": "string",
      "title": "string",
      "description": "primitive type."
    },
    "float": {
      "type": "number",
      "title": "float",
      "description": "primitive type."
    },
    "datetime": {
      "type": "string",
      "title": "datetime",
      "description": "string that represents timestamp in ISO 8601 format."
    },
    "integer": {
      "type": "integer",
      "title": "integer",
      "description": "primitive type."
    },
    "xlinktype": {
      "type": "string",
      "title": "xlinktype",
      "description": "string that represents the type of an XLink element. See {{url(https://www.w3.org/TR/xlink11/)}}."
    },
    "xslang": {
      "type": "string",
      "title": "xslang",
      "description": "string that represents a language tag. See {{url(http://www.ietf.org/rfc/rfc4646.txt)}}."
    },
    "SECOND": {
      "type": "string",
      "title": "SECOND",
      "description": "float that represents time in seconds."
    },
    "xlinkhref": {
      "type": "string",
      "title": "xlinkhref",
      "description": "string that represents the locator attribute of an XLink element. See {{url(https://www.w3.org/TR/xlink11/)}}."
    },
    "ResetTriggeredEnum": {
      "title": "ResetTriggeredEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTION_COMPLETE",
            "ANNUAL",
            "DAY",
            "MAINTENANCE",
            "MANUAL",
            "MONTH",
            "POWER_ON",
            "SHIFT",
            "WEEK"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "QualifierEnum": {
      "title": "QualifierEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "HIGH",
            "LOW"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "SampleEnum": {
      "title": "SampleEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACCELERATION",
            "ACCUMULATED_TIME",
            "AMPERAGE",
            "ANGLE",
            "ANGULAR_ACCELERATION",
            "ANGULAR_VELOCITY",
            "AXIS_FEEDRATE",
            "CAPACITY_FLUID",
            "CAPACITY_SPATIAL",
            "CONCENTRATION",
            "CONDUCTIVITY",
            "CUTTING_SPEED",
            "DENSITY",
            "DEPOSITION_ACCELERATION_VOLUMETRIC",
            "DEPOSITION_DENSITY",
            "DEPOSITION_MASS",
            "DEPOSITION_RATE_VOLUMETRIC",
            "DEPOSITION_VOLUME",
            "DISPLACEMENT",
            "ELECTRICAL_ENERGY",
            "EQUIPMENT_TIMER",
            "FILL_LEVEL",
            "FLOW",
            "FREQUENCY",
            "GLOBAL_POSITION",
            "LENGTH",
            "LEVEL",
            "LINEAR_FORCE",
            "LOAD",
            "MASS",
            "PATH_FEEDRATE",
            "PATH_FEEDRATE_PER_REVOLUTION",
            "PATH_POSITION",
            "PH",
            "POSITION",
            "POWER_FACTOR",
            "PRESSURE",
            "PROCESS_TIMER",
            "RESISTANCE",
            "ROTARY_VELOCITY",
            "SOUND_LEVEL",
            "SPINDLE_SPEED",
            "STRAIN",
            "TEMPERATURE",
            "TENSION",
            "TILT",
            "TORQUE",
            "VELOCITY",
            "VISCOSITY",
            "VOLTAGE",
            "VOLT_AMPERE",
            "VOLT_AMPERE_REACTIVE",
            "VOLUME_FLUID",
            "VOLUME_SPATIAL",
            "WATTAGE",
            "AMPERAGE_AC",
            "AMPERAGE_DC",
            "VOLTAGE_AC",
            "VOLTAGE_DC",
            "X_DIMENSION",
            "Y_DIMENSION",
            "Z_DIMENSION",
            "DIAMETER",
            "ORIENTATION",
            "HUMIDITY_RELATIVE",
            "HUMIDITY_ABSOLUTE",
            "HUMIDITY_SPECIFIC",
            "OBSERVATION_UPDATE_RATE",
            "ASSET_UPDATE_RATE",
            "PRESSURIZATION_RATE",
            "DECELERATION",
            "ANGULAR_DECELERATION",
            "PRESSURE_ABSOLUTE",
            "OPENNESS",
            "DEW_POINT",
            "GRAVITATIONAL_FORCE",
            "GRAVITATIONAL_ACCELERATION",
            "BATTERY_CAPACITY",
            "DISCHARGE_RATE",
            "CHARGE_RATE",
            "BATTERY_CHARGE",
            "SETTLING_ERROR",
            "FOLLOWING_ERROR",
            "SETTLING_ERROR_LINEAR",
            "SETTLING_ERROR_ANGULAR",
            "FOLLOWING_ERROR_LINEAR",
            "FOLLOWING_ERROR_ANGULAR",
            "DISPLACEMENT_LINEAR",
            "DISPLACEMENT_ANGULAR",
            "POSITION_CARTESIAN",
            "RESISTIVITY",
            "FILL_HEIGHT",
            "PARTICLE_COUNT",
            "PARTICLE_SIZE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "EventEnum": {
      "title": "EventEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE_AXES",
            "ACTUATOR_STATE",
            "ALARM",
            "ASSET_CHANGED",
            "ASSET_REMOVED",
            "AVAILABILITY",
            "AXIS_COUPLING",
            "AXIS_FEEDRATE_OVERRIDE",
            "AXIS_INTERLOCK",
            "AXIS_STATE",
            "BLOCK",
            "BLOCK_COUNT",
            "CHUCK_INTERLOCK",
            "CHUCK_STATE",
            "CODE",
            "COMPOSITION_STATE",
            "CONTROLLER_MODE",
            "CONTROLLER_MODE_OVERRIDE",
            "COUPLED_AXES",
            "DATE_CODE",
            "DEVICE_UUID",
            "DIRECTION",
            "DOOR_STATE",
            "EMERGENCY_STOP",
            "END_OF_BAR",
            "EQUIPMENT_MODE",
            "EXECUTION",
            "FUNCTIONAL_MODE",
            "HARDNESS",
            "LINE",
            "LINE_LABEL",
            "LINE_NUMBER",
            "MATERIAL",
            "MATERIAL_LAYER",
            "MESSAGE",
            "OPERATOR_ID",
            "PALLET_ID",
            "PART_COUNT",
            "PART_DETECT",
            "PART_ID",
            "PART_NUMBER",
            "PATH_FEEDRATE_OVERRIDE",
            "PATH_MODE",
            "POWER_STATE",
            "POWER_STATUS",
            "PROCESS_TIME",
            "PROGRAM",
            "PROGRAM_COMMENT",
            "PROGRAM_EDIT",
            "PROGRAM_EDIT_NAME",
            "PROGRAM_HEADER",
            "PROGRAM_LOCATION",
            "PROGRAM_LOCATION_TYPE",
            "PROGRAM_NEST_LEVEL",
            "ROTARY_MODE",
            "ROTARY_VELOCITY_OVERRIDE",
            "SERIAL_NUMBER",
            "SPINDLE_INTERLOCK",
            "TOOL_ASSET_ID",
            "TOOL_GROUP",
            "TOOL_ID",
            "TOOL_NUMBER",
            "TOOL_OFFSET",
            "USER",
            "VARIABLE",
            "WAIT_STATE",
            "WIRE",
            "WORKHOLDING_ID",
            "WORK_OFFSET",
            "OPERATING_SYSTEM",
            "FIRMWARE",
            "APPLICATION",
            "LIBRARY",
            "HARDWARE",
            "NETWORK",
            "ROTATION",
            "TRANSLATION",
            "DEVICE_ADDED",
            "DEVICE_REMOVED",
            "DEVICE_CHANGED",
            "CONNECTION_STATUS",
            "ADAPTER_SOFTWARE_VERSION",
            "ADAPTER_URI",
            "MTCONNECT_VERSION",
            "SENSOR_ATTACHMENT",
            "PART_STATUS",
            "PROCESS_OCCURRENCE_ID",
            "PROCESS_AGGREGATE_ID",
            "PROCESS_KIND_ID",
            "PART_GROUP_ID",
            "PART_KIND_ID",
            "PART_UNIQUE_ID",
            "CONTROL_LIMIT",
            "SPECIFICATION_LIMIT",
            "ALARM_LIMIT",
            "LOAD_COUNT",
            "UNLOAD_COUNT",
            "TRANSFER_COUNT",
            "ACTIVATION_COUNT",
            "DEACTIVATION_COUNT",
            "CYCLE_COUNT",
            "VALVE_STATE",
            "LOCK_STATE",
            "PROCESS_STATE",
            "PART_PROCESSING_STATE",
            "OPERATING_MODE",
            "ASSET_COUNT",
            "MAINTENANCE_LIST",
            "FIXTURE_ID",
            "PART_COUNT_TYPE",
            "CLOCK_TIME",
            "HOST_NAME",
            "NETWORK_PORT",
            "LEAK_DETECT",
            "BATTERY_STATE",
            "FEATURE_PERSISTENT_ID",
            "SENSOR_STATE",
            "COMPONENT_DATA",
            "WORK_OFFSETS",
            "TOOL_OFFSETS",
            "FEATURE_MEASUREMENT",
            "CHARACTERISTIC_PERSISTENT_ID",
            "MEASUREMENT_TYPE",
            "MEASUREMENT_VALUE",
            "MEASUREMENT_UNITS",
            "CHARACTERISTIC_STATUS",
            "UNCERTAINTY_TYPE",
            "UNCERTAINTY",
            "SPECIFICATION_LIMITS",
            "CONTROL_LIMITS",
            "ALARM_LIMITS",
            "TOOL_CUTTING_ITEM",
            "LOCATION_ADDRESS",
            "ACTIVE_POWER_SOURCE",
            "LOCATION_NARRATIVE",
            "THICKNESS",
            "LOCATION_SPATIAL_GEOGRAPHIC",
            "PART_INDEX"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ConditionEnum": {
      "title": "ConditionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "COMMUNICATIONS",
            "DATA_RANGE",
            "LOGIC_PROGRAM",
            "MOTION_PROGRAM",
            "SYSTEM",
            "ACTUATOR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MotionActuationTypeEnum": {
      "title": "MotionActuationTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "DIRECT",
            "VIRTUAL",
            "NONE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MotionTypeEnum": {
      "title": "MotionTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PRISMATIC",
            "CONTINUOUS",
            "REVOLUTE",
            "FIXED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CoordinateSystemTypeEnum": {
      "title": "CoordinateSystemTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "WORLD",
            "BASE",
            "OBJECT",
            "TASK",
            "MECHANICAL_INTERFACE",
            "TOOL",
            "MOBILE_PLATFORM",
            "MACHINE",
            "CAMERA"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DataItemRelationshipTypeEnum": {
      "title": "DataItemRelationshipTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ATTACHMENT",
            "COORDINATE_SYSTEM",
            "LIMIT",
            "OBSERVATION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "SpecificationRelationshipTypeEnum": {
      "title": "SpecificationRelationshipTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "LIMIT"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MediaTypeEnum": {
      "title": "MediaTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "STEP",
            "STL",
            "GDML",
            "OBJ",
            "COLLADA",
            "IGES",
            "3DS",
            "ACIS",
            "X_T"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "OriginatorEnum": {
      "title": "OriginatorEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MANUFACTURER",
            "USER"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "QIFDocumentTypeEnum": {
      "title": "QIFDocumentTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MEASUREMENT_RESOURCE",
            "PLAN",
            "PRODUCT",
            "RESULTS",
            "RULES",
            "STATISTICS"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ProcessStateEnum": {
      "title": "ProcessStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "INITIALIZING",
            "READY",
            "ACTIVE",
            "COMPLETE",
            "INTERRUPTED",
            "ABORTED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AssetTypeEnum": {
      "title": "AssetTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CuttingTool",
            "File",
            "QIFDocumentWrapper",
            "RawMaterial",
            "CuttingToolArchetype",
            "FileArchetype"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ChuckStateEnum": {
      "title": "ChuckStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "OPEN",
            "CLOSED",
            "UNLATCHED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AxisCouplingEnum": {
      "title": "AxisCouplingEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "TANDEM",
            "SYNCHRONOUS",
            "MASTER",
            "SLAVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PartStatusEnum": {
      "title": "PartStatusEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PASS",
            "FAIL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DirectionRotaryEnum": {
      "title": "DirectionRotaryEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CLOCKWISE",
            "COUNTER_CLOCKWISE",
            "NONE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FileStateEnum": {
      "title": "FileStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "EXPERIMENTAL",
            "PRODUCTION",
            "REVISION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CriticalityTypeEnum": {
      "title": "CriticalityTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CRITICAL",
            "NONCRITICAL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateMotionEnum": {
      "title": "CompositionStateMotionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "OPEN",
            "UNLATCHED",
            "CLOSED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ActuatorStateEnum": {
      "title": "ActuatorStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ConnectionStatusEnum": {
      "title": "ConnectionStatusEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CLOSED",
            "LISTEN",
            "ESTABLISHED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RoleTypeEnum": {
      "title": "RoleTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "SYSTEM",
            "AUXILIARY"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PathModeEnum": {
      "title": "PathModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "INDEPENDENT",
            "MASTER",
            "SYNCHRONOUS",
            "MIRROR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateActionEnum": {
      "title": "CompositionStateActionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MILLIMETER": {
      "type": "string",
      "title": "MILLIMETER",
      "description": "float that represents measurement in millimeter."
    },
    "PartProcessingStateEnum": {
      "title": "PartProcessingStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "NEEDS_PROCESSING",
            "IN_PROCESS",
            "PROCESSING_ENDED",
            "PROCESSING_ENDED_COMPLETE",
            "PROCESSING_ENDED_STOPPED",
            "PROCESSING_ENDED_ABORTED",
            "PROCESSING_ENDED_LOST",
            "PROCESSING_ENDED_SKIPPED",
            "PROCESSING_ENDED_REJECTED",
            "WAITING_FOR_TRANSIT",
            "IN_TRANSIT",
            "TRANSIT_COMPLETE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ControllerModeEnum": {
      "title": "ControllerModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AUTOMATIC",
            "MANUAL",
            "MANUAL_DATA_INPUT",
            "SEMI_AUTOMATIC",
            "EDIT",
            "FEED_HOLD"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DirectionLinearEnum": {
      "title": "DirectionLinearEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "POSITIVE",
            "NEGATIVE",
            "NONE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "EndOfBarEnum": {
      "title": "EndOfBarEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "YES",
            "NO"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "LocationTypeEnum": {
      "title": "LocationTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "description": "Enumeration for Location types",
          "enum": [
            "POT",
            "STATION",
            "CRIB",
            "SPINDLE",
            "TRANSFER_POT",
            "RETURN_POT",
            "STAGING_POT",
            "REMOVAL_POT",
            "EXPIRED_POT",
            "END_EFFECTOR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ValveStateEnum": {
      "title": "ValveStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "OPEN",
            "OPENING",
            "CLOSED",
            "CLOSING"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CodeEnum": {
      "title": "CodeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "BDX",
            "LBX",
            "APMX",
            "DC",
            "DF",
            "OAL",
            "DMM",
            "H",
            "LS",
            "LUX",
            "LPR",
            "WT",
            "LF",
            "CRP",
            "L",
            "DRVA",
            "WF",
            "IC",
            "SIG",
            "KAPR",
            "PSIR",
            "N/A",
            "BS",
            "SDLx",
            "STAx",
            "DCx",
            "HF",
            "RE",
            "LFx",
            "BCH",
            "CHW",
            "W1"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ApplicationCategoryEnum": {
      "title": "ApplicationCategoryEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ASSEMBLY",
            "DEVICE",
            "HANDLING",
            "MAINTENANCE",
            "PART",
            "PROCESS",
            "INSPECTION",
            "SETUP"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CutterStatusTypeEnum": {
      "title": "CutterStatusTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "description": "Enumeration for CutterStatus values.",
          "enum": [
            "NEW",
            "AVAILABLE",
            "UNAVAILABLE",
            "ALLOCATED",
            "UNALLOCATED",
            "MEASURED",
            "RECONDITIONED",
            "USED",
            "EXPIRED",
            "BROKEN",
            "NOT_REGISTERED",
            "UNKNOWN"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateLateralEnum": {
      "title": "CompositionStateLateralEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "RIGHT",
            "LEFT",
            "TRANSITIONING"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PartDetectEnum": {
      "title": "PartDetectEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PRESENT",
            "NOT_PRESENT"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ExecutionEnum": {
      "title": "ExecutionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "READY",
            "ACTIVE",
            "INTERRUPTED",
            "FEED_HOLD",
            "STOPPED",
            "OPTIONAL_STOP",
            "PROGRAM_STOPPED",
            "PROGRAM_COMPLETED",
            "WAIT",
            "PROGRAM_OPTIONAL_STOP"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DEGREE": {
      "type": "string",
      "title": "DEGREE",
      "description": "float that represents angle in degrees."
    },
    "AvailabilityEnum": {
      "title": "AvailabilityEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AVAILABLE",
            "UNAVAILABLE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ProgramEditEnum": {
      "title": "ProgramEditEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "READY",
            "NOT_READY"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ControllerModeOverrideEnum": {
      "title": "ControllerModeOverrideEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FunctionalModeEnum": {
      "title": "FunctionalModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PRODUCTION",
            "SETUP",
            "TEARDOWN",
            "MAINTENANCE",
            "PROCESS_DEVELOPMENT"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "WaitStateEnum": {
      "title": "WaitStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "POWERING_UP",
            "POWERING_DOWN",
            "PART_LOAD",
            "PART_UNLOAD",
            "TOOL_LOAD",
            "TOOL_UNLOAD",
            "MATERIAL_LOAD",
            "MATERIAL_UNLOAD",
            "SECONDARY_PROCESS",
            "PAUSING",
            "RESUMING"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RotaryModeEnum": {
      "title": "RotaryModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "SPINDLE",
            "INDEX",
            "CONTOUR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateSwitchedEnum": {
      "title": "CompositionStateSwitchedEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "EmergencyStopEnum": {
      "title": "EmergencyStopEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ARMED",
            "TRIGGERED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "LockStateEnum": {
      "title": "LockStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "LOCKED",
            "UNLOCKED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AxisStateEnum": {
      "title": "AxisStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "HOME",
            "TRAVEL",
            "PARKED",
            "STOPPED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "InterfaceStateEnum": {
      "title": "InterfaceStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ENABLED",
            "DISABLED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RelationshipTypeEnum": {
      "title": "RelationshipTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PARENT",
            "CHILD",
            "PEER"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FormatTypeEnum": {
      "title": "FormatTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "description": "Enumeration for CuttingToolDefinition format values.",
          "enum": [
            "EXPRESS",
            "TEXT",
            "UNDEFINED",
            "XML"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionTypeEnum": {
      "title": "CompositionTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTUATOR",
            "AMPLIFIER",
            "BALLSCREW",
            "BELT",
            "BRAKE",
            "CHAIN",
            "CHOPPER",
            "CHUCK",
            "CHUTE",
            "CIRCUIT_BREAKER",
            "CLAMP",
            "COMPRESSOR",
            "DOOR",
            "DRAIN",
            "ENCODER",
            "EXPOSURE_UNIT",
            "EXTRUSION_UNIT",
            "FAN",
            "FILTER",
            "GALVANOMOTOR",
            "GRIPPER",
            "HOPPER",
            "LINEAR_POSITION_FEEDBACK",
            "MOTOR",
            "OIL",
            "POWER_SUPPLY",
            "PULLEY",
            "PUMP",
            "REEL",
            "SENSING_ELEMENT",
            "SPREADER",
            "STORAGE_BATTERY",
            "SWITCH",
            "TABLE",
            "TANK",
            "TENSIONER",
            "TRANSFORMER",
            "VALVE",
            "VAT",
            "WATER",
            "WIRE",
            "WORKPIECE",
            "COOLING_TOWER",
            "POT",
            "STATION",
            "TRANSFER_ARM",
            "TRANSFER_POT",
            "RETURN_POT",
            "STAGING_POT",
            "REMOVAL_POT",
            "EXPIRED_POT"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CountDirectionTypeEnum": {
      "title": "CountDirectionTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "description": "Enumeration for countDirection types.",
          "enum": [
            "UP",
            "DOWN"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ApplicationTypeEnum": {
      "title": "ApplicationTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "DESIGN",
            "DATA",
            "DOCUMENTATION",
            "INSTRUCTIONS",
            "LOG",
            "PRODUCTION_PROGRAM"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "FormEnum": {
      "title": "FormEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "BAR",
            "SHEET",
            "BLOCK",
            "CASTING",
            "POWDER",
            "LIQUID",
            "GEL",
            "FILAMENT",
            "GAS"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CompositionStateVerticalEnum": {
      "title": "CompositionStateVerticalEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "UP",
            "DOWN",
            "TRANSITIONING"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "RequestStateEnum": {
      "title": "RequestStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "NOT_READY",
            "READY",
            "ACTIVE",
            "FAIL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ResponseStateEnum": {
      "title": "ResponseStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "NOT_READY",
            "READY",
            "ACTIVE",
            "COMPLETE",
            "FAIL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "SpindleInterlockEnum": {
      "title": "SpindleInterlockEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AxisInterlockEnum": {
      "title": "AxisInterlockEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ChuckInterlockEnum": {
      "title": "ChuckInterlockEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ACTIVE",
            "INACTIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DoorStateEnum": {
      "title": "DoorStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "OPEN",
            "CLOSED",
            "UNLATCHED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "EquipmentModeEnum": {
      "title": "EquipmentModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PowerStateEnum": {
      "title": "PowerStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ConditionStateEnum": {
      "title": "ConditionStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "Normal",
            "Warning",
            "Fault"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ToolLifeEnum": {
      "title": "ToolLifeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "MINUTES",
            "PART_COUNT",
            "WEAR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "x509": {
      "type": "string",
      "title": "x509",
      "description": "string that represents an `x509` data block. {{cite(ISO/IEC 9594-8:2020)}}."
    },
    "unit": {
      "type": "string",
      "title": "unit"
    },
    "CUBIC_MILLIMETER": {
      "type": "string",
      "title": "CUBIC_MILLIMETER",
      "description": "float that represents volume in cubic millimeter."
    },
    "OperatingModeEnum": {
      "title": "OperatingModeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "AUTOMATIC",
            "MANUAL",
            "SEMI_AUTOMATIC"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "int32": {
      "type": "string",
      "title": "int32",
      "description": "32-bit integer."
    },
    "int64": {
      "type": "string",
      "title": "int64",
      "description": "64-bit integer."
    },
    "version": {
      "type": "string",
      "title": "version",
      "description": "series of three numeric values, separated by a decimal point, representing a {{term(major)}}, {{term(minor)}}, and {{term(patch)}} number of the MTConnect Standard."
    },
    "uint32": {
      "type": "string",
      "title": "uint32",
      "description": "32-bit unsigned integer."
    },
    "uint64": {
      "type": "string",
      "title": "uint64",
      "description": "64-bit unsigned integer."
    },
    "ErrorCodeEnum": {
      "title": "ErrorCodeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ASSET_NOT_FOUND",
            "INTERNAL_ERROR",
            "INVALID_REQUEST",
            "INVALID_URI",
            "INVALID_XPATH",
            "NO_DEVICE",
            "OUT_OF_RANGE",
            "QUERY_ERROR",
            "TOO_MANY",
            "UNAUTHORIZED",
            "UNSUPPORTED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MaintenanceListDirectionEnum": {
      "title": "MaintenanceListDirectionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "UP",
            "DOWN"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "MaintenanceListIntervalEnum": {
      "title": "MaintenanceListIntervalEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ABSOLUTE",
            "INCREMENTAL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PartCountTypeEnum": {
      "title": "PartCountTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "EACH",
            "BATCH"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AlarmSeverityEnum": {
      "title": "AlarmSeverityEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CRITICAL",
            "ERROR",
            "WARNING",
            "INFORMATION"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AlarmStateEnum": {
      "title": "AlarmStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "INSTANT",
            "ACTIVE",
            "CLEARED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "AlarmCodeEnum": {
      "title": "AlarmCodeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CRASH",
            "JAM",
            "FAILURE",
            "FAULT",
            "STALLED",
            "OVERLOAD",
            "ESTOP",
            "MATERIAL",
            "MESSAGE",
            "OTHER"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "DirectionEnum": {
      "title": "DirectionEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CLOCKWISE",
            "COUNTER_CLOCKWISE",
            "POSITIVE",
            "NEGATIVE"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "PowerStatusEnum": {
      "title": "PowerStatusEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "ON",
            "OFF"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "NetworkWirelessEnum": {
      "title": "NetworkWirelessEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "YES",
            "NO"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ProgramLocationTypeEnum": {
      "title": "ProgramLocationTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "LOCAL",
            "EXTERNAL"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "InterfaceEventEnum": {
      "title": "InterfaceEventEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "INTERFACE_STATE",
            "MATERIAL_FEED",
            "MATERIAL_CHANGE",
            "MATERIAL_RETRACT",
            "PART_CHANGE",
            "MATERIAL_LOAD",
            "MATERIAL_UNLOAD",
            "OPEN_CHUCK",
            "OPEN_DOOR",
            "CLOSE_CHUCK",
            "CLOSE_DOOR"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "LeakDetectEnum": {
      "title": "LeakDetectEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "DETECTED",
            "NOT_DETECTED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "BatteryStateEnum": {
      "title": "BatteryStateEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "CHARGED",
            "CHARGING",
            "DISCHARGING",
            "DISCHARGED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "binary": {
      "type": "string",
      "title": "binary",
      "description": "base-2 numeral system or binary numeral system represented by two digits: \"0\" and \"1\"."
    },
    "SensorStateDetectEnum": {
      "title": "SensorStateDetectEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "DETECTED",
            "NOT_DETECTED",
            "UNKNOWN"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "CharacteristicStatusEnum": {
      "title": "CharacteristicStatusEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PASS",
            "FAIL",
            "REWORK",
            "SYSTEM_ERROR",
            "INDETERMINATE",
            "NOT_ANALYZED",
            "BASIC_OR_THEORETIC_EXACT_DIMENSION",
            "UNDEFINED"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "UncertaintyTypeEnum": {
      "title": "UncertaintyTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "COMBINED",
            "MEAN"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "double": {
      "type": "number",
      "title": "double",
      "description": "primitive type."
    },
    "Array": {
      "type": "string",
      "title": "Array",
      "description": "array."
    },
    "float3d": {
      "type": "string",
      "title": "float3d",
      "description": "array of size 3 and datatype float."
    },
    "PowerSourceTypeEnum": {
      "title": "PowerSourceTypeEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "PRIMARY",
            "SECONDARY",
            "STANDBY"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "UUID": {
      "type": "string",
      "title": "UUID",
      "description": "Universally Unique IDentifier. {{cite(IETF:RFC-4122)}}"
    },
    "METER": {
      "type": "string",
      "title": "METER",
      "description": "float that represents measurement in meter."
    },
    "QualityEnum": {
      "title": "QualityEnum",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "VALID",
            "UNVERIFIABLE",
            "INVALID"
          ]
        },
        {
          "type": "string",
          "pattern": "^[a-ln-zA-LN-Z]:[A-Z_][A-Z0-9_]*$"
        }
      ]
    },
    "ControlLimitResult": {
      "type": "object",
      "title": "ControlLimitResult",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "UpperLimit": {
          "type": "number",
          "description": "upper conformance boundary for a variable. > Note: immediate concern or action may be required."
        },
        "UpperWarning": {
          "type": "number",
          "description": "upper boundary indicating increased concern and supervision may be required."
        },
        "Nominal": {
          "type": "number",
          "description": "ideal or desired value for a variable."
        },
        "LowerWarning": {
          "type": "number",
          "description": "lower boundary indicating increased concern and supervision may be required."
        },
        "LowerLimit": {
          "type": "number",
          "description": "lower conformance boundary for a variable. > Note: immediate concern or action may be required."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "SpecificationLimitResult": {
      "type": "object",
      "title": "SpecificationLimitResult",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "UpperLimit": {
          "type": "number",
          "description": "upper conformance boundary for a variable. > Note: immediate concern or action may be required."
        },
        "Nominal": {
          "type": "number",
          "description": "ideal or desired value for a variable."
        },
        "LowerLimit": {
          "type": "number",
          "description": "lower conformance boundary for a variable. > Note: immediate concern or action may be required."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "AlarmLimitResult": {
      "type": "object",
      "title": "AlarmLimitResult",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "UpperLimit": {
          "type": "number",
          "description": "upper conformance boundary for a variable. > Note: immediate concern or action may be required."
        },
        "UpperWarning": {
          "type": "number",
          "description": "upper boundary indicating increased concern and supervision may be required."
        },
        "LowerWarning": {
          "type": "number",
          "description": "lower boundary indicating increased concern and supervision may be required."
        },
        "LowerLimit": {
          "type": "number",
          "description": "lower conformance boundary for a variable. > Note: immediate concern or action may be required."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "SensorAttachmentResult": {
      "type": "object",
      "title": "SensorAttachmentResult",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "SensorId": {
          "type": "string",
          "description": "identity of a sensor used to observe some measurement of an item."
        }
      },
      "required": [
        "SensorId",
        "count"
      ],
      "additionalProperties": false
    },
    "MaintenanceListResult": {
      "type": "object",
      "title": "MaintenanceListResult",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{termplural(key-value pair)}} represented as {{block(Entry)}} entities."
        },
        "Value": {
          "type": "number",
          "description": "current interval value of the activity."
        },
        "Interval": {
          "$ref": "#/$defs/MaintenanceListIntervalEnum",
          "description": "interval of the value observed.",
          "default": "ABSOLUTE"
        },
        "NextServiceDate": {
          "type": "string",
          "description": "next date/time stamp that maintenance should be performed."
        },
        "Reset": {
          "type": "string",
          "description": "last date/time stamp of the {{term(observation)}} was reset."
        },
        "Severity": {
          "type": "integer",
          "description": "level of severity on a scale of 1-10."
        },
        "Direction": {
          "$ref": "#/$defs/MaintenanceListDirectionEnum",
          "description": "direction of the value observed.",
          "default": "UP"
        },
        "Name": {
          "type": "string",
          "description": "identifier of the maintenance activity."
        },
        "LastServiceDate": {
          "type": "string",
          "description": "last date/time stamp that maintenance was performed."
        },
        "Units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "same as {{property(DataItem::units)}}. See {{package(Device Information Model)}}.",
          "default": "COUNT"
        },
        "Target": {
          "type": "number",
          "description": "target value of the next maintenance."
        }
      },
      "required": [
        "Name",
        "Value",
        "count"
      ],
      "additionalProperties": false
    },
    "FeatureMeasurementResult": {
      "type": "object",
      "title": "FeatureMeasurementResult",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{termplural(key-value pair)}} represented as {{block(Entry)}} entities."
        },
        "MeasurementId": {
          "type": "string",
          "description": "identifier of this measurement."
        },
        "FeaturePersistentId": {
          "type": "string",
          "description": "{{term(UUID)}} of the feature."
        },
        "CharacteristicPersistentId": {
          "type": "string",
          "description": "{{term(UUID)}} of the characteristic."
        },
        "MeasurementType": {
          "type": "string",
          "description": "class of measurement being performed. {{cite(QIF 3:2018 Section 6.3)}} Examples: `POINT`, `RADIUS`, `ANGLE`, `LENGTH`, etc."
        },
        "MeasurementValue": {
          "type": "number",
          "description": "measurement based on the measurement type."
        },
        "MeasurementUnits": {
          "type": "string",
          "description": "engineering units of the measurement."
        },
        "CharacteristicStatus": {
          "$ref": "#/$defs/CharacteristicStatusEnum",
          "description": "pass/fail result of the measurement."
        },
        "UncertaintyType": {
          "$ref": "#/$defs/UncertaintyTypeEnum",
          "description": "method used to compute {{term(standard uncertainty)}}."
        },
        "Uncertainty": {
          "type": "number",
          "description": "{{term(uncertainty)}} specified by `UNCERTAINTY_TYPE`."
        }
      },
      "required": [
        "FeaturePersistentId",
        "MeasurementId",
        "MeasurementType",
        "MeasurementUnits",
        "MeasurementValue",
        "count"
      ],
      "additionalProperties": false
    },
    "LocationAddressResult": {
      "type": "object",
      "title": "LocationAddressResult",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "GivenName": {
          "type": "string",
          "description": "element specifying the name used to distinguish between persons having the same surname(s) and who may have access to a particular delivery point."
        },
        "Surname": {
          "type": "string",
          "description": "element which identifies the family or parentage of an individual."
        },
        "Qualification": {
          "type": "string",
          "description": "element indicating an individual’s professional or academic qualification or rank in a professional group or society (e.g. PhD, Fellow of the Royal Society, FRS, Barrister at Law)."
        },
        "NameQualifier": {
          "type": "string",
          "description": "element used in some countries to distinguish between persons with the same surname(s) who have similar given names or initials (e.g.III, Senior, the Third.)."
        },
        "OrganizationName": {
          "type": "string",
          "description": "element giving the official name, the registered business name or other official designation of an organization."
        },
        "LegalStatus": {
          "type": "string",
          "description": "element indicating the formal registration of an organization (e.g. GmbH, Inc., Ltd.)."
        },
        "OrganizationUnit": {
          "type": "string",
          "description": "element identifying a subdivision of an organization."
        },
        "Building": {
          "type": "string",
          "description": "element identifying the number or name and type of the edifice or construction in or adjacent to which a delivery point is located."
        },
        "Floor": {
          "type": "string",
          "description": "element indicating the floor or level on which a delivery point is located in a multi-story building."
        },
        "Door": {
          "type": "string",
          "description": "element indicating the apartment, room or office in, at or adjacent to which a delivery point, situated within a building, is located."
        },
        "Thoroughfare": {
          "type": "string",
          "description": "element which identifies the road or part of a road or other access route along which a delivery point can be accessed, either directly or via a secondary or tertiary road or access route."
        },
        "PremiseIdentifier": {
          "type": "string",
          "description": "element designating the area or the object on an area, adjacent to thoroughfare, in which the delivery point or delivery point access is located."
        },
        "Town": {
          "type": "string",
          "description": "element indicating the name of the populated place in which a delivery point is located, or the populated."
        },
        "Region": {
          "type": "string",
          "description": "element specifying the geographic or administrative area of the country in which the town is situated."
        },
        "District": {
          "type": "string",
          "description": "element indicating the name of the area within or adjacent to the town in which a delivery point is located, or via which it is accessed."
        },
        "CountryName": {
          "type": "string",
          "description": "element designating the country, dependency or area of geopolitical interest, in which a delivery point is located or via which the delivery point is accessed."
        },
        "CountryCode": {
          "type": "string",
          "description": "interest, in which a delivery point is located or via which the delivery point is accessed."
        },
        "PostCode": {
          "type": "string",
          "description": "element designating the code used for the sorting of mail."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "LocationSpatialGeographicResult": {
      "type": "object",
      "title": "LocationSpatialGeographicResult",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "Longitude": {
          "type": "string",
          "description": "geographic longitude."
        },
        "Latitude": {
          "type": "string",
          "description": "geographic latitude."
        },
        "Altitude": {
          "type": "string",
          "description": "height relative to a reference."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "TranslationDataSet": {
      "type": "object",
      "title": "TranslationDataSet",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "X": {
          "type": "string",
          "description": "translation along X axis."
        },
        "Y": {
          "type": "string",
          "description": "translation along Y axis."
        },
        "Z": {
          "type": "string",
          "description": "translation along Z axis."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "RotationDataSet": {
      "type": "object",
      "title": "RotationDataSet",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "X": {
          "type": "string",
          "description": "rotation about X axis."
        },
        "Y": {
          "type": "string",
          "description": "rotation about Y axis."
        },
        "Z": {
          "type": "string",
          "description": "rotation about Z axis."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "ScaleDataSet": {
      "type": "object",
      "title": "ScaleDataSet",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "X": {
          "type": "number",
          "description": "multiplier for X axis."
        },
        "Y": {
          "type": "number",
          "description": "multiplier for Y axis."
        },
        "Z": {
          "type": "number",
          "description": "multiplier for Z axis."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "AxisDataSet": {
      "type": "object",
      "title": "AxisDataSet",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "X": {
          "type": "number",
          "description": "x-component of {{block(Axis)}}."
        },
        "Y": {
          "type": "number",
          "description": "y-component of {{block(Axis)}}."
        },
        "Z": {
          "type": "number",
          "description": "z-component of {{block(Axis)}}."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "OriginDataSet": {
      "type": "object",
      "title": "OriginDataSet",
      "properties": {
        "count": {
          "type": "integer",
          "description": "number of {{block(Entry)}} elements for the {{block(Observation)}}."
        },
        "X": {
          "type": "string",
          "description": "x-coordinate."
        },
        "Y": {
          "type": "string",
          "description": "y-coordinate."
        },
        "Z": {
          "type": "string",
          "description": "x-coordinate."
        }
      },
      "required": [
        "count"
      ],
      "additionalProperties": false
    },
    "organizer": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "deprecated": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "extensible": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "informative": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "normative": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "observes": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "satisfiedBy": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "hasFormatSpecificRepresentation": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "valueType": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "isArray": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "MTConnectRequirementSpecification": {
      "description": "Unsupported UML kind: uml:Stereotype"
    },
    "Header": {
      "type": "object",
      "title": "Header",
      "description": "provides information from an {{term(agent)}} defining version information, storage capacity, and parameters associated with the data management within the {{term(agent)}}.",
      "properties": {
        "schemaVersion": {
          "type": "string"
        },
        "bufferSize": {
          "type": "integer",
          "minimum": 0
        },
        "nextSequence": {
          "type": "integer"
        },
        "lastSequence": {
          "type": "integer"
        },
        "firstSequence": {
          "type": "integer"
        },
        "deviceModelChangeTime": {
          "type": "string",
          "description": "timestamp of the last update of the {{block(Device)}} information for any device."
        },
        "assetBufferSize": {
          "type": "integer",
          "minimum": 0,
          "description": "maximum number of {{block(Asset)}} types that can be stored in the {{term(agent)}} that published the {{term(response document)}}. > Note: The implementer is responsible for allocating the appropriate amount of storage capacity required to accommodate the {{property(Header::assetBufferSize)}}."
        },
        "assetCount": {
          "type": "integer",
          "minimum": 0,
          "description": "current number of {{block(Asset)}} that are currently stored in the {{term(agent)}} as of the {{property(Header::creationTime)}} that the {{term(agent)}} published the {{term(response document)}}. {{property(Header::assetCount)}} **MUST NOT** be larger than the value reported for {{property(Header::assetBufferSize)}}."
        },
        "creationTime": {
          "type": "string",
          "description": "timestamp that an {{term(agent)}} published the {{term(response document)}}."
        },
        "instanceId": {
          "type": "integer",
          "description": "identifier for a specific instantiation of the {{term(buffer)}} associated with the {{term(agent)}} that published the {{term(response document)}}. {{property(Header::instanceId)}} **MUST** be changed to a different unique number each time the {{term(buffer)}} is cleared and a new set of data begins to be collected."
        },
        "sender": {
          "type": "string",
          "description": "identification defining where the {{term(agent)}} that published the {{term(response document)}} is installed or hosted. {{property(Header::sender)}} **MUST** be either an IP Address or Hostname describing where the {{term(agent)}} is installed or the URL of the {{term(agent)}}; e.g., `http://<address>[:port]/`. > Note: The port number need not be specified if it is the default HTTP port 80."
        },
        "testIndicator": {
          "type": "boolean",
          "description": "indicates whether the {{term(agent)}} that published the {{term(response document)}} is operating in a test mode. If {{property(Header::testIndicator)}} is not specified, the value for {{property(Header::testIndicator)}} **MUST** be interpreted to be `false`.",
          "default": "false"
        },
        "version": {
          "type": "string",
          "description": "{{term(major)}}, {{term(minor)}}, and {{term(revision)}} number of the MTConnect Standard that defines the {{term(semantic data model)}} that represents the content of the {{term(response document)}}. It also includes the revision number of the {{term(schema)}} associated with that specific {{term(semantic data model)}}. As an example, the value reported for {{property(Header::version)}} for a {{term(response document)}} that was structured based on {{term(schema)}} revision 10 associated with Version 1.4.0 of the MTConnect Standard would be: 1.4.0.10"
        },
        "validation": {
          "type": "boolean",
          "description": "indicates if the {{term(MTConnect Agent)}} is validating against the normative model.",
          "default": "false"
        }
      },
      "required": [
        "creationTime",
        "deviceModelChangeTime",
        "instanceId",
        "sender",
        "version"
      ],
      "additionalProperties": false
    },
    "MTConnectStreams": {
      "type": "object",
      "title": "MTConnectStreams",
      "description": "root entity of an {{term(MTConnectStreams Response Document)}} that contains the {{term(Observation Information Model)}} of one or more {{block(Device)}} entities. ![MTConnectStreams](figures/MTConnectStreams.png \"MTConnectStreams\"){: width=\"0.8\"} > Note: Additional properties of {{block(MTConnectStreams)}} **MAY** be defined for schema and namespace declaration. See {{sect(Schema and Namespace Declaration Information)}} for an {{term(XML)}} example.",
      "properties": {
        "jsonVersion": {
          "type": "integer"
        },
        "schemaVersion": {
          "type": "string"
        },
        "Header": {
          "$ref": "#/$defs/Header"
        },
        "Streams": {
          "type": "object",
          "properties": {
            "DeviceStream": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/DeviceStream"
              }
            }
          },
          "additionalProperties": false
        }
      },
      "required": [
        "Header"
      ],
      "additionalProperties": false
    },
    "Error": {
      "type": "object",
      "title": "Error",
      "description": "error encountered by an {{term(agent)}} when responding to a {{term(request)}}.",
      "properties": {
        "errorCode": {
          "$ref": "#/$defs/ErrorCodeEnum",
          "description": "descriptive code that indicates the type of error that was encountered by an {{term(agent)}}."
        },
        "value": {
          "type": "string",
          "description": "textual description of the error and any additional information an {{term(agent)}} is capable of providing regarding a specific error."
        }
      },
      "required": [
        "errorCode"
      ],
      "additionalProperties": false
    },
    "Agent": {
      "type": "object",
      "title": "Agent",
      "description": "{{term(agent)}}. An {{term(agent)}} **MUST** perform the following tasks: * Collect data from manufacturing equipment. * Generate {{termplural(response document)}}. * Provide a REST interface using {{term(HTTP)}}. In addition to {{term(XML)}} and {{term(HTTP)}}, An {{term(agent)}} **MAY** provide additional protocols and representations. Some representations **MAY** have companion specifications.",
      "properties": {
        "mtconnectVersion": {
          "type": "string"
        },
        "instanceId": {
          "type": "string",
          "description": "identifier for an {{term(instance)}} of the {{term(agent)}}. {{property(Header::instanceId)}} **MUST** be changed to a different unique number each time the {{term(buffer)}} is cleared and a new set of data begins to be collected."
        },
        "sequenceNumber": {
          "type": "string",
          "description": "{{term(sequence number)}}."
        },
        "bufferSize": {
          "type": "string",
          "description": "maximum number of {{termplural(Observation)}} that **MAY** be retained in the {{term(agent)}} that published the {{term(response document)}} at any point in time."
        },
        "maxAssets": {
          "type": "string",
          "description": "maximum number of {{termplural(Asset)}} that **MAY** be retained in the {{term(agent)}} that published the {{term(response document)}} at any point in time."
        },
        "assetCount": {
          "type": "string",
          "description": "current number of {{termplural(Asset)}} that are currently stored in the {{term(agent)}} as of the {{property(Header::creationTime)}} that the {{term(agent)}} published the {{term(response document)}}."
        }
      },
      "required": [
        "assetCount",
        "bufferSize",
        "instanceId",
        "maxAssets",
        "sequenceNumber"
      ],
      "additionalProperties": false
    },
    "SampleTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Observation"
        },
        {
          "type": "object",
          "title": "Sample",
          "description": "{{block(Observation)}} that is continuously changing or analog data value.",
          "properties": {
            "duration": {
              "type": "number",
              "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
            },
            "resetTriggered": {
              "$ref": "#/$defs/ResetTriggeredEnum",
              "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
            },
            "sampleRate": {
              "type": "number",
              "description": "rate at which successive samples of the value are recorded."
            },
            "statistic": {
              "$ref": "#/$defs/StatisticEnum",
              "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
            },
            "value": {},
            "units": {
              "$ref": "#/$defs/UnitEnum"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum"
            }
          },
          "required": [
            "value"
          ]
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "SampleTimeSeries"
    },
    "AccelerationTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Acceleration",
          "description": "{{def(SampleEnum::ACCELERATION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ACCELERATION"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "AccelerationTimeSeries"
    },
    "AccumulatedTimeTimeSeries": {
      "type": "object",
      "title": "AccumulatedTimeTimeSeries",
      "description": "{{def(SampleEnum::ACCUMULATED_TIME)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "ACCUMULATED_TIME"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AmperageTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Amperage",
          "description": "{{def(SampleEnum::AMPERAGE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "AMPERAGE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "AmperageTimeSeries"
    },
    "AngleTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Angle",
          "description": "{{def(SampleEnum::ANGLE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ANGLE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "AngleTimeSeries"
    },
    "AngularAccelerationTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AngularAcceleration",
          "description": "{{def(SampleEnum::ANGULAR_ACCELERATION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ANGULAR_ACCELERATION"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "AngularAccelerationTimeSeries"
    },
    "AngularVelocityTimeSeries": {
      "type": "object",
      "title": "AngularVelocityTimeSeries",
      "description": "{{def(SampleEnum::ANGULAR_VELOCITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "ANGULAR_VELOCITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "DEGREE/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AxisFeedrateTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AxisFeedrate",
          "description": "{{def(SampleEnum::AXIS_FEEDRATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "AXIS_FEEDRATE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "AxisFeedrateTimeSeries"
    },
    "CapacityFluidTimeSeries": {
      "type": "object",
      "title": "CapacityFluidTimeSeries",
      "description": "{{def(SampleEnum::CAPACITY_FLUID)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "CAPACITY_FLUID"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLILITER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "CapacitySpatialTimeSeries": {
      "type": "object",
      "title": "CapacitySpatialTimeSeries",
      "description": "{{def(SampleEnum::CAPACITY_SPATIAL)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "CAPACITY_SPATIAL"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ConcentrationTimeSeries": {
      "type": "object",
      "title": "ConcentrationTimeSeries",
      "description": "{{def(SampleEnum::CONCENTRATION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "CONCENTRATION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ConductivityTimeSeries": {
      "type": "object",
      "title": "ConductivityTimeSeries",
      "description": "{{def(SampleEnum::CONDUCTIVITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "CONDUCTIVITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "SIEMENS/METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "CuttingSpeedTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "CuttingSpeed",
          "description": "{{def(SampleEnum::CUTTING_SPEED)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "CUTTING_SPEED"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "CuttingSpeedTimeSeries"
    },
    "DensityTimeSeries": {
      "type": "object",
      "title": "DensityTimeSeries",
      "description": "{{def(SampleEnum::DENSITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DENSITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIGRAM/CUBIC_MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DepositionAccelerationVolumetricTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionAccelerationVolumetric",
          "description": "{{def(SampleEnum::DEPOSITION_ACCELERATION_VOLUMETRIC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "CUBIC_MILLIMETER/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_ACCELERATION_VOLUMETRIC"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "DepositionAccelerationVolumetricTimeSeries"
    },
    "DepositionDensityTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionDensity",
          "description": "{{def(SampleEnum::DEPOSITION_DENSITY)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIGRAM/CUBIC_MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_DENSITY"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "DepositionDensityTimeSeries"
    },
    "DepositionMassTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionMass",
          "description": "{{def(SampleEnum::DEPOSITION_MASS)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIGRAM"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_MASS"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "DepositionMassTimeSeries"
    },
    "DepositionRateVolumetricTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionRateVolumetric",
          "description": "{{def(SampleEnum::DEPOSITION_RATE_VOLUMETRIC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "CUBIC_MILLIMETER/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_RATE_VOLUMETRIC"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "DepositionRateVolumetricTimeSeries"
    },
    "DepositionVolumeTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DepositionVolume",
          "description": "{{def(SampleEnum::DEPOSITION_VOLUME)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "CUBIC_MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DEPOSITION_VOLUME"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "DepositionVolumeTimeSeries"
    },
    "DisplacementTimeSeries": {
      "type": "object",
      "title": "DisplacementTimeSeries",
      "description": "{{def(SampleEnum::DISPLACEMENT)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DISPLACEMENT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ElectricalEnergyTimeSeries": {
      "type": "object",
      "title": "ElectricalEnergyTimeSeries",
      "description": "{{def(SampleEnum::ELECTRICAL_ENERGY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "ELECTRICAL_ENERGY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "WATT_SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "EquipmentTimerTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "EquipmentTimer",
          "description": "{{def(SampleEnum::EQUIPMENT_TIMER)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "EQUIPMENT_TIMER"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "EquipmentTimerTimeSeries"
    },
    "FillLevelTimeSeries": {
      "type": "object",
      "title": "FillLevelTimeSeries",
      "description": "{{def(SampleEnum::FILL_LEVEL)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "FILL_LEVEL"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "FlowTimeSeries": {
      "type": "object",
      "title": "FlowTimeSeries",
      "description": "{{def(SampleEnum::FLOW)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "FLOW"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "LITER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "FrequencyTimeSeries": {
      "type": "object",
      "title": "FrequencyTimeSeries",
      "description": "{{def(SampleEnum::FREQUENCY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "FREQUENCY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "HERTZ"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "GlobalPositionTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "GlobalPosition",
          "description": "{{def(SampleEnum::GLOBAL_POSITION)}}",
          "properties": {
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "GLOBAL_POSITION"
            },
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "GlobalPositionTimeSeries"
    },
    "LengthTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Length",
          "description": "{{def(SampleEnum::LENGTH)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "LENGTH"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "LengthTimeSeries"
    },
    "LevelTimeSeries": {
      "type": "object",
      "title": "LevelTimeSeries",
      "description": "{{def(SampleEnum::LEVEL)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "LEVEL"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "LinearForceTimeSeries": {
      "type": "object",
      "title": "LinearForceTimeSeries",
      "description": "{{def(SampleEnum::LINEAR_FORCE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "LINEAR_FORCE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "NEWTON"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "LoadTimeSeries": {
      "type": "object",
      "title": "LoadTimeSeries",
      "description": "{{def(SampleEnum::LOAD)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "LOAD"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "MassTimeSeries": {
      "type": "object",
      "title": "MassTimeSeries",
      "description": "{{def(SampleEnum::MASS)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "MASS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "KILOGRAM"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PathFeedrateTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "PathFeedrate",
          "description": "{{def(SampleEnum::PATH_FEEDRATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PATH_FEEDRATE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "PathFeedrateTimeSeries"
    },
    "PathFeedratePerRevolutionTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "PathFeedratePerRevolution",
          "description": "{{def(SampleEnum::PATH_FEEDRATE_PER_REVOLUTION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/REVOLUTION"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PATH_FEEDRATE_PER_REVOLUTION"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "PathFeedratePerRevolutionTimeSeries"
    },
    "PHTimeSeries": {
      "type": "object",
      "title": "PHTimeSeries",
      "description": "{{def(SampleEnum::PH)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "PH"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PH"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PositionTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Position",
          "description": "{{def(SampleEnum::POSITION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "POSITION"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "PositionTimeSeries"
    },
    "PowerFactorTimeSeries": {
      "type": "object",
      "title": "PowerFactorTimeSeries",
      "description": "{{def(SampleEnum::POWER_FACTOR)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "POWER_FACTOR"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ProcessTimerTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "ProcessTimer",
          "description": "{{def(SampleEnum::PROCESS_TIMER)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PROCESS_TIMER"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "ProcessTimerTimeSeries"
    },
    "ResistanceTimeSeries": {
      "type": "object",
      "title": "ResistanceTimeSeries",
      "description": "{{def(SampleEnum::RESISTANCE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "RESISTANCE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "OHM"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "RotaryVelocityTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "RotaryVelocity",
          "description": "{{def(SampleEnum::ROTARY_VELOCITY)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "REVOLUTION/MINUTE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ROTARY_VELOCITY"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "RotaryVelocityTimeSeries"
    },
    "SoundLevelTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SoundLevel",
          "description": "{{def(SampleEnum::SOUND_LEVEL)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DECIBEL"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SOUND_LEVEL"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "A_SCALE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "SoundLevelTimeSeries"
    },
    "SpindleSpeedTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SpindleSpeed",
          "description": "{{def(SampleEnum::SPINDLE_SPEED)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "REVOLUTION/MINUTE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SPINDLE_SPEED"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "SpindleSpeedTimeSeries"
    },
    "StrainTimeSeries": {
      "type": "object",
      "title": "StrainTimeSeries",
      "description": "{{def(SampleEnum::STRAIN)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "STRAIN"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "TemperatureTimeSeries": {
      "type": "object",
      "title": "TemperatureTimeSeries",
      "description": "{{def(SampleEnum::TEMPERATURE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "TEMPERATURE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "CELSIUS"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "TensionTimeSeries": {
      "type": "object",
      "title": "TensionTimeSeries",
      "description": "{{def(SampleEnum::TENSION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "TENSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "NEWTON"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "TiltTimeSeries": {
      "type": "object",
      "title": "TiltTimeSeries",
      "description": "{{def(SampleEnum::TILT)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "TILT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MICRO_RADIAN"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "TorqueTimeSeries": {
      "type": "object",
      "title": "TorqueTimeSeries",
      "description": "{{def(SampleEnum::TORQUE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "TORQUE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "NEWTON_METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VelocityTimeSeries": {
      "type": "object",
      "title": "VelocityTimeSeries",
      "description": "{{def(SampleEnum::VELOCITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "VELOCITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ViscosityTimeSeries": {
      "type": "object",
      "title": "ViscosityTimeSeries",
      "description": "{{def(SampleEnum::VISCOSITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "VISCOSITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PASCAL_SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VoltageTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Voltage",
          "description": "{{def(SampleEnum::VOLTAGE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "VOLT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLTAGE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "VoltageTimeSeries"
    },
    "VoltAmpereTimeSeries": {
      "type": "object",
      "title": "VoltAmpereTimeSeries",
      "description": "{{def(SampleEnum::VOLT_AMPERE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "VOLT_AMPERE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "VOLT_AMPERE"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VoltAmpereReactiveTimeSeries": {
      "type": "object",
      "title": "VoltAmpereReactiveTimeSeries",
      "description": "{{def(SampleEnum::VOLT_AMPERE_REACTIVE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "VOLT_AMPERE_REACTIVE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "VOLT_AMPERE_REACTIVE"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VolumeFluidTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "VolumeFluid",
          "description": "{{def(SampleEnum::VOLUME_FLUID)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLILITER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLUME_FLUID"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "VolumeFluidTimeSeries"
    },
    "VolumeSpatialTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "VolumeSpatial",
          "description": "{{def(SampleEnum::VOLUME_SPATIAL)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "CUBIC_MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLUME_SPATIAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "VolumeSpatialTimeSeries"
    },
    "WattageTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Wattage",
          "description": "{{def(SampleEnum::WATTAGE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "WATT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "WATTAGE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "WattageTimeSeries"
    },
    "AmperageDCTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AmperageDC",
          "description": "{{def(SampleEnum::AMPERAGE_DC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "AMPERAGE_DC"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "AmperageDCTimeSeries"
    },
    "AmperageACTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AmperageAC",
          "description": "{{def(SampleEnum::AMPERAGE_AC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "AMPERAGE_AC"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "AmperageACTimeSeries"
    },
    "VoltageACTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "VoltageAC",
          "description": "{{def(SampleEnum::VOLTAGE_AC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "VOLT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLTAGE_AC"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "VoltageACTimeSeries"
    },
    "VoltageDCTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "VoltageDC",
          "description": "{{def(SampleEnum::VOLTAGE_DC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "VOLT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "VOLTAGE_DC"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          },
          "required": [
            "subType"
          ]
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "VoltageDCTimeSeries"
    },
    "XDimensionTimeSeries": {
      "type": "object",
      "title": "XDimensionTimeSeries",
      "description": "{{def(SampleEnum::X_DIMENSION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "X_DIMENSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "YDimensionTimeSeries": {
      "type": "object",
      "title": "YDimensionTimeSeries",
      "description": "{{def(SampleEnum::Y_DIMENSION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "Y_DIMENSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ZDimensionTimeSeries": {
      "type": "object",
      "title": "ZDimensionTimeSeries",
      "description": "{{def(SampleEnum::Z_DIMENSION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "Z_DIMENSION"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DiameterTimeSeries": {
      "type": "object",
      "title": "DiameterTimeSeries",
      "description": "{{def(SampleEnum::DIAMETER)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DIAMETER"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "HumidityRelativeTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "HumidityRelative",
          "description": "{{def(SampleEnum::HUMIDITY_RELATIVE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "PERCENT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "HUMIDITY_RELATIVE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "HumidityRelativeTimeSeries"
    },
    "HumidityAbsoluteTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "HumidityAbsolute",
          "description": "{{def(SampleEnum::HUMIDITY_ABSOLUTE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "GRAM/CUBIC_METER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "HUMIDITY_ABSOLUTE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "HumidityAbsoluteTimeSeries"
    },
    "HumiditySpecificTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "HumiditySpecific",
          "description": "{{def(SampleEnum::HUMIDITY_SPECIFIC)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "PERCENT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "HUMIDITY_SPECIFIC"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "HumiditySpecificTimeSeries"
    },
    "PressurizationRateTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "PressurizationRate",
          "description": "{{def(SampleEnum::PRESSURIZATION_RATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "PASCAL/SECOND"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PRESSURIZATION_RATE"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "PressurizationRateTimeSeries"
    },
    "DecelerationTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "Deceleration",
          "description": "{{def(SampleEnum::DECELERATION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DECELERATION"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "DecelerationTimeSeries"
    },
    "AssetUpdateRateTimeSeries": {
      "type": "object",
      "title": "AssetUpdateRateTimeSeries",
      "description": "{{def(SampleEnum::ASSET_UPDATE_RATE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "ASSET_UPDATE_RATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "COUNT/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "AngularDecelerationTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "AngularDeceleration",
          "description": "{{def(SampleEnum::ANGULAR_DECELERATION)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE/SECOND\\^2"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "ANGULAR_DECELERATION"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "AngularDecelerationTimeSeries"
    },
    "ObservationUpdateRateTimeSeries": {
      "type": "object",
      "title": "ObservationUpdateRateTimeSeries",
      "description": "{{def(SampleEnum::OBSERVATION_UPDATE_RATE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "OBSERVATION_UPDATE_RATE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "COUNT/SECOND"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "PressureAbsoluteTimeSeries": {
      "type": "object",
      "title": "PressureAbsoluteTimeSeries",
      "description": "The force per unit area measured relative to a vacuum.",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "PRESSURE_ABSOLUTE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PASCAL"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "OpennessTimeSeries": {
      "type": "object",
      "title": "OpennessTimeSeries",
      "description": "{{def(SampleEnum::OPENNESS)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "OPENNESS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "PERCENT"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DewPointTimeSeries": {
      "type": "object",
      "title": "DewPointTimeSeries",
      "description": "{{def(SampleEnum::DEW_POINT)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DEW_POINT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "CELSIUS"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "GravitationalForceTimeSeries": {
      "type": "object",
      "title": "GravitationalForceTimeSeries",
      "description": "{{def(SampleEnum::GRAVITATIONAL_FORCE)}} > Note: $$Mass\\times GravitationalAcceleration$$",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "GRAVITATIONAL_FORCE"
        },
        "units": {
          "$ref": "#/$defs/NativeUnitEnum",
          "default": "GRAVITATIONAL_FORCE"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "GravitationalAccelerationTimeSeries": {
      "type": "object",
      "title": "GravitationalAccelerationTimeSeries",
      "description": "{{def(SampleEnum::GRAVITATIONAL_ACCELERATION)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "GRAVITATIONAL_ACCELERATION"
        },
        "units": {
          "$ref": "#/$defs/NativeUnitEnum",
          "default": "GRAVITATIONAL_ACCELERATION"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "BatteryCapacityTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "BatteryCapacity",
          "description": "{{def(SampleEnum::BATTERY_CAPACITY)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "COULOMB"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "BATTERY_CAPACITY"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "BatteryCapacityTimeSeries"
    },
    "DischargeRateTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "DischargeRate",
          "description": "{{def(SampleEnum::DISCHARGE_RATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "DISCHARGE_RATE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "DischargeRateTimeSeries"
    },
    "ChargeRateTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "ChargeRate",
          "description": "{{def(SampleEnum::CHARGE_RATE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "AMPERE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "CHARGE_RATE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "ChargeRateTimeSeries"
    },
    "BatteryChargeTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "BatteryCharge",
          "description": "{{def(SampleEnum::BATTERY_CHARGE)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "PERCENT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "BATTERY_CHARGE"
            },
            "subType": {
              "$ref": "#/$defs/DataItemSubTypeEnum",
              "default": "ACTUAL"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "BatteryChargeTimeSeries"
    },
    "SettlingErrorTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SettlingError",
          "description": "{{def(SampleEnum::SETTLING_ERROR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "COUNT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SETTLING_ERROR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "SettlingErrorTimeSeries"
    },
    "SettlingErrorLinearTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SettlingErrorLinear",
          "description": "{{def(SampleEnum::SETTLING_ERROR_LINEAR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SETTLING_ERROR_LINEAR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "SettlingErrorLinearTimeSeries"
    },
    "SettlingErrorAngularTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "SettlingErrorAngular",
          "description": "{{def(SampleEnum::SETTLING_ERROR_ANGULAR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "SETTLING_ERROR_ANGULAR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "SettlingErrorAngularTimeSeries"
    },
    "FollowingErrorTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "FollowingError",
          "description": "{{def(SampleEnum::FOLLOWING_ERROR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "COUNT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "FOLLOWING_ERROR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "FollowingErrorTimeSeries"
    },
    "FollowingErrorAngularTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "FollowingErrorAngular",
          "description": "{{def(SampleEnum::FOLLOWING_ERROR_ANGULAR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "DEGREE"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "FOLLOWING_ERROR_ANGULAR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "FollowingErrorAngularTimeSeries"
    },
    "FollowingErrorLinearTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "FollowingErrorLinear",
          "description": "{{def(SampleEnum::FOLLOWING_ERROR_LINEAR)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "FOLLOWING_ERROR_LINEAR"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "FollowingErrorLinearTimeSeries"
    },
    "DisplacementLinearTimeSeries": {
      "type": "object",
      "title": "DisplacementLinearTimeSeries",
      "description": "{{def(SampleEnum::DISPLACEMENT_LINEAR)}} > Note: The displacement vector **MAY** be defined by the motion of the owning {{block(Component)}}.",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DISPLACEMENT_LINEAR"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "DisplacementAngularTimeSeries": {
      "type": "object",
      "title": "DisplacementAngularTimeSeries",
      "description": "{{def(SampleEnum::DISPLACEMENT_ANGULAR)}} > Note: The displacement vector **MAY** be defined by the motion of the owning {{block(Component)}}.",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "DISPLACEMENT_ANGULAR"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "DEGREE"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "ResistivityTimeSeries": {
      "type": "object",
      "title": "ResistivityTimeSeries",
      "description": "{{def(SampleEnum::RESISTIVITY)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "RESISTIVITY"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "OHM_METER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "FillHeightTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "FillHeight",
          "description": "{{def(SampleEnum::FILL_HEIGHT)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "MILLIMETER"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "FILL_HEIGHT"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "FillHeightTimeSeries"
    },
    "ParticleCountTimeSeries": {
      "allOf": [
        {
          "$ref": "#/$defs/Sample"
        },
        {
          "type": "object",
          "title": "ParticleCount",
          "description": "{{def(SampleEnum::PARTICLE_COUNT)}}",
          "properties": {
            "units": {
              "$ref": "#/$defs/UnitEnum",
              "default": "COUNT"
            },
            "type": {
              "$ref": "#/$defs/SampleEnum",
              "default": "PARTICLE_COUNT"
            },
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "enum": [
                    "UNAVAILABLE"
                  ]
                }
              ]
            }
          }
        }
      ],
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "title": "ParticleCountTimeSeries"
    },
    "ParticleSizeTimeSeries": {
      "type": "object",
      "title": "ParticleSizeTimeSeries",
      "description": "{{def(SampleEnum::PARTICLE_SIZE)}}",
      "properties": {
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/SampleEnum",
          "default": "PARTICLE_SIZE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "default": "MILLIMETER"
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "duration": {
          "type": "number",
          "description": "time-period over which the data was collected. {{property(Sample::duration)}} **MUST** be provided when the {{property(DataItem::statistic)}} is defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        },
        "sampleRate": {
          "type": "number",
          "description": "rate at which successive samples of the value are recorded."
        },
        "statistic": {
          "$ref": "#/$defs/StatisticEnum",
          "description": "type of statistical calculation defined by the {{property(DataItem::statistic)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sampleCount": {
          "type": "integer"
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp",
        "value"
      ],
      "additionalProperties": false
    },
    "VariableDataSet": {
      "type": "object",
      "title": "VariableDataSet",
      "description": "{{def(EventEnum::VARIABLE)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "VARIABLE"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WorkOffsetTable": {
      "type": "object",
      "title": "WorkOffsetTable",
      "description": "{{def(EventEnum::WORK_OFFSET)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "WORK_OFFSET"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AlarmLimitDataSet": {
      "type": "object",
      "title": "AlarmLimitDataSet",
      "description": "{{def(EventEnum::ALARM_LIMIT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ALARM_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SpecificationLimitDataSet": {
      "type": "object",
      "title": "SpecificationLimitDataSet",
      "description": "{{def(EventEnum::SPECIFICATION_LIMIT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "SPECIFICATION_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SensorAttachmentDataSet": {
      "type": "object",
      "title": "SensorAttachmentDataSet",
      "description": "{{def(EventEnum::SENSOR_ATTACHMENT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "SENSOR_ATTACHMENT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ControlLimitDataSet": {
      "type": "object",
      "title": "ControlLimitDataSet",
      "description": "{{def(EventEnum::CONTROL_LIMIT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "CONTROL_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AssetCountDataSet": {
      "type": "object",
      "title": "AssetCountDataSet",
      "description": "{{def(EventEnum::ASSET_COUNT)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ASSET_COUNT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ComponentDataTable": {
      "type": "object",
      "title": "ComponentDataTable",
      "description": "tabular {{def(EventEnum::COMPONENT_DATA)}} If the {{block(Component)}} multiplicity can be determined, the device model **MUST** use a fixed set of {{block(Component)}}s. {{block(ComponentData)}} **MUST** provide a {{block(DataItem)}} {{block(Definition)}}.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "COMPONENT_DATA"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "WorkOffsetsTable": {
      "type": "object",
      "title": "WorkOffsetsTable",
      "description": "tabular representation of {{def(EventEnum::WORK_OFFSETS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "WORK_OFFSETS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ToolOffsetsTable": {
      "type": "object",
      "title": "ToolOffsetsTable",
      "description": "tabular representation of {{def(EventEnum::TOOL_OFFSETS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "TOOL_OFFSETS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "FeatureMeasurementTable": {
      "type": "object",
      "title": "FeatureMeasurementTable",
      "description": "tabular representation of {{def(EventEnum::FEATURE_MEASUREMENT)}} {{block(FeatureMeasurement)}} **MAY** include a {{term(characteristic)}} in which case it **MAY** include a `CHARACTERISTIC_STATUS`.",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "FEATURE_MEASUREMENT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "AlarmLimitsDataSet": {
      "type": "object",
      "title": "AlarmLimitsDataSet",
      "description": "{{def(EventEnum::ALARM_LIMITS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "ALARM_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "ControlLimitsDataSet": {
      "type": "object",
      "title": "ControlLimitsDataSet",
      "description": "{{def(EventEnum::CONTROL_LIMITS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "CONTROL_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "SpecificationLimitsDataSet": {
      "type": "object",
      "title": "SpecificationLimitsDataSet",
      "description": "{{def(EventEnum::SPECIFICATION_LIMITS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "SPECIFICATION_LIMIT"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "LocationAddressDataSet": {
      "type": "object",
      "title": "LocationAddressDataSet",
      "description": "{{def(EventEnum::LOCATION_ADDRESS)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "LOCATION_ADDRESS"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    },
    "LocationSpatialGeographicDataSet": {
      "type": "object",
      "title": "LocationSpatialGeographicDataSet",
      "description": "{{def(EventEnum::LOCATION_SPATIAL_GEOGRAPHIC)}}",
      "properties": {
        "value": {
          "oneOf": [
            {
              "type": "object"
            },
            {
              "type": "string",
              "enum": [
                "UNAVAILABLE"
              ]
            }
          ]
        },
        "assetType": {
          "type": "string"
        },
        "count": {
          "type": "integer"
        },
        "compositionId": {
          "type": "string",
          "description": "identifier of the {{block(Composition)}} entity defined in the {{term(MTConnectDevices Response Document)}} associated with the data reported for the {{block(Observation)}}."
        },
        "dataItemId": {
          "type": "string",
          "description": "unique identifier of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::dataItemId)}} **MUST** match the associated {{property(DataItem::id)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "name": {
          "type": "string",
          "description": "name of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::name)}} **MUST** match the associated {{property(DataItem::name)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "sequence": {
          "type": "integer",
          "description": "number representing the sequential position of an occurrence of an {{term(observation)}} in the data buffer of an {{term(agent)}}. {{property(Observation::sequence)}} **MUST** have a value represented as an unsigned 64-bit value from 1 to $$ 2^{64}-1 $$."
        },
        "subType": {
          "$ref": "#/$defs/DataItemSubTypeEnum",
          "description": "subtype of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::subType)}} **MUST** match the associated {{property(DataItem::subType)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "timestamp": {
          "type": "string",
          "description": "most accurate time available to a piece of equipment that represents the point in time that the data reported was measured."
        },
        "type": {
          "$ref": "#/$defs/EventEnum",
          "default": "LOCATION_SPATIAL_GEOGRAPHIC"
        },
        "units": {
          "$ref": "#/$defs/UnitEnum",
          "description": "units of the {{block(DataItem)}} associated with this {{block(Observation)}}. {{property(Observation::units)}} **MUST** match the associated {{property(DataItem::units)}} defined in the {{term(MTConnectDevices Response Document)}}."
        },
        "isUnavailable": {
          "type": "boolean",
          "description": "when `true`, {{property(Observation::result)}} is indeterminate. > Note 1 to entry: In {{term(XML)}}, when `isUnavailable` is `true`, the {{term(XML)}} `CDATA` of the `Observation` **MUST** be `UNAVAILABLE`. ```xml <Execution dataItemId=\"...\" ...>UNAVAILABLE</Execution> ``` > Note 2 to entry: In {{term(JSON)}}, when `isUnavailable` is `true`, the {{term(JSON)}} `value` of the `Observation` **MUST** be `UNAVAILABLE`. ```json \"Execution\" : [ { \"dataItemId\": \"...\" ..., \"value\": \"UNAVAILABLE\" } ] ```",
          "default": "true"
        },
        "quality": {
          "$ref": "#/$defs/QualityEnum",
          "description": "indicates if the {{block(Observation)}} is verifiable and is in accordance with the normative definitions within the MTConnect Standard.",
          "default": "UNVERIFIABLE"
        },
        "deprecated": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been deprecated in the MTConnect Standard.",
          "default": "false"
        },
        "extended": {
          "type": "boolean",
          "description": "indicates if the {{block(Observation)}} has any property or controlled vocabulary that has been extended and cannot be validated.",
          "default": "false"
        },
        "resetTriggered": {
          "$ref": "#/$defs/ResetTriggeredEnum",
          "description": "identifies when a reported value has been reset and what has caused that reset to occur for those {{block(DataItem)}} entities that may be periodically reset to an initial value. `resetTriggered` **MUST** only be provided for the specific occurrence of a {{block(DataItem)}} reported in the {{term(MTConnectStreams Response Document)}} when the reset occurred."
        }
      },
      "required": [
        "dataItemId",
        "sequence",
        "timestamp"
      ],
      "additionalProperties": false
    }
  }
}