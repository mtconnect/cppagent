#pragma once

#include <string>

// the configured options and settings for MTConnect Agent
constexpr const int AGENT_VERSION_MAJOR = @AGENT_VERSION_MAJOR@;
constexpr const int AGENT_VERSION_MINOR = @AGENT_VERSION_MINOR@;
constexpr const int AGENT_VERSION_PATCH = @AGENT_VERSION_PATCH@;
constexpr const int AGENT_VERSION_BUILD = @AGENT_VERSION_BUILD@;
constexpr const char* AGENT_VERSION_RC = "@AGENT_VERSION_RC@";

extern void PrintMTConnectAgentVersion();
extern std::string GetAgentVersion();
