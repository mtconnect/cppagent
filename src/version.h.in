#include <string>

// the configured options and settings for MTConnect Agent
const int AGENT_VERSION_MAJOR = @AGENT_VERSION_MAJOR@;
const int AGENT_VERSION_MINOR = @AGENT_VERSION_MINOR@;
const int AGENT_VERSION_PATCH = @AGENT_VERSION_PATCH@;
const int AGENT_VERSION_BUILD = @AGENT_VERSION_BUILD@;
const char* AGENT_VERSION_RC = "@AGENT_VERSION_RC@";

extern void PrintMTConnectAgentVersion();
extern std::string GetAgentVersion();
